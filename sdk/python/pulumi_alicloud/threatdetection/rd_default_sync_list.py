# coding=utf-8
# *** WARNING: this file was generated by pulumi-language-python. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import builtins as _builtins
import warnings
import sys
import pulumi
import pulumi.runtime
from typing import Any, Mapping, Optional, Sequence, Union, overload
if sys.version_info >= (3, 11):
    from typing import NotRequired, TypedDict, TypeAlias
else:
    from typing_extensions import NotRequired, TypedDict, TypeAlias
from .. import _utilities

__all__ = ['RdDefaultSyncListArgs', 'RdDefaultSyncList']

@pulumi.input_type
class RdDefaultSyncListArgs:
    def __init__(__self__, *,
                 folder_ids: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None):
        """
        The set of arguments for constructing a RdDefaultSyncList resource.

        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] folder_ids: The list of resource directory folder ids to be synchronized by Threat Detection. Setting an empty list clears the synchronized folders (disables the default synchronization). Folder ids can be obtained via the `GetRdTree` API.
        """
        if folder_ids is not None:
            pulumi.set(__self__, "folder_ids", folder_ids)

    @_builtins.property
    @pulumi.getter(name="folderIds")
    def folder_ids(self) -> pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]]:
        """
        The list of resource directory folder ids to be synchronized by Threat Detection. Setting an empty list clears the synchronized folders (disables the default synchronization). Folder ids can be obtained via the `GetRdTree` API.
        """
        return pulumi.get(self, "folder_ids")

    @folder_ids.setter
    def folder_ids(self, value: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]]):
        pulumi.set(self, "folder_ids", value)


@pulumi.input_type
class _RdDefaultSyncListState:
    def __init__(__self__, *,
                 folder_ids: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None):
        """
        Input properties used for looking up and filtering RdDefaultSyncList resources.

        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] folder_ids: The list of resource directory folder ids to be synchronized by Threat Detection. Setting an empty list clears the synchronized folders (disables the default synchronization). Folder ids can be obtained via the `GetRdTree` API.
        """
        if folder_ids is not None:
            pulumi.set(__self__, "folder_ids", folder_ids)

    @_builtins.property
    @pulumi.getter(name="folderIds")
    def folder_ids(self) -> pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]]:
        """
        The list of resource directory folder ids to be synchronized by Threat Detection. Setting an empty list clears the synchronized folders (disables the default synchronization). Folder ids can be obtained via the `GetRdTree` API.
        """
        return pulumi.get(self, "folder_ids")

    @folder_ids.setter
    def folder_ids(self, value: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]]):
        pulumi.set(self, "folder_ids", value)


@pulumi.type_token("alicloud:threatdetection/rdDefaultSyncList:RdDefaultSyncList")
class RdDefaultSyncList(pulumi.CustomResource):
    @overload
    def __init__(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 folder_ids: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
                 __props__=None):
        """
        Provides a Threat Detection Rd Default Sync List resource. The default synchronization list of resource directory folders for Threat Detection. The resource is account-level (singleton): each Alibaba Cloud account holds at most one such list, and applying a new configuration replaces the previous folder list in full (set/replace semantics). Setting an empty folder list clears the synchronized folders, which is equivalent to disabling the default synchronization.

        For information about Threat Detection Rd Default Sync List and how to use it, see [CreateRdDefaultSyncList](https://next.api.alibabacloud.com/document/Sas/2018-12-03/CreateRdDefaultSyncList) and [ListRdDefaultSyncList](https://next.api.alibabacloud.com/document/Sas/2018-12-03/ListRdDefaultSyncList).

        > **NOTE:** Available since v1.292.0.

        > **NOTE:** The resource folder ids can be obtained via the `GetRdTree` API. A resource directory management account or a Threat Detection delegated administrator account is required to call these APIs.

        > **NOTE:** This resource is account-level (singleton): each account holds at most one synchronization list. When adopting an account that already holds a list (for example after `pulumi import`), declare `folder_ids` in the configuration with the current folder ids. Omitting `folder_ids` on create will not send the `FolderIds` parameter, so the existing synchronized list is not wiped out; to clear the list intentionally, set `folder_ids = []`.

        > **NOTE:** Declaring multiple `threatdetection.RdDefaultSyncList` resources for the same account is not recommended. Each apply replaces the entire folder list (set/replace semantics), so the last applied configuration wins and resources created earlier will show drift on the next plan. Always pass the full set of `folder_ids` on every update; omitting them clears the existing list.

        ## Example Usage

        Basic Usage

        ```python
        import pulumi
        import pulumi_alicloud as alicloud

        config = pulumi.Config()
        name = config.get("name")
        if name is None:
            name = "terraform-example"
        default = alicloud.threatdetection.RdDefaultSyncList("default", folder_ids=[
            "fd-xxxxx",
            "fd-yyyyy",
        ])
        ```

        📚 Need more examples? VIEW MORE EXAMPLES

        ## Import

        Threat Detection Rd Default Sync List can be imported using the id, e.g. the Alibaba Cloud account id, or an empty string to import the current account's list:

        ```sh
        $ terraform import alicloud_threat_detection_rd_default_sync_list.example
        ```


        :param str resource_name: The name of the resource.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] folder_ids: The list of resource directory folder ids to be synchronized by Threat Detection. Setting an empty list clears the synchronized folders (disables the default synchronization). Folder ids can be obtained via the `GetRdTree` API.
        """
        ...
    @overload
    def __init__(__self__,
                 resource_name: str,
                 args: Optional[RdDefaultSyncListArgs] = None,
                 opts: Optional[pulumi.ResourceOptions] = None):
        """
        Provides a Threat Detection Rd Default Sync List resource. The default synchronization list of resource directory folders for Threat Detection. The resource is account-level (singleton): each Alibaba Cloud account holds at most one such list, and applying a new configuration replaces the previous folder list in full (set/replace semantics). Setting an empty folder list clears the synchronized folders, which is equivalent to disabling the default synchronization.

        For information about Threat Detection Rd Default Sync List and how to use it, see [CreateRdDefaultSyncList](https://next.api.alibabacloud.com/document/Sas/2018-12-03/CreateRdDefaultSyncList) and [ListRdDefaultSyncList](https://next.api.alibabacloud.com/document/Sas/2018-12-03/ListRdDefaultSyncList).

        > **NOTE:** Available since v1.292.0.

        > **NOTE:** The resource folder ids can be obtained via the `GetRdTree` API. A resource directory management account or a Threat Detection delegated administrator account is required to call these APIs.

        > **NOTE:** This resource is account-level (singleton): each account holds at most one synchronization list. When adopting an account that already holds a list (for example after `pulumi import`), declare `folder_ids` in the configuration with the current folder ids. Omitting `folder_ids` on create will not send the `FolderIds` parameter, so the existing synchronized list is not wiped out; to clear the list intentionally, set `folder_ids = []`.

        > **NOTE:** Declaring multiple `threatdetection.RdDefaultSyncList` resources for the same account is not recommended. Each apply replaces the entire folder list (set/replace semantics), so the last applied configuration wins and resources created earlier will show drift on the next plan. Always pass the full set of `folder_ids` on every update; omitting them clears the existing list.

        ## Example Usage

        Basic Usage

        ```python
        import pulumi
        import pulumi_alicloud as alicloud

        config = pulumi.Config()
        name = config.get("name")
        if name is None:
            name = "terraform-example"
        default = alicloud.threatdetection.RdDefaultSyncList("default", folder_ids=[
            "fd-xxxxx",
            "fd-yyyyy",
        ])
        ```

        📚 Need more examples? VIEW MORE EXAMPLES

        ## Import

        Threat Detection Rd Default Sync List can be imported using the id, e.g. the Alibaba Cloud account id, or an empty string to import the current account's list:

        ```sh
        $ terraform import alicloud_threat_detection_rd_default_sync_list.example
        ```


        :param str resource_name: The name of the resource.
        :param RdDefaultSyncListArgs args: The arguments to use to populate this resource's properties.
        :param pulumi.ResourceOptions opts: Options for the resource.
        """
        ...
    def __init__(__self__, resource_name: str, *args, **kwargs):
        resource_args, opts = _utilities.get_resource_args_opts(RdDefaultSyncListArgs, pulumi.ResourceOptions, *args, **kwargs)
        if resource_args is not None:
            __self__._internal_init(resource_name, opts, **resource_args.__dict__)
        else:
            __self__._internal_init(resource_name, *args, **kwargs)

    def _internal_init(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 folder_ids: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
                 __props__=None):
        opts = pulumi.ResourceOptions.merge(_utilities.get_resource_opts_defaults(), opts)
        if not isinstance(opts, pulumi.ResourceOptions):
            raise TypeError('Expected resource options to be a ResourceOptions instance')
        if opts.id is None:
            if __props__ is not None:
                raise TypeError('__props__ is only valid when passed in combination with a valid opts.id to get an existing resource')
            __props__ = RdDefaultSyncListArgs.__new__(RdDefaultSyncListArgs)

            __props__.__dict__["folder_ids"] = folder_ids
        super(RdDefaultSyncList, __self__).__init__(
            'alicloud:threatdetection/rdDefaultSyncList:RdDefaultSyncList',
            resource_name,
            __props__,
            opts)

    @staticmethod
    def get(resource_name: str,
            id: pulumi.Input[str],
            opts: Optional[pulumi.ResourceOptions] = None,
            folder_ids: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None) -> 'RdDefaultSyncList':
        """
        Get an existing RdDefaultSyncList resource's state with the given name, id, and optional extra
        properties used to qualify the lookup.

        :param str resource_name: The unique name of the resulting resource.
        :param pulumi.Input[str] id: The unique provider ID of the resource to lookup.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] folder_ids: The list of resource directory folder ids to be synchronized by Threat Detection. Setting an empty list clears the synchronized folders (disables the default synchronization). Folder ids can be obtained via the `GetRdTree` API.
        """
        opts = pulumi.ResourceOptions.merge(opts, pulumi.ResourceOptions(id=id))

        __props__ = _RdDefaultSyncListState.__new__(_RdDefaultSyncListState)

        __props__.__dict__["folder_ids"] = folder_ids
        return RdDefaultSyncList(resource_name, opts=opts, __props__=__props__)

    @_builtins.property
    @pulumi.getter(name="folderIds")
    def folder_ids(self) -> pulumi.Output[Optional[Sequence[_builtins.str]]]:
        """
        The list of resource directory folder ids to be synchronized by Threat Detection. Setting an empty list clears the synchronized folders (disables the default synchronization). Folder ids can be obtained via the `GetRdTree` API.
        """
        return pulumi.get(self, "folder_ids")

