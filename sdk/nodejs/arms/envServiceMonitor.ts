// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provides a ARMS Env Service Monitor resource. ServiceMonitor for the arms environment.
 *
 * For information about ARMS Env Service Monitor and how to use it, see [What is Env Service Monitor](https://www.alibabacloud.com/help/en/arms/developer-reference/api-arms-2019-08-08-createenvservicemonitor).
 *
 * > **NOTE:** Available since v1.212.0.
 *
 * ## Example Usage
 *
 * Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 * import * as random from "@pulumi/random";
 * import * as std from "@pulumi/std";
 *
 * const defaultInteger = new random.index.Integer("default", {
 *     max: 99999,
 *     min: 10000,
 * });
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const enhanced = alicloud.vpc.getEnhancedNatAvailableZones({});
 * const vpc = new alicloud.vpc.Network("vpc", {
 *     description: name,
 *     cidrBlock: "192.168.0.0/16",
 *     vpcName: name,
 * });
 * const vswitch = new alicloud.vpc.Switch("vswitch", {
 *     description: name,
 *     vpcId: vpc.id,
 *     vswitchName: name,
 *     zoneId: enhanced.then(enhanced => enhanced.zones?.[0]?.zoneId),
 *     cidrBlock: vpc.cidrBlock.apply(cidrBlock => std.cidrsubnetOutput({
 *         input: cidrBlock,
 *         newbits: 8,
 *         netnum: 8,
 *     })).apply(invoke => invoke.result),
 * });
 * const defaultSnapshotPolicy = new alicloud.ecs.SnapshotPolicy("default", {
 *     name: name,
 *     repeatWeekdays: [
 *         "1",
 *         "2",
 *         "3",
 *     ],
 *     retentionDays: -1,
 *     timePoints: [
 *         "1",
 *         "22",
 *         "23",
 *     ],
 * });
 * const _default = vswitch.zoneId.apply(zoneId => alicloud.ecs.getInstanceTypesOutput({
 *     availabilityZone: zoneId,
 *     cpuCoreCount: 2,
 *     memorySize: 4,
 *     kubernetesNodeRole: "Worker",
 *     instanceTypeFamily: "ecs.n1",
 * }));
 * const defaultManagedKubernetes = new alicloud.cs.ManagedKubernetes("default", {
 *     name: `terraform-example-${defaultInteger.result}`,
 *     clusterSpec: "ack.pro.small",
 *     version: "1.24.6-aliyun.1",
 *     newNatGateway: true,
 *     nodeCidrMask: 26,
 *     proxyMode: "ipvs",
 *     serviceCidr: "172.23.0.0/16",
 *     podCidr: "10.95.0.0/16",
 *     workerVswitchIds: [vswitch.id],
 * });
 * const defaultKeyPair = new alicloud.ecs.KeyPair("default", {keyPairName: `terraform-example-${defaultInteger.result}`});
 * const defaultNodePool = new alicloud.cs.NodePool("default", {
 *     nodePoolName: "desired_size",
 *     clusterId: defaultManagedKubernetes.id,
 *     vswitchIds: [vswitch.id],
 *     instanceTypes: [_default.apply(_default => _default.instanceTypes?.[0]?.id)],
 *     systemDiskCategory: "cloud_efficiency",
 *     systemDiskSize: 40,
 *     keyName: defaultKeyPair.keyPairName,
 *     desiredSize: "2",
 * });
 * const defaultEnvironment = new alicloud.arms.Environment("default", {
 *     bindResourceId: defaultNodePool.clusterId,
 *     environmentSubType: "ManagedKubernetes",
 *     environmentType: "CS",
 *     environmentName: `terraform-example-${defaultInteger.result}`,
 * });
 * const defaultEnvServiceMonitor = new alicloud.arms.EnvServiceMonitor("default", {
 *     environmentId: defaultEnvironment.id,
 *     configYaml: `apiVersion: monitoring.coreos.com/v1
 * kind: ServiceMonitor
 * metadata:
 *   name: arms-admin1
 *   namespace: arms-prom
 *   annotations:
 *     arms.prometheus.io/discovery: 'true'
 *     o11y.aliyun.com/addon-name: mysql
 *     o11y.aliyun.com/addon-version: 1.0.1
 *     o11y.aliyun.com/release-name: mysql1
 * spec:
 *   endpoints:
 *   - interval: 30s
 *     port: operator
 *     path: /metrics
 *   - interval: 10s
 *     port: operator1
 *     path: /metrics
 *   namespaceSelector:
 *     any: true
 *   selector:
 *     matchLabels:
 *      app: arms-prometheus-ack-arms-prometheus
 * `,
 *     aliyunLang: "zh",
 * });
 * ```
 *
 * ## Import
 *
 * ARMS Env Service Monitor can be imported using the id, e.g.
 *
 * ```sh
 * $ pulumi import alicloud:arms/envServiceMonitor:EnvServiceMonitor example <environment_id>:<namespace>:<env_service_monitor_name>
 * ```
 */
export class EnvServiceMonitor extends pulumi.CustomResource {
    /**
     * Get an existing EnvServiceMonitor resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: EnvServiceMonitorState, opts?: pulumi.CustomResourceOptions): EnvServiceMonitor {
        return new EnvServiceMonitor(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'alicloud:arms/envServiceMonitor:EnvServiceMonitor';

    /**
     * Returns true if the given object is an instance of EnvServiceMonitor.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is EnvServiceMonitor {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === EnvServiceMonitor.__pulumiType;
    }

    /**
     * Language environment, default is Chinese zh | en.
     */
    declare public readonly aliyunLang: pulumi.Output<string | undefined>;
    /**
     * Yaml configuration string.
     */
    declare public readonly configYaml: pulumi.Output<string>;
    /**
     * The name of the resource.
     */
    declare public /*out*/ readonly envServiceMonitorName: pulumi.Output<string>;
    /**
     * Environment id.
     */
    declare public readonly environmentId: pulumi.Output<string>;
    /**
     * The namespace where the resource is located.
     */
    declare public /*out*/ readonly namespace: pulumi.Output<string>;
    /**
     * Status: run, stop.
     */
    declare public /*out*/ readonly status: pulumi.Output<string>;

    /**
     * Create a EnvServiceMonitor resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: EnvServiceMonitorArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: EnvServiceMonitorArgs | EnvServiceMonitorState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as EnvServiceMonitorState | undefined;
            resourceInputs["aliyunLang"] = state?.aliyunLang;
            resourceInputs["configYaml"] = state?.configYaml;
            resourceInputs["envServiceMonitorName"] = state?.envServiceMonitorName;
            resourceInputs["environmentId"] = state?.environmentId;
            resourceInputs["namespace"] = state?.namespace;
            resourceInputs["status"] = state?.status;
        } else {
            const args = argsOrState as EnvServiceMonitorArgs | undefined;
            if (args?.configYaml === undefined && !opts.urn) {
                throw new Error("Missing required property 'configYaml'");
            }
            if (args?.environmentId === undefined && !opts.urn) {
                throw new Error("Missing required property 'environmentId'");
            }
            resourceInputs["aliyunLang"] = args?.aliyunLang;
            resourceInputs["configYaml"] = args?.configYaml;
            resourceInputs["environmentId"] = args?.environmentId;
            resourceInputs["envServiceMonitorName"] = undefined /*out*/;
            resourceInputs["namespace"] = undefined /*out*/;
            resourceInputs["status"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(EnvServiceMonitor.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering EnvServiceMonitor resources.
 */
export interface EnvServiceMonitorState {
    /**
     * Language environment, default is Chinese zh | en.
     */
    aliyunLang?: pulumi.Input<string>;
    /**
     * Yaml configuration string.
     */
    configYaml?: pulumi.Input<string>;
    /**
     * The name of the resource.
     */
    envServiceMonitorName?: pulumi.Input<string>;
    /**
     * Environment id.
     */
    environmentId?: pulumi.Input<string>;
    /**
     * The namespace where the resource is located.
     */
    namespace?: pulumi.Input<string>;
    /**
     * Status: run, stop.
     */
    status?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a EnvServiceMonitor resource.
 */
export interface EnvServiceMonitorArgs {
    /**
     * Language environment, default is Chinese zh | en.
     */
    aliyunLang?: pulumi.Input<string>;
    /**
     * Yaml configuration string.
     */
    configYaml: pulumi.Input<string>;
    /**
     * Environment id.
     */
    environmentId: pulumi.Input<string>;
}
