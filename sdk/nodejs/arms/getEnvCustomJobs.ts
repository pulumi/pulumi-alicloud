// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * This data source provides the ARMS Env Custom Jobs of the current Alibaba Cloud user.
 *
 * > **NOTE:** Available since v1.258.0.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 * import * as random from "@pulumi/random";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const defaultInteger = new random.index.Integer("default", {
 *     min: 10000,
 *     max: 99999,
 * });
 * const _default = alicloud.vpc.getNetworks({
 *     nameRegex: "^default-NODELETING$",
 * });
 * const defaultEnvironment = new alicloud.arms.Environment("default", {
 *     bindResourceId: _default.then(_default => _default.ids?.[0]),
 *     environmentSubType: "ECS",
 *     environmentType: "ECS",
 *     environmentName: `${name}-${defaultInteger.result}`,
 *     tags: {
 *         Created: "TF",
 *         For: "Environment",
 *     },
 * });
 * const defaultEnvCustomJob = new alicloud.arms.EnvCustomJob("default", {
 *     status: "run",
 *     environmentId: defaultEnvironment.id,
 *     envCustomJobName: `${name}-${defaultInteger.result}`,
 *     configYaml: `scrape_configs:
 * - job_name: job-demo1
 *   honor_timestamps: false
 *   honor_labels: false
 *   scrape_interval: 30s
 *   scheme: http
 *   metrics_path: /metric
 *   static_configs:
 *   - targets:
 *     - 127.0.0.1:9090
 * `,
 *     aliyunLang: "en",
 * });
 * const ids = alicloud.arms.getEnvCustomJobsOutput({
 *     environmentId: defaultEnvCustomJob.environmentId,
 *     ids: [defaultEnvCustomJob.id],
 * });
 * export const armsEnvCustomJobsId0 = ids.apply(ids => ids.jobs?.[0]?.id);
 * ```
 */
export function getEnvCustomJobs(args: GetEnvCustomJobsArgs, opts?: pulumi.InvokeOptions): Promise<GetEnvCustomJobsResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("alicloud:arms/getEnvCustomJobs:getEnvCustomJobs", {
        "environmentId": args.environmentId,
        "ids": args.ids,
        "nameRegex": args.nameRegex,
        "outputFile": args.outputFile,
    }, opts);
}

/**
 * A collection of arguments for invoking getEnvCustomJobs.
 */
export interface GetEnvCustomJobsArgs {
    /**
     * The ID of the environment instance.
     */
    environmentId: string;
    /**
     * A list of ARMS Env Custom Job IDs.
     */
    ids?: string[];
    /**
     * A regex string to filter results by ARMS Env Custom Job name.
     */
    nameRegex?: string;
    /**
     * File name where to save data source results (after running `pulumi preview`).
     */
    outputFile?: string;
}

/**
 * A collection of values returned by getEnvCustomJobs.
 */
export interface GetEnvCustomJobsResult {
    /**
     * The ID of the environment instance.
     */
    readonly environmentId: string;
    /**
     * The provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
    readonly ids: string[];
    /**
     * A list of ARMS Env Custom Jobs. Each element contains the following attributes:
     */
    readonly jobs: outputs.arms.GetEnvCustomJobsJob[];
    readonly nameRegex?: string;
    /**
     * A list of ARMS Env Custom Job names.
     */
    readonly names: string[];
    readonly outputFile?: string;
}
/**
 * This data source provides the ARMS Env Custom Jobs of the current Alibaba Cloud user.
 *
 * > **NOTE:** Available since v1.258.0.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 * import * as random from "@pulumi/random";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const defaultInteger = new random.index.Integer("default", {
 *     min: 10000,
 *     max: 99999,
 * });
 * const _default = alicloud.vpc.getNetworks({
 *     nameRegex: "^default-NODELETING$",
 * });
 * const defaultEnvironment = new alicloud.arms.Environment("default", {
 *     bindResourceId: _default.then(_default => _default.ids?.[0]),
 *     environmentSubType: "ECS",
 *     environmentType: "ECS",
 *     environmentName: `${name}-${defaultInteger.result}`,
 *     tags: {
 *         Created: "TF",
 *         For: "Environment",
 *     },
 * });
 * const defaultEnvCustomJob = new alicloud.arms.EnvCustomJob("default", {
 *     status: "run",
 *     environmentId: defaultEnvironment.id,
 *     envCustomJobName: `${name}-${defaultInteger.result}`,
 *     configYaml: `scrape_configs:
 * - job_name: job-demo1
 *   honor_timestamps: false
 *   honor_labels: false
 *   scrape_interval: 30s
 *   scheme: http
 *   metrics_path: /metric
 *   static_configs:
 *   - targets:
 *     - 127.0.0.1:9090
 * `,
 *     aliyunLang: "en",
 * });
 * const ids = alicloud.arms.getEnvCustomJobsOutput({
 *     environmentId: defaultEnvCustomJob.environmentId,
 *     ids: [defaultEnvCustomJob.id],
 * });
 * export const armsEnvCustomJobsId0 = ids.apply(ids => ids.jobs?.[0]?.id);
 * ```
 */
export function getEnvCustomJobsOutput(args: GetEnvCustomJobsOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetEnvCustomJobsResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("alicloud:arms/getEnvCustomJobs:getEnvCustomJobs", {
        "environmentId": args.environmentId,
        "ids": args.ids,
        "nameRegex": args.nameRegex,
        "outputFile": args.outputFile,
    }, opts);
}

/**
 * A collection of arguments for invoking getEnvCustomJobs.
 */
export interface GetEnvCustomJobsOutputArgs {
    /**
     * The ID of the environment instance.
     */
    environmentId: pulumi.Input<string>;
    /**
     * A list of ARMS Env Custom Job IDs.
     */
    ids?: pulumi.Input<pulumi.Input<string>[]>;
    /**
     * A regex string to filter results by ARMS Env Custom Job name.
     */
    nameRegex?: pulumi.Input<string>;
    /**
     * File name where to save data source results (after running `pulumi preview`).
     */
    outputFile?: pulumi.Input<string>;
}
