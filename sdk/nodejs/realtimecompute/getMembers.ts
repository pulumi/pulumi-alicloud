// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * This data source provides the Realtime Compute Member of the current Alibaba Cloud user.
 *
 * > **NOTE:** Available since v1.292.0.
 *
 * ## Example Usage
 *
 * Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const _default = alicloud.oss.getBuckets({});
 * const defaultNetwork = new alicloud.vpc.Network("default", {
 *     isDefault: false,
 *     cidrBlock: "172.16.0.0/16",
 *     vpcName: name,
 * });
 * const defaultSwitch = new alicloud.vpc.Switch("default", {
 *     isDefault: false,
 *     vpcId: defaultNetwork.id,
 *     zoneId: "cn-hangzhou-i",
 *     cidrBlock: "172.16.0.0/24",
 *     vswitchName: name,
 * });
 * const defaultUser = new alicloud.ram.User("default", {
 *     name: name,
 *     displayName: "displayname",
 *     mobile: "86-18888888888",
 *     email: "hello.uuu@aaa.com",
 *     comments: "yoyoyo",
 * });
 * const defaultVvpInstance = new alicloud.realtimecompute.VvpInstance("default", {
 *     vvpInstanceName: name,
 *     storage: {
 *         oss: {
 *             bucket: _default.then(_default => _default.buckets?.[0]?.name),
 *         },
 *     },
 *     vpcId: defaultNetwork.id,
 *     vswitchIds: [defaultSwitch.id],
 *     resourceSpec: {
 *         cpu: 8,
 *         memoryGb: 32,
 *     },
 *     paymentType: "PayAsYouGo",
 *     zoneId: defaultSwitch.zoneId,
 * });
 * const defaultMember = new alicloud.realtimecompute.Member("default", {
 *     member: defaultUser.id,
 *     namespace: pulumi.interpolate`${defaultVvpInstance.vvpInstanceName}-default`,
 *     resourceId: defaultVvpInstance.resourceId,
 *     role: "viewer",
 * });
 * const ids = alicloud.realtimecompute.getMembersOutput({
 *     ids: [defaultMember.id],
 *     namespace: defaultMember.namespace,
 *     resourceId: defaultMember.resourceId,
 * });
 * export const realtimeComputeMembersId0 = ids.apply(ids => ids.members?.[0]?.id);
 * ```
 */
export function getMembers(args: GetMembersArgs, opts?: pulumi.InvokeOptions): Promise<GetMembersResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("alicloud:realtimecompute/getMembers:getMembers", {
        "ids": args.ids,
        "namespace": args.namespace,
        "outputFile": args.outputFile,
        "resourceId": args.resourceId,
    }, opts);
}

/**
 * A collection of arguments for invoking getMembers.
 */
export interface GetMembersArgs {
    /**
     * A list of Member IDs. It formats as `<resource_id>:<namespace>:<member>`.
     */
    ids?: string[];
    /**
     * The name of the namespace.
     */
    namespace: string;
    /**
     * File name where to save data source results (after running `pulumi preview`).
     */
    outputFile?: string;
    /**
     * The workspace ID.
     */
    resourceId: string;
}

/**
 * A collection of values returned by getMembers.
 */
export interface GetMembersResult {
    /**
     * The provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
    readonly ids: string[];
    /**
     * A list of Members. Each element contains the following attributes:
     */
    readonly members: outputs.realtimecompute.GetMembersMember[];
    /**
     * The name of the namespace.
     */
    readonly namespace: string;
    readonly outputFile?: string;
    /**
     * The workspace ID.
     */
    readonly resourceId: string;
}
/**
 * This data source provides the Realtime Compute Member of the current Alibaba Cloud user.
 *
 * > **NOTE:** Available since v1.292.0.
 *
 * ## Example Usage
 *
 * Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const _default = alicloud.oss.getBuckets({});
 * const defaultNetwork = new alicloud.vpc.Network("default", {
 *     isDefault: false,
 *     cidrBlock: "172.16.0.0/16",
 *     vpcName: name,
 * });
 * const defaultSwitch = new alicloud.vpc.Switch("default", {
 *     isDefault: false,
 *     vpcId: defaultNetwork.id,
 *     zoneId: "cn-hangzhou-i",
 *     cidrBlock: "172.16.0.0/24",
 *     vswitchName: name,
 * });
 * const defaultUser = new alicloud.ram.User("default", {
 *     name: name,
 *     displayName: "displayname",
 *     mobile: "86-18888888888",
 *     email: "hello.uuu@aaa.com",
 *     comments: "yoyoyo",
 * });
 * const defaultVvpInstance = new alicloud.realtimecompute.VvpInstance("default", {
 *     vvpInstanceName: name,
 *     storage: {
 *         oss: {
 *             bucket: _default.then(_default => _default.buckets?.[0]?.name),
 *         },
 *     },
 *     vpcId: defaultNetwork.id,
 *     vswitchIds: [defaultSwitch.id],
 *     resourceSpec: {
 *         cpu: 8,
 *         memoryGb: 32,
 *     },
 *     paymentType: "PayAsYouGo",
 *     zoneId: defaultSwitch.zoneId,
 * });
 * const defaultMember = new alicloud.realtimecompute.Member("default", {
 *     member: defaultUser.id,
 *     namespace: pulumi.interpolate`${defaultVvpInstance.vvpInstanceName}-default`,
 *     resourceId: defaultVvpInstance.resourceId,
 *     role: "viewer",
 * });
 * const ids = alicloud.realtimecompute.getMembersOutput({
 *     ids: [defaultMember.id],
 *     namespace: defaultMember.namespace,
 *     resourceId: defaultMember.resourceId,
 * });
 * export const realtimeComputeMembersId0 = ids.apply(ids => ids.members?.[0]?.id);
 * ```
 */
export function getMembersOutput(args: GetMembersOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetMembersResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("alicloud:realtimecompute/getMembers:getMembers", {
        "ids": args.ids,
        "namespace": args.namespace,
        "outputFile": args.outputFile,
        "resourceId": args.resourceId,
    }, opts);
}

/**
 * A collection of arguments for invoking getMembers.
 */
export interface GetMembersOutputArgs {
    /**
     * A list of Member IDs. It formats as `<resource_id>:<namespace>:<member>`.
     */
    ids?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * The name of the namespace.
     */
    namespace: pulumi.Input<string>;
    /**
     * File name where to save data source results (after running `pulumi preview`).
     */
    outputFile?: pulumi.Input<string | undefined>;
    /**
     * The workspace ID.
     */
    resourceId: pulumi.Input<string>;
}
