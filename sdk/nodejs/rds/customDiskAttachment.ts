// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provides a RDS Custom Disk Attachment resource.
 *
 * Operating cloud disk mount and unmount resources.
 *
 * For information about RDS Custom Disk Attachment and how to use it, see [What is Custom Disk Attachment](https://next.api.alibabacloud.com/document/Rds/2014-08-15/AttachRCDisk).
 *
 * > **NOTE:** Available since v1.275.0.
 *
 * ## Example Usage
 *
 * Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const _default = alicloud.vpc.getNetworks({
 *     nameRegex: "^default-NODELETING$",
 * });
 * const defaultGetSwitches = _default.then(_default => alicloud.vpc.getSwitches({
 *     vpcId: _default.ids?.[0],
 *     zoneId: "cn-hangzhou-i",
 * }));
 * const defaultGetSecurityGroups = _default.then(_default => alicloud.ecs.getSecurityGroups({
 *     vpcId: _default.ids?.[0],
 *     nameRegex: "default-NODELETING",
 * }));
 * const defaultCustom = new alicloud.rds.Custom("default", {
 *     zoneId: defaultGetSwitches.then(defaultGetSwitches => defaultGetSwitches.zoneId),
 *     instanceChargeType: "PostPaid",
 *     vswitchId: defaultGetSwitches.then(defaultGetSwitches => defaultGetSwitches.ids?.[0]),
 *     amount: 1,
 *     securityGroupIds: [defaultGetSecurityGroups.then(defaultGetSecurityGroups => defaultGetSecurityGroups.ids?.[0])],
 *     systemDisk: {
 *         size: "40",
 *     },
 *     force: true,
 *     instanceType: "mysql.x4.xlarge.6cm",
 *     spotStrategy: "NoSpot",
 * });
 * const defaultCustomDisk = new alicloud.rds.CustomDisk("default", {
 *     zoneId: defaultGetSwitches.then(defaultGetSwitches => defaultGetSwitches.zoneId),
 *     size: 40,
 *     diskCategory: "cloud_ssd",
 *     autoPay: true,
 *     diskName: "ran_disk_attach",
 * });
 * const defaultCustomDiskAttachment = new alicloud.rds.CustomDiskAttachment("default", {
 *     instanceId: defaultCustom.id,
 *     diskId: defaultCustomDisk.id,
 * });
 * ```
 *
 * 📚 Need more examples? VIEW MORE EXAMPLES
 *
 * ## Import
 *
 * RDS Custom Disk Attachment can be imported using the id, e.g.
 *
 * ```sh
 * $ pulumi import alicloud:rds/customDiskAttachment:CustomDiskAttachment example <disk_id>:<instance_id>
 * ```
 */
export class CustomDiskAttachment extends pulumi.CustomResource {
    /**
     * Get an existing CustomDiskAttachment resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: CustomDiskAttachmentState, opts?: pulumi.CustomResourceOptions): CustomDiskAttachment {
        return new CustomDiskAttachment(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'alicloud:rds/customDiskAttachment:CustomDiskAttachment';

    /**
     * Returns true if the given object is an instance of CustomDiskAttachment.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is CustomDiskAttachment {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === CustomDiskAttachment.__pulumiType;
    }

    /**
     * Whether the disk is released together with the instance when the instance is released. Value range:
     * true: Release.
     * false: Do not release. The disk is converted to a pay-as-you-go data disk and is retained.
     * When Setting this parameter, you need to pay attention:
     * After the DeleteWithInstance is set to false, once the instance is under security control, the value "LockReason" : "security" is marked in OperationLocks. When the instance is released, the disk will be ignored and released at the same time.
     * If the target disk to be attached is an elastic temporary disk, you must set the DeleteWithInstance parameter to true.
     * This parameter is not supported for cloud disks with the multi-Mount feature enabled.
     */
    declare public readonly deleteWithInstance: pulumi.Output<boolean>;
    /**
     * The ID of the cloud disk to be mounted. The cloud disk ('DiskId') and the instance ('InstanceId') must be in the same zone.
     */
    declare public readonly diskId: pulumi.Output<string>;
    /**
     * The ID of the target RDS Custom instance.
     */
    declare public readonly instanceId: pulumi.Output<string>;
    /**
     * The region ID of the resource.
     */
    declare public /*out*/ readonly regionId: pulumi.Output<string>;
    /**
     * The status of the disk.
     */
    declare public /*out*/ readonly status: pulumi.Output<string>;

    /**
     * Create a CustomDiskAttachment resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: CustomDiskAttachmentArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: CustomDiskAttachmentArgs | CustomDiskAttachmentState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as CustomDiskAttachmentState | undefined;
            resourceInputs["deleteWithInstance"] = state?.deleteWithInstance;
            resourceInputs["diskId"] = state?.diskId;
            resourceInputs["instanceId"] = state?.instanceId;
            resourceInputs["regionId"] = state?.regionId;
            resourceInputs["status"] = state?.status;
        } else {
            const args = argsOrState as CustomDiskAttachmentArgs | undefined;
            if (args?.diskId === undefined && !opts.urn) {
                throw new Error("Missing required property 'diskId'");
            }
            if (args?.instanceId === undefined && !opts.urn) {
                throw new Error("Missing required property 'instanceId'");
            }
            resourceInputs["deleteWithInstance"] = args?.deleteWithInstance;
            resourceInputs["diskId"] = args?.diskId;
            resourceInputs["instanceId"] = args?.instanceId;
            resourceInputs["regionId"] = undefined /*out*/;
            resourceInputs["status"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(CustomDiskAttachment.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering CustomDiskAttachment resources.
 */
export interface CustomDiskAttachmentState {
    /**
     * Whether the disk is released together with the instance when the instance is released. Value range:
     * true: Release.
     * false: Do not release. The disk is converted to a pay-as-you-go data disk and is retained.
     * When Setting this parameter, you need to pay attention:
     * After the DeleteWithInstance is set to false, once the instance is under security control, the value "LockReason" : "security" is marked in OperationLocks. When the instance is released, the disk will be ignored and released at the same time.
     * If the target disk to be attached is an elastic temporary disk, you must set the DeleteWithInstance parameter to true.
     * This parameter is not supported for cloud disks with the multi-Mount feature enabled.
     */
    deleteWithInstance?: pulumi.Input<boolean>;
    /**
     * The ID of the cloud disk to be mounted. The cloud disk ('DiskId') and the instance ('InstanceId') must be in the same zone.
     */
    diskId?: pulumi.Input<string>;
    /**
     * The ID of the target RDS Custom instance.
     */
    instanceId?: pulumi.Input<string>;
    /**
     * The region ID of the resource.
     */
    regionId?: pulumi.Input<string>;
    /**
     * The status of the disk.
     */
    status?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a CustomDiskAttachment resource.
 */
export interface CustomDiskAttachmentArgs {
    /**
     * Whether the disk is released together with the instance when the instance is released. Value range:
     * true: Release.
     * false: Do not release. The disk is converted to a pay-as-you-go data disk and is retained.
     * When Setting this parameter, you need to pay attention:
     * After the DeleteWithInstance is set to false, once the instance is under security control, the value "LockReason" : "security" is marked in OperationLocks. When the instance is released, the disk will be ignored and released at the same time.
     * If the target disk to be attached is an elastic temporary disk, you must set the DeleteWithInstance parameter to true.
     * This parameter is not supported for cloud disks with the multi-Mount feature enabled.
     */
    deleteWithInstance?: pulumi.Input<boolean>;
    /**
     * The ID of the cloud disk to be mounted. The cloud disk ('DiskId') and the instance ('InstanceId') must be in the same zone.
     */
    diskId: pulumi.Input<string>;
    /**
     * The ID of the target RDS Custom instance.
     */
    instanceId: pulumi.Input<string>;
}
