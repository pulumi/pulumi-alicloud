// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provide a whitelist template resource.
 *
 * For information about Resource AliCloudWhitelistTemplate and how to use it, see [What is Whitelist Template](https://www.alibabacloud.com/help/en/rds/developer-reference/api-rds-2014-08-15-modifywhitelisttemplate?).
 *
 * > **NOTE:** Available since v1.254.0.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const example = new alicloud.rds.WhitelistTemplate("example", {
 *     ipWhiteList: "172.16.0.0",
 *     templateName: "example-whitelist",
 * });
 * ```
 *
 * 📚 Need more examples? VIEW MORE EXAMPLES
 *
 * ## Import
 *
 * You can use the whitelist template ID to import whitelist templates, e.g.
 *
 * ```sh
 * $ pulumi import alicloud:rds/whitelistTemplate:WhitelistTemplate example <id>
 * ```
 */
export class WhitelistTemplate extends pulumi.CustomResource {
    /**
     * Get an existing WhitelistTemplate resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: WhitelistTemplateState, opts?: pulumi.CustomResourceOptions): WhitelistTemplate {
        return new WhitelistTemplate(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'alicloud:rds/whitelistTemplate:WhitelistTemplate';

    /**
     * Returns true if the given object is an instance of WhitelistTemplate.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is WhitelistTemplate {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === WhitelistTemplate.__pulumiType;
    }

    /**
     * IP whitelist, multiple IP addresses should be separated by commas (,) and cannot be duplicated.Supports the following two formats:
     * - IP address format, for example: 10.23.XX.XX.
     * - CIDR format, for example: 10.23.XX.XX/24 (no inter domain routing, 24 represents the length of the prefix in the address, ranging from 1 to 32).
     */
    declare public readonly ipWhiteList: pulumi.Output<string>;
    /**
     * Whitelist template name. Passed in when creating a template, and cannot have the same name under the same account, starting with a letter.
     */
    declare public readonly templateName: pulumi.Output<string>;

    /**
     * Create a WhitelistTemplate resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: WhitelistTemplateArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: WhitelistTemplateArgs | WhitelistTemplateState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as WhitelistTemplateState | undefined;
            resourceInputs["ipWhiteList"] = state?.ipWhiteList;
            resourceInputs["templateName"] = state?.templateName;
        } else {
            const args = argsOrState as WhitelistTemplateArgs | undefined;
            if (args?.ipWhiteList === undefined && !opts.urn) {
                throw new Error("Missing required property 'ipWhiteList'");
            }
            if (args?.templateName === undefined && !opts.urn) {
                throw new Error("Missing required property 'templateName'");
            }
            resourceInputs["ipWhiteList"] = args?.ipWhiteList;
            resourceInputs["templateName"] = args?.templateName;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(WhitelistTemplate.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering WhitelistTemplate resources.
 */
export interface WhitelistTemplateState {
    /**
     * IP whitelist, multiple IP addresses should be separated by commas (,) and cannot be duplicated.Supports the following two formats:
     * - IP address format, for example: 10.23.XX.XX.
     * - CIDR format, for example: 10.23.XX.XX/24 (no inter domain routing, 24 represents the length of the prefix in the address, ranging from 1 to 32).
     */
    ipWhiteList?: pulumi.Input<string>;
    /**
     * Whitelist template name. Passed in when creating a template, and cannot have the same name under the same account, starting with a letter.
     */
    templateName?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a WhitelistTemplate resource.
 */
export interface WhitelistTemplateArgs {
    /**
     * IP whitelist, multiple IP addresses should be separated by commas (,) and cannot be duplicated.Supports the following two formats:
     * - IP address format, for example: 10.23.XX.XX.
     * - CIDR format, for example: 10.23.XX.XX/24 (no inter domain routing, 24 represents the length of the prefix in the address, ranging from 1 to 32).
     */
    ipWhiteList: pulumi.Input<string>;
    /**
     * Whitelist template name. Passed in when creating a template, and cannot have the same name under the same account, starting with a letter.
     */
    templateName: pulumi.Input<string>;
}
