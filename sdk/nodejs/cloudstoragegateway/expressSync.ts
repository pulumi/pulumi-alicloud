// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provides a Cloud Storage Gateway Express Sync resource.
 *
 * For information about Cloud Storage Gateway Express Sync and how to use it, see [What is Express Sync](https://www.alibabacloud.com/help/en/cloud-storage-gateway/latest/xzpxo3).
 *
 * > **NOTE:** Available since v1.144.0.
 *
 * ## Example Usage
 *
 * Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 * import * as random from "@pulumi/random";
 * import * as std from "@pulumi/std";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "tf-example";
 * const defaultInteger = new random.index.Integer("default", {
 *     min: 10000,
 *     max: 99999,
 * });
 * const _default = alicloud.getRegions({
 *     current: true,
 * });
 * const defaultUuid = new random.index.Uuid("default", {});
 * const defaultStorageBundle = new alicloud.cloudstoragegateway.StorageBundle("default", {storageBundleName: std.replace({
 *     text: defaultUuid.result,
 *     search: "-",
 *     replace: "",
 * }).then(invoke => std.substr({
 *     input: `tf-example-${invoke.result}`,
 *     offset: 0,
 *     length: 16,
 * })).then(invoke => invoke.result)});
 * const defaultBucket = new alicloud.oss.Bucket("default", {bucket: std.replace({
 *     text: defaultUuid.result,
 *     search: "-",
 *     replace: "",
 * }).then(invoke => std.substr({
 *     input: `tf-example-${invoke.result}`,
 *     offset: 0,
 *     length: 16,
 * })).then(invoke => invoke.result)});
 * const defaultBucketAcl = new alicloud.oss.BucketAcl("default", {
 *     bucket: defaultBucket.bucket,
 *     acl: "public-read-write",
 * });
 * const defaultNetwork = new alicloud.vpc.Network("default", {
 *     vpcName: name,
 *     cidrBlock: "172.16.0.0/12",
 * });
 * const defaultGetStocks = alicloud.cloudstoragegateway.getStocks({
 *     gatewayClass: "Standard",
 * });
 * const defaultSwitch = new alicloud.vpc.Switch("default", {
 *     vpcId: defaultNetwork.id,
 *     cidrBlock: "172.16.0.0/21",
 *     zoneId: defaultGetStocks.then(defaultGetStocks => defaultGetStocks.stocks?.[0]?.zoneId),
 *     vswitchName: name,
 * });
 * const defaultGateway = new alicloud.cloudstoragegateway.Gateway("default", {
 *     gatewayName: name,
 *     description: name,
 *     gatewayClass: "Standard",
 *     type: "File",
 *     paymentType: "PayAsYouGo",
 *     vswitchId: defaultSwitch.id,
 *     releaseAfterExpiration: true,
 *     publicNetworkBandwidth: 40,
 *     storageBundleId: defaultStorageBundle.id,
 *     location: "Cloud",
 * });
 * const defaultGatewayCacheDisk = new alicloud.cloudstoragegateway.GatewayCacheDisk("default", {
 *     cacheDiskCategory: "cloud_efficiency",
 *     gatewayId: defaultGateway.id,
 *     cacheDiskSizeInGb: 50,
 * });
 * const defaultGatewayFileShare = new alicloud.cloudstoragegateway.GatewayFileShare("default", {
 *     gatewayFileShareName: name,
 *     gatewayId: defaultGateway.id,
 *     localPath: defaultGatewayCacheDisk.localFilePath,
 *     ossBucketName: defaultBucket.bucket,
 *     ossEndpoint: defaultBucket.extranetEndpoint,
 *     protocol: "NFS",
 *     remoteSync: true,
 *     pollingInterval: 4500,
 *     feLimit: 0,
 *     backendLimit: 0,
 *     cacheMode: "Cache",
 *     squash: "none",
 *     lagPeriod: 5,
 * });
 * const defaultExpressSync = new alicloud.cloudstoragegateway.ExpressSync("default", {
 *     bucketName: defaultGatewayFileShare.ossBucketName,
 *     bucketRegion: _default.then(_default => _default.regions?.[0]?.id),
 *     description: name,
 *     expressSyncName: `${name}-${defaultInteger.result}`,
 * });
 * ```
 *
 * 📚 Need more examples? VIEW MORE EXAMPLES
 *
 * ## Import
 *
 * Cloud Storage Gateway Express Sync can be imported using the id, e.g.
 *
 * ```sh
 * $ pulumi import alicloud:cloudstoragegateway/expressSync:ExpressSync example <id>
 * ```
 */
export class ExpressSync extends pulumi.CustomResource {
    /**
     * Get an existing ExpressSync resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: ExpressSyncState, opts?: pulumi.CustomResourceOptions): ExpressSync {
        return new ExpressSync(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'alicloud:cloudstoragegateway/expressSync:ExpressSync';

    /**
     * Returns true if the given object is an instance of ExpressSync.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is ExpressSync {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === ExpressSync.__pulumiType;
    }

    /**
     * The name of the OSS Bucket.
     */
    declare public readonly bucketName: pulumi.Output<string>;
    /**
     * The prefix of the OSS Bucket.
     */
    declare public readonly bucketPrefix: pulumi.Output<string | undefined>;
    /**
     * The region of the OSS Bucket.
     */
    declare public readonly bucketRegion: pulumi.Output<string>;
    /**
     * The description of the Express Sync. The length of the name is limited to `1` to `255` characters.
     */
    declare public readonly description: pulumi.Output<string | undefined>;
    /**
     * The name of the ExpressSync. The length of the name is limited to `1` to `128` characters. It can contain uppercase and lowercase letters, Chinese characters, numbers, English periods (.), underscores (_), or hyphens (-), and must start with  letters.
     */
    declare public readonly expressSyncName: pulumi.Output<string>;

    /**
     * Create a ExpressSync resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: ExpressSyncArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: ExpressSyncArgs | ExpressSyncState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as ExpressSyncState | undefined;
            resourceInputs["bucketName"] = state?.bucketName;
            resourceInputs["bucketPrefix"] = state?.bucketPrefix;
            resourceInputs["bucketRegion"] = state?.bucketRegion;
            resourceInputs["description"] = state?.description;
            resourceInputs["expressSyncName"] = state?.expressSyncName;
        } else {
            const args = argsOrState as ExpressSyncArgs | undefined;
            if (args?.bucketName === undefined && !opts.urn) {
                throw new Error("Missing required property 'bucketName'");
            }
            if (args?.bucketRegion === undefined && !opts.urn) {
                throw new Error("Missing required property 'bucketRegion'");
            }
            if (args?.expressSyncName === undefined && !opts.urn) {
                throw new Error("Missing required property 'expressSyncName'");
            }
            resourceInputs["bucketName"] = args?.bucketName;
            resourceInputs["bucketPrefix"] = args?.bucketPrefix;
            resourceInputs["bucketRegion"] = args?.bucketRegion;
            resourceInputs["description"] = args?.description;
            resourceInputs["expressSyncName"] = args?.expressSyncName;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(ExpressSync.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering ExpressSync resources.
 */
export interface ExpressSyncState {
    /**
     * The name of the OSS Bucket.
     */
    bucketName?: pulumi.Input<string>;
    /**
     * The prefix of the OSS Bucket.
     */
    bucketPrefix?: pulumi.Input<string>;
    /**
     * The region of the OSS Bucket.
     */
    bucketRegion?: pulumi.Input<string>;
    /**
     * The description of the Express Sync. The length of the name is limited to `1` to `255` characters.
     */
    description?: pulumi.Input<string>;
    /**
     * The name of the ExpressSync. The length of the name is limited to `1` to `128` characters. It can contain uppercase and lowercase letters, Chinese characters, numbers, English periods (.), underscores (_), or hyphens (-), and must start with  letters.
     */
    expressSyncName?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a ExpressSync resource.
 */
export interface ExpressSyncArgs {
    /**
     * The name of the OSS Bucket.
     */
    bucketName: pulumi.Input<string>;
    /**
     * The prefix of the OSS Bucket.
     */
    bucketPrefix?: pulumi.Input<string>;
    /**
     * The region of the OSS Bucket.
     */
    bucketRegion: pulumi.Input<string>;
    /**
     * The description of the Express Sync. The length of the name is limited to `1` to `255` characters.
     */
    description?: pulumi.Input<string>;
    /**
     * The name of the ExpressSync. The length of the name is limited to `1` to `128` characters. It can contain uppercase and lowercase letters, Chinese characters, numbers, English periods (.), underscores (_), or hyphens (-), and must start with  letters.
     */
    expressSyncName: pulumi.Input<string>;
}
