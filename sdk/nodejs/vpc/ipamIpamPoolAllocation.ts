// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provides a Vpc Ipam Ipam Pool Allocation resource.
 *
 * Allocates or reserves a CIDR from an IPAM address pool.
 *
 * For information about Vpc Ipam Ipam Pool Allocation and how to use it, see [What is Ipam Pool Allocation](https://next.api.alibabacloud.com/document/VpcIpam/2023-02-28/CreateIpamPoolAllocation).
 *
 * > **NOTE:** Available since v1.238.0.
 *
 * ## Example Usage
 *
 * Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const defaultIpam = new alicloud.vpc.IpamIpam("defaultIpam", {operatingRegionLists: ["cn-hangzhou"]});
 * const defaultIpamPool = new alicloud.vpc.IpamIpamPool("defaultIpamPool", {
 *     ipamScopeId: defaultIpam.privateDefaultScopeId,
 *     poolRegionId: "cn-hangzhou",
 * });
 * const defaultIpamPoolCidr = new alicloud.vpc.IpamIpamPoolCidr("defaultIpamPoolCidr", {
 *     cidr: "10.0.0.0/8",
 *     ipamPoolId: defaultIpamPool.id,
 * });
 * const _default = new alicloud.vpc.IpamIpamPoolAllocation("default", {
 *     ipamPoolAllocationDescription: "init alloc desc",
 *     ipamPoolAllocationName: name,
 *     cidr: "10.0.0.0/20",
 *     ipamPoolId: defaultIpamPoolCidr.ipamPoolId,
 * });
 * ```
 *
 * 📚 Need more examples? VIEW MORE EXAMPLES
 *
 * ## Import
 *
 * Vpc Ipam Ipam Pool Allocation can be imported using the id, e.g.
 *
 * ```sh
 * $ pulumi import alicloud:vpc/ipamIpamPoolAllocation:IpamIpamPoolAllocation example <id>
 * ```
 */
export class IpamIpamPoolAllocation extends pulumi.CustomResource {
    /**
     * Get an existing IpamIpamPoolAllocation resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: IpamIpamPoolAllocationState, opts?: pulumi.CustomResourceOptions): IpamIpamPoolAllocation {
        return new IpamIpamPoolAllocation(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'alicloud:vpc/ipamIpamPoolAllocation:IpamIpamPoolAllocation';

    /**
     * Returns true if the given object is an instance of IpamIpamPoolAllocation.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is IpamIpamPoolAllocation {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === IpamIpamPoolAllocation.__pulumiType;
    }

    /**
     * The allocated address segment.
     */
    declare public readonly cidr: pulumi.Output<string>;
    /**
     * Create a custom reserved network segment from The IPAM address pool by entering a mask.
     *
     * > **NOTE:**  Enter at least one of `Cidr` or **CidrMask.
     */
    declare public readonly cidrMask: pulumi.Output<number | undefined>;
    /**
     * Instance creation time.
     */
    declare public /*out*/ readonly createTime: pulumi.Output<string>;
    /**
     * The description of the ipam pool alloctaion.
     * It must be 1 to 256 characters in length and must start with an English letter or Chinese character, but cannot start with 'http:// 'or 'https. If it is not filled in, it is empty. The default value is empty.
     */
    declare public readonly ipamPoolAllocationDescription: pulumi.Output<string | undefined>;
    /**
     * The name of the ipam pool allocation.
     * It must be 1 to 128 characters in length and cannot start with 'http:// 'or 'https.
     */
    declare public readonly ipamPoolAllocationName: pulumi.Output<string | undefined>;
    /**
     * The ID of the IPAM Pool.
     */
    declare public readonly ipamPoolId: pulumi.Output<string>;
    /**
     * When the IPAM Pool to which CIDR is allocated has the region attribute, this attribute is the IPAM Pool region.
     * When the IPAM Pool to which CIDR is allocated does not have the region attribute, this attribute is the IPAM region.
     */
    declare public /*out*/ readonly regionId: pulumi.Output<string>;
    /**
     * The status of the instance. Value:
     */
    declare public /*out*/ readonly status: pulumi.Output<string>;

    /**
     * Create a IpamIpamPoolAllocation resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: IpamIpamPoolAllocationArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: IpamIpamPoolAllocationArgs | IpamIpamPoolAllocationState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as IpamIpamPoolAllocationState | undefined;
            resourceInputs["cidr"] = state?.cidr;
            resourceInputs["cidrMask"] = state?.cidrMask;
            resourceInputs["createTime"] = state?.createTime;
            resourceInputs["ipamPoolAllocationDescription"] = state?.ipamPoolAllocationDescription;
            resourceInputs["ipamPoolAllocationName"] = state?.ipamPoolAllocationName;
            resourceInputs["ipamPoolId"] = state?.ipamPoolId;
            resourceInputs["regionId"] = state?.regionId;
            resourceInputs["status"] = state?.status;
        } else {
            const args = argsOrState as IpamIpamPoolAllocationArgs | undefined;
            if (args?.ipamPoolId === undefined && !opts.urn) {
                throw new Error("Missing required property 'ipamPoolId'");
            }
            resourceInputs["cidr"] = args?.cidr;
            resourceInputs["cidrMask"] = args?.cidrMask;
            resourceInputs["ipamPoolAllocationDescription"] = args?.ipamPoolAllocationDescription;
            resourceInputs["ipamPoolAllocationName"] = args?.ipamPoolAllocationName;
            resourceInputs["ipamPoolId"] = args?.ipamPoolId;
            resourceInputs["createTime"] = undefined /*out*/;
            resourceInputs["regionId"] = undefined /*out*/;
            resourceInputs["status"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(IpamIpamPoolAllocation.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering IpamIpamPoolAllocation resources.
 */
export interface IpamIpamPoolAllocationState {
    /**
     * The allocated address segment.
     */
    cidr?: pulumi.Input<string>;
    /**
     * Create a custom reserved network segment from The IPAM address pool by entering a mask.
     *
     * > **NOTE:**  Enter at least one of `Cidr` or **CidrMask.
     */
    cidrMask?: pulumi.Input<number>;
    /**
     * Instance creation time.
     */
    createTime?: pulumi.Input<string>;
    /**
     * The description of the ipam pool alloctaion.
     * It must be 1 to 256 characters in length and must start with an English letter or Chinese character, but cannot start with 'http:// 'or 'https. If it is not filled in, it is empty. The default value is empty.
     */
    ipamPoolAllocationDescription?: pulumi.Input<string>;
    /**
     * The name of the ipam pool allocation.
     * It must be 1 to 128 characters in length and cannot start with 'http:// 'or 'https.
     */
    ipamPoolAllocationName?: pulumi.Input<string>;
    /**
     * The ID of the IPAM Pool.
     */
    ipamPoolId?: pulumi.Input<string>;
    /**
     * When the IPAM Pool to which CIDR is allocated has the region attribute, this attribute is the IPAM Pool region.
     * When the IPAM Pool to which CIDR is allocated does not have the region attribute, this attribute is the IPAM region.
     */
    regionId?: pulumi.Input<string>;
    /**
     * The status of the instance. Value:
     */
    status?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a IpamIpamPoolAllocation resource.
 */
export interface IpamIpamPoolAllocationArgs {
    /**
     * The allocated address segment.
     */
    cidr?: pulumi.Input<string>;
    /**
     * Create a custom reserved network segment from The IPAM address pool by entering a mask.
     *
     * > **NOTE:**  Enter at least one of `Cidr` or **CidrMask.
     */
    cidrMask?: pulumi.Input<number>;
    /**
     * The description of the ipam pool alloctaion.
     * It must be 1 to 256 characters in length and must start with an English letter or Chinese character, but cannot start with 'http:// 'or 'https. If it is not filled in, it is empty. The default value is empty.
     */
    ipamPoolAllocationDescription?: pulumi.Input<string>;
    /**
     * The name of the ipam pool allocation.
     * It must be 1 to 128 characters in length and cannot start with 'http:// 'or 'https.
     */
    ipamPoolAllocationName?: pulumi.Input<string>;
    /**
     * The ID of the IPAM Pool.
     */
    ipamPoolId: pulumi.Input<string>;
}
