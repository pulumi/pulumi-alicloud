// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * ## Import
 *
 * Vpc Ipam Service can be imported using the id, e.g.
 *
 * ```sh
 * $ pulumi import alicloud:vpc/ipamService:IpamService example 
 * ```
 */
export class IpamService extends pulumi.CustomResource {
    /**
     * Get an existing IpamService resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: IpamServiceState, opts?: pulumi.CustomResourceOptions): IpamService {
        return new IpamService(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'alicloud:vpc/ipamService:IpamService';

    /**
     * Returns true if the given object is an instance of IpamService.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is IpamService {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === IpamService.__pulumiType;
    }

    /**
     * Whether the IPAM service has been activated.
     */
    declare public /*out*/ readonly enabled: pulumi.Output<boolean>;

    /**
     * Create a IpamService resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args?: IpamServiceArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: IpamServiceArgs | IpamServiceState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as IpamServiceState | undefined;
            resourceInputs["enabled"] = state?.enabled;
        } else {
            const args = argsOrState as IpamServiceArgs | undefined;
            resourceInputs["enabled"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(IpamService.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering IpamService resources.
 */
export interface IpamServiceState {
    /**
     * Whether the IPAM service has been activated.
     */
    enabled?: pulumi.Input<boolean>;
}

/**
 * The set of arguments for constructing a IpamService resource.
 */
export interface IpamServiceArgs {
}
