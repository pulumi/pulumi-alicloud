// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * Provides a VPC Route Target Group resource.
 *
 * Route target group.
 *
 * For information about VPC Route Target Group and how to use it, see [What is Route Target Group](https://next.api.alibabacloud.com/document/Vpc/2016-04-28/CreateRouteTargetGroup).
 *
 * > **NOTE:** Available since v1.292.0.
 *
 * ## Example Usage
 *
 * Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const region = config.get("region") || "cn-wulanchabu";
 * const zoneId1 = config.get("zoneId1") || "cn-wulanchabu-b";
 * const zoneId2 = config.get("zoneId2") || "cn-wulanchabu-c";
 * const _default = new alicloud.vpc.Network("default", {
 *     vpcName: name,
 *     cidrBlock: "192.168.0.0/16",
 * });
 * const zoneA = new alicloud.vpc.Switch("zone_a", {
 *     vpcId: _default.id,
 *     zoneId: zoneId1,
 *     cidrBlock: "192.168.0.0/24",
 * });
 * const zoneB = new alicloud.vpc.Switch("zone_b", {
 *     vpcId: _default.id,
 *     zoneId: zoneId2,
 *     cidrBlock: "192.168.1.0/24",
 * });
 * // Active member (zone A): GWLB load balancer + GWLB-type endpoint service +
 * // service-resource attachment + GatewayLoadBalancer endpoint. The endpoint
 * // depends_on the service-resource so the GWLB is attached to the service
 * // before the endpoint is created.
 * const active = new alicloud.gwlb.LoadBalancer("active", {
 *     loadBalancerName: `${name}-gwlb-active`,
 *     addressIpVersion: "Ipv4",
 *     vpcId: _default.id,
 *     zoneMappings: [{
 *         vswitchId: zoneA.id,
 *         zoneId: zoneId1,
 *     }],
 * });
 * const activeVpcEndpointService = new alicloud.privatelink.VpcEndpointService("active", {
 *     autoAcceptConnection: true,
 *     serviceDescription: `${name}-eps-active`,
 *     serviceResourceType: "gwlb",
 * });
 * const activeVpcEndpointServiceResource = new alicloud.privatelink.VpcEndpointServiceResource("active", {
 *     resourceId: active.id,
 *     resourceType: "gwlb",
 *     serviceId: activeVpcEndpointService.id,
 *     zoneId: zoneId1,
 *     dryRun: false,
 * });
 * const activeVpcEndpoint = new alicloud.privatelink.VpcEndpoint("active", {
 *     serviceId: activeVpcEndpointService.id,
 *     vpcEndpointName: `${name}-ep-active`,
 *     vpcId: _default.id,
 *     serviceName: activeVpcEndpointService.vpcEndpointServiceName,
 *     endpointType: "GatewayLoadBalancer",
 * });
 * // Attach zone A to the GWLB endpoint. The route target group backend looks up
 * // the member endpoint by zone, so the endpoint must carry a non-empty zone.
 * const activeVpcEndpointZone = new alicloud.privatelink.VpcEndpointZone("active", {
 *     endpointId: activeVpcEndpoint.id,
 *     vswitchId: zoneA.id,
 * });
 * // Standby member (zone B): identical chain in a different zone so the two
 * // members satisfy active-standby's two-different-zone rule.
 * const standby = new alicloud.gwlb.LoadBalancer("standby", {
 *     loadBalancerName: `${name}-gwlb-standby`,
 *     addressIpVersion: "Ipv4",
 *     vpcId: _default.id,
 *     zoneMappings: [{
 *         vswitchId: zoneB.id,
 *         zoneId: zoneId2,
 *     }],
 * });
 * const standbyVpcEndpointService = new alicloud.privatelink.VpcEndpointService("standby", {
 *     autoAcceptConnection: true,
 *     serviceDescription: `${name}-eps-standby`,
 *     serviceResourceType: "gwlb",
 * });
 * const standbyVpcEndpointServiceResource = new alicloud.privatelink.VpcEndpointServiceResource("standby", {
 *     resourceId: standby.id,
 *     resourceType: "gwlb",
 *     serviceId: standbyVpcEndpointService.id,
 *     zoneId: zoneId2,
 *     dryRun: false,
 * });
 * const standbyVpcEndpoint = new alicloud.privatelink.VpcEndpoint("standby", {
 *     serviceId: standbyVpcEndpointService.id,
 *     vpcEndpointName: `${name}-ep-standby`,
 *     vpcId: _default.id,
 *     serviceName: standbyVpcEndpointService.vpcEndpointServiceName,
 *     endpointType: "GatewayLoadBalancer",
 * });
 * // Attach zone B to the standby GWLB endpoint (different zone from active).
 * const standbyVpcEndpointZone = new alicloud.privatelink.VpcEndpointZone("standby", {
 *     endpointId: standbyVpcEndpoint.id,
 *     vswitchId: zoneB.id,
 * });
 * // The route target group depends_on both endpoint zones: the backend looks up
 * // each member endpoint by zone, so the zones must exist before Create is called.
 * const defaultRouteTargetGroup = new alicloud.vpc.RouteTargetGroup("default", {
 *     routeTargetGroupName: name,
 *     routeTargetGroupDescription: name,
 *     vpcId: _default.id,
 *     configMode: "Active-Standby",
 *     routeTargetMemberLists: [
 *         {
 *             memberId: activeVpcEndpoint.id,
 *             memberType: "GatewayLoadBalancerEndpoint",
 *             weight: 100,
 *         },
 *         {
 *             memberId: standbyVpcEndpoint.id,
 *             memberType: "GatewayLoadBalancerEndpoint",
 *             weight: 0,
 *         },
 *     ],
 * });
 * ```
 *
 * 📚 Need more examples? VIEW MORE EXAMPLES
 *
 * ## Import
 *
 * VPC Route Target Group can be imported using the id, e.g.
 *
 * ```sh
 * $ pulumi import alicloud:vpc/routeTargetGroup:RouteTargetGroup example <route_target_group_id>
 * ```
 */
export class RouteTargetGroup extends pulumi.CustomResource {
    /**
     * Get an existing RouteTargetGroup resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: RouteTargetGroupState, opts?: pulumi.CustomResourceOptions): RouteTargetGroup {
        return new RouteTargetGroup(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'alicloud:vpc/routeTargetGroup:RouteTargetGroup';

    /**
     * Returns true if the given object is an instance of RouteTargetGroup.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is RouteTargetGroup {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === RouteTargetGroup.__pulumiType;
    }

    /**
     * The configuration mode of the route target group. Supported modes include:
     * - **Active-Standby**: active-standby mode.
     */
    declare public readonly configMode: pulumi.Output<string>;
    /**
     * The time when the route target group was created.
     */
    declare public /*out*/ readonly createTime: pulumi.Output<string>;
    /**
     * The ID of the resource group to which the route target group belongs.
     */
    declare public readonly resourceGroupId: pulumi.Output<string>;
    /**
     * The description of the route target group.
     * The description must be 1 to 256 characters in length and cannot start with http:// or https://.
     */
    declare public readonly routeTargetGroupDescription: pulumi.Output<string | undefined>;
    /**
     * The name of the route target group.
     * The name must be 1 to 128 characters in length and cannot start with http:// or https://.
     */
    declare public readonly routeTargetGroupName: pulumi.Output<string | undefined>;
    /**
     * The member list of the route target group.
     * **Note: The parameter is immutable after resource creation. In active-standby mode, member weight and type cannot be changed via UpdateRouteTargetGroup; switching active/standby uses a separate SwitchActiveRouteTarget operation.
     * In active/standby mode, the following restrictions apply to route target group members:
     * 1. The route target group must contain exactly two members.
     * 2. The route target group members must belong to different zones. See `routeTargetMemberList` below.
     */
    declare public readonly routeTargetMemberLists: pulumi.Output<outputs.vpc.RouteTargetGroupRouteTargetMemberList[]>;
    /**
     * The status of the route target group. Valid values: `Pending`, `Available`.
     */
    declare public /*out*/ readonly status: pulumi.Output<string>;
    /**
     * The tags of the route target group.
     */
    declare public readonly tags: pulumi.Output<{[key: string]: string} | undefined>;
    /**
     * The ID of the VPC to which the route target group belongs.
     */
    declare public readonly vpcId: pulumi.Output<string>;

    /**
     * Create a RouteTargetGroup resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: RouteTargetGroupArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: RouteTargetGroupArgs | RouteTargetGroupState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as RouteTargetGroupState | undefined;
            resourceInputs["configMode"] = state?.configMode;
            resourceInputs["createTime"] = state?.createTime;
            resourceInputs["resourceGroupId"] = state?.resourceGroupId;
            resourceInputs["routeTargetGroupDescription"] = state?.routeTargetGroupDescription;
            resourceInputs["routeTargetGroupName"] = state?.routeTargetGroupName;
            resourceInputs["routeTargetMemberLists"] = state?.routeTargetMemberLists;
            resourceInputs["status"] = state?.status;
            resourceInputs["tags"] = state?.tags;
            resourceInputs["vpcId"] = state?.vpcId;
        } else {
            const args = argsOrState as RouteTargetGroupArgs | undefined;
            if (args?.configMode === undefined && !opts.urn) {
                throw new Error("Missing required property 'configMode'");
            }
            if (args?.routeTargetMemberLists === undefined && !opts.urn) {
                throw new Error("Missing required property 'routeTargetMemberLists'");
            }
            if (args?.vpcId === undefined && !opts.urn) {
                throw new Error("Missing required property 'vpcId'");
            }
            resourceInputs["configMode"] = args?.configMode;
            resourceInputs["resourceGroupId"] = args?.resourceGroupId;
            resourceInputs["routeTargetGroupDescription"] = args?.routeTargetGroupDescription;
            resourceInputs["routeTargetGroupName"] = args?.routeTargetGroupName;
            resourceInputs["routeTargetMemberLists"] = args?.routeTargetMemberLists;
            resourceInputs["tags"] = args?.tags;
            resourceInputs["vpcId"] = args?.vpcId;
            resourceInputs["createTime"] = undefined /*out*/;
            resourceInputs["status"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(RouteTargetGroup.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering RouteTargetGroup resources.
 */
export interface RouteTargetGroupState {
    /**
     * The configuration mode of the route target group. Supported modes include:
     * - **Active-Standby**: active-standby mode.
     */
    configMode?: pulumi.Input<string | undefined>;
    /**
     * The time when the route target group was created.
     */
    createTime?: pulumi.Input<string | undefined>;
    /**
     * The ID of the resource group to which the route target group belongs.
     */
    resourceGroupId?: pulumi.Input<string | undefined>;
    /**
     * The description of the route target group.
     * The description must be 1 to 256 characters in length and cannot start with http:// or https://.
     */
    routeTargetGroupDescription?: pulumi.Input<string | undefined>;
    /**
     * The name of the route target group.
     * The name must be 1 to 128 characters in length and cannot start with http:// or https://.
     */
    routeTargetGroupName?: pulumi.Input<string | undefined>;
    /**
     * The member list of the route target group.
     * **Note: The parameter is immutable after resource creation. In active-standby mode, member weight and type cannot be changed via UpdateRouteTargetGroup; switching active/standby uses a separate SwitchActiveRouteTarget operation.
     * In active/standby mode, the following restrictions apply to route target group members:
     * 1. The route target group must contain exactly two members.
     * 2. The route target group members must belong to different zones. See `routeTargetMemberList` below.
     */
    routeTargetMemberLists?: pulumi.Input<pulumi.Input<inputs.vpc.RouteTargetGroupRouteTargetMemberList>[] | undefined>;
    /**
     * The status of the route target group. Valid values: `Pending`, `Available`.
     */
    status?: pulumi.Input<string | undefined>;
    /**
     * The tags of the route target group.
     */
    tags?: pulumi.Input<{[key: string]: pulumi.Input<string>} | undefined>;
    /**
     * The ID of the VPC to which the route target group belongs.
     */
    vpcId?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a RouteTargetGroup resource.
 */
export interface RouteTargetGroupArgs {
    /**
     * The configuration mode of the route target group. Supported modes include:
     * - **Active-Standby**: active-standby mode.
     */
    configMode: pulumi.Input<string>;
    /**
     * The ID of the resource group to which the route target group belongs.
     */
    resourceGroupId?: pulumi.Input<string | undefined>;
    /**
     * The description of the route target group.
     * The description must be 1 to 256 characters in length and cannot start with http:// or https://.
     */
    routeTargetGroupDescription?: pulumi.Input<string | undefined>;
    /**
     * The name of the route target group.
     * The name must be 1 to 128 characters in length and cannot start with http:// or https://.
     */
    routeTargetGroupName?: pulumi.Input<string | undefined>;
    /**
     * The member list of the route target group.
     * **Note: The parameter is immutable after resource creation. In active-standby mode, member weight and type cannot be changed via UpdateRouteTargetGroup; switching active/standby uses a separate SwitchActiveRouteTarget operation.
     * In active/standby mode, the following restrictions apply to route target group members:
     * 1. The route target group must contain exactly two members.
     * 2. The route target group members must belong to different zones. See `routeTargetMemberList` below.
     */
    routeTargetMemberLists: pulumi.Input<pulumi.Input<inputs.vpc.RouteTargetGroupRouteTargetMemberList>[]>;
    /**
     * The tags of the route target group.
     */
    tags?: pulumi.Input<{[key: string]: pulumi.Input<string>} | undefined>;
    /**
     * The ID of the VPC to which the route target group belongs.
     */
    vpcId: pulumi.Input<string>;
}
