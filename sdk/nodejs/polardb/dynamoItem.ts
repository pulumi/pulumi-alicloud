// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provides a PolarDB DynamoDB-compatible table item resource to manage a single item in a DynamoDB-compatible table of a PolarDB for PostgreSQL cluster.
 *
 * > **NOTE:** Available since v1.287.0.
 *
 * > **NOTE:** This resource is intended for managing a small amount of well-known, seed-style data. It is not recommended to manage large numbers of items with Terraform.
 *
 * > **NOTE:** All operations are performed against the DynamoDB-compatible endpoint (`http://<connection_string>:5432`) using the DynamoDB API, not the PolarDB OpenAPI.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const _default = alicloud.polardb.getNodeClasses({
 *     dbType: "PostgreSQL",
 *     dbVersion: "16",
 *     payType: "PostPaid",
 *     dbNodeClass: "polar.pg.x4.medium",
 * });
 * const defaultNetwork = new alicloud.vpc.Network("default", {
 *     vpcName: name,
 *     cidrBlock: "172.16.0.0/16",
 * });
 * const defaultSwitch = new alicloud.vpc.Switch("default", {
 *     vpcId: defaultNetwork.id,
 *     cidrBlock: "172.16.0.0/24",
 *     zoneId: _default.then(_default => _default.classes?.[0]?.zoneId),
 *     vswitchName: name,
 * });
 * const defaultGlobalSecurityIpGroup = new alicloud.polardb.GlobalSecurityIpGroup("default", {
 *     globalIpGroupName: "tf_dynamo_whitelist",
 *     globalIpList: "0.0.0.0/0",
 * });
 * const defaultCluster = new alicloud.polardb.Cluster("default", {
 *     dbType: "PostgreSQL",
 *     dbVersion: "16",
 *     dbNodeClass: "polar.pg.x4.medium",
 *     payType: "PostPaid",
 *     vswitchId: defaultSwitch.id,
 *     description: name,
 *     enableDynamodb: true,
 *     globalSecurityGroupLists: [defaultGlobalSecurityIpGroup.id],
 * });
 * const dynamo = new alicloud.polardb.Account("dynamo", {
 *     dbClusterId: defaultCluster.id,
 *     accountName: "tf_dynamo_acc",
 *     accountPassword: "Example1234!",
 *     accountType: "DynamoDB",
 * });
 * const dynamoEndpoint = new alicloud.polardb.Endpoint("dynamo", {
 *     dbClusterId: dynamo.dbClusterId,
 *     endpointType: "DynamoDB",
 *     readWriteMode: "ReadWrite",
 * });
 * const dynamoPublic = new alicloud.polardb.EndpointAddress("dynamo_public", {
 *     dbClusterId: defaultCluster.id,
 *     dbEndpointId: dynamoEndpoint.dbEndpointId,
 *     netType: "Public",
 * });
 * const defaultDynamoTable = new alicloud.polardb.DynamoTable("default", {
 *     endpoint: pulumi.interpolate`http://${dynamoPublic.connectionString}:5432`,
 *     dbClusterId: defaultCluster.id,
 *     accountName: dynamo.accountName,
 *     accountAuth: dynamo.dynamodbAuthPassword,
 *     tableName: name,
 *     hashKey: "pk",
 *     rangeKey: "sk",
 *     billingMode: "PAY_PER_REQUEST",
 *     attributes: [
 *         {
 *             name: "pk",
 *             type: "S",
 *         },
 *         {
 *             name: "sk",
 *             type: "S",
 *         },
 *     ],
 * });
 * const defaultDynamoItem = new alicloud.polardb.DynamoItem("default", {
 *     endpoint: pulumi.interpolate`http://${dynamoPublic.connectionString}:5432`,
 *     dbClusterId: defaultCluster.id,
 *     accountName: dynamo.accountName,
 *     accountAuth: dynamo.dynamodbAuthPassword,
 *     tableName: defaultDynamoTable.tableName,
 *     hashKey: "pk",
 *     rangeKey: "sk",
 *     item: JSON.stringify({
 *         pk: {
 *             S: "test-item-1",
 *         },
 *         sk: {
 *             S: "row1",
 *         },
 *         name: {
 *             S: "Test Item",
 *         },
 *         count: {
 *             N: "42",
 *         },
 *     }),
 * });
 * ```
 *
 * 📚 Need more examples? VIEW MORE EXAMPLES
 *
 * ## Import
 *
 * PolarDB DynamoDB-compatible table item can be imported using the id, e.g.
 *
 * ```sh
 * $ pulumi import alicloud:polardb/dynamoItem:DynamoItem example pc-abc123456:table_name:hash_value:range_value
 * ```
 *
 * > **NOTE:** On import, `accountName`, `accountAuth`, `hashKey`, `rangeKey` and the endpoint address are resolved from the cluster and table schema automatically, but `endpoint`, `hashKey` and the other required arguments must still be present in the resource block.
 */
export class DynamoItem extends pulumi.CustomResource {
    /**
     * Get an existing DynamoItem resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: DynamoItemState, opts?: pulumi.CustomResourceOptions): DynamoItem {
        return new DynamoItem(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'alicloud:polardb/dynamoItem:DynamoItem';

    /**
     * Returns true if the given object is an instance of DynamoItem.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is DynamoItem {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === DynamoItem.__pulumiType;
    }

    /**
     * The authentication password for PolarDB DynamoDB. Usually references the `dynamodbAuthPassword` attribute of an `alicloud.polardb.Account` with `accountType = "DynamoDB"`. If not set, it is resolved from the cluster's DynamoDB-type account automatically.
     */
    declare public readonly accountAuth: pulumi.Output<string>;
    /**
     * The account name for PolarDB DynamoDB authentication. If not set, it is resolved from the cluster's DynamoDB-type account automatically.
     */
    declare public readonly accountName: pulumi.Output<string>;
    /**
     * The ID of the PolarDB cluster where the DynamoDB table resides.
     */
    declare public readonly dbClusterId: pulumi.Output<string>;
    /**
     * The PolarDB DynamoDB-compatible endpoint URL, in the format `http://<connection_string>:5432`.
     */
    declare public readonly endpoint: pulumi.Output<string>;
    /**
     * The partition key (hash key) attribute name of the item. Must match the table's hash key.
     */
    declare public readonly hashKey: pulumi.Output<string>;
    /**
     * JSON representation of the item attributes in DynamoDB attribute value format, e.g. `{"pk": {"S": "value"}, "count": {"N": "42"}}`. The item must contain the `hashKey` attribute (and the `rangeKey` attribute if set). Supported type descriptors: `S`, `N`, `B`, `BOOL`, `NULL`, `L`, `M`, `SS`, `NS`, `BS`.
     *
     * > **NOTE:** Changing the key attribute values inside `item` results in a new item being written; the resource ID will be recomputed accordingly.
     */
    declare public readonly item: pulumi.Output<string>;
    /**
     * The sort key (range key) attribute name of the item. Required if the table has a range key.
     */
    declare public readonly rangeKey: pulumi.Output<string | undefined>;
    /**
     * The name of the DynamoDB-compatible table that contains the item.
     */
    declare public readonly tableName: pulumi.Output<string>;

    /**
     * Create a DynamoItem resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: DynamoItemArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: DynamoItemArgs | DynamoItemState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as DynamoItemState | undefined;
            resourceInputs["accountAuth"] = state?.accountAuth;
            resourceInputs["accountName"] = state?.accountName;
            resourceInputs["dbClusterId"] = state?.dbClusterId;
            resourceInputs["endpoint"] = state?.endpoint;
            resourceInputs["hashKey"] = state?.hashKey;
            resourceInputs["item"] = state?.item;
            resourceInputs["rangeKey"] = state?.rangeKey;
            resourceInputs["tableName"] = state?.tableName;
        } else {
            const args = argsOrState as DynamoItemArgs | undefined;
            if (args?.dbClusterId === undefined && !opts.urn) {
                throw new Error("Missing required property 'dbClusterId'");
            }
            if (args?.endpoint === undefined && !opts.urn) {
                throw new Error("Missing required property 'endpoint'");
            }
            if (args?.hashKey === undefined && !opts.urn) {
                throw new Error("Missing required property 'hashKey'");
            }
            if (args?.item === undefined && !opts.urn) {
                throw new Error("Missing required property 'item'");
            }
            if (args?.tableName === undefined && !opts.urn) {
                throw new Error("Missing required property 'tableName'");
            }
            resourceInputs["accountAuth"] = args?.accountAuth ? pulumi.secret(args.accountAuth) : undefined;
            resourceInputs["accountName"] = args?.accountName ? pulumi.secret(args.accountName) : undefined;
            resourceInputs["dbClusterId"] = args?.dbClusterId;
            resourceInputs["endpoint"] = args?.endpoint;
            resourceInputs["hashKey"] = args?.hashKey;
            resourceInputs["item"] = args?.item;
            resourceInputs["rangeKey"] = args?.rangeKey;
            resourceInputs["tableName"] = args?.tableName;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        const secretOpts = { additionalSecretOutputs: ["accountAuth", "accountName"] };
        opts = pulumi.mergeOptions(opts, secretOpts);
        super(DynamoItem.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering DynamoItem resources.
 */
export interface DynamoItemState {
    /**
     * The authentication password for PolarDB DynamoDB. Usually references the `dynamodbAuthPassword` attribute of an `alicloud.polardb.Account` with `accountType = "DynamoDB"`. If not set, it is resolved from the cluster's DynamoDB-type account automatically.
     */
    accountAuth?: pulumi.Input<string | undefined>;
    /**
     * The account name for PolarDB DynamoDB authentication. If not set, it is resolved from the cluster's DynamoDB-type account automatically.
     */
    accountName?: pulumi.Input<string | undefined>;
    /**
     * The ID of the PolarDB cluster where the DynamoDB table resides.
     */
    dbClusterId?: pulumi.Input<string | undefined>;
    /**
     * The PolarDB DynamoDB-compatible endpoint URL, in the format `http://<connection_string>:5432`.
     */
    endpoint?: pulumi.Input<string | undefined>;
    /**
     * The partition key (hash key) attribute name of the item. Must match the table's hash key.
     */
    hashKey?: pulumi.Input<string | undefined>;
    /**
     * JSON representation of the item attributes in DynamoDB attribute value format, e.g. `{"pk": {"S": "value"}, "count": {"N": "42"}}`. The item must contain the `hashKey` attribute (and the `rangeKey` attribute if set). Supported type descriptors: `S`, `N`, `B`, `BOOL`, `NULL`, `L`, `M`, `SS`, `NS`, `BS`.
     *
     * > **NOTE:** Changing the key attribute values inside `item` results in a new item being written; the resource ID will be recomputed accordingly.
     */
    item?: pulumi.Input<string | undefined>;
    /**
     * The sort key (range key) attribute name of the item. Required if the table has a range key.
     */
    rangeKey?: pulumi.Input<string | undefined>;
    /**
     * The name of the DynamoDB-compatible table that contains the item.
     */
    tableName?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a DynamoItem resource.
 */
export interface DynamoItemArgs {
    /**
     * The authentication password for PolarDB DynamoDB. Usually references the `dynamodbAuthPassword` attribute of an `alicloud.polardb.Account` with `accountType = "DynamoDB"`. If not set, it is resolved from the cluster's DynamoDB-type account automatically.
     */
    accountAuth?: pulumi.Input<string | undefined>;
    /**
     * The account name for PolarDB DynamoDB authentication. If not set, it is resolved from the cluster's DynamoDB-type account automatically.
     */
    accountName?: pulumi.Input<string | undefined>;
    /**
     * The ID of the PolarDB cluster where the DynamoDB table resides.
     */
    dbClusterId: pulumi.Input<string>;
    /**
     * The PolarDB DynamoDB-compatible endpoint URL, in the format `http://<connection_string>:5432`.
     */
    endpoint: pulumi.Input<string>;
    /**
     * The partition key (hash key) attribute name of the item. Must match the table's hash key.
     */
    hashKey: pulumi.Input<string>;
    /**
     * JSON representation of the item attributes in DynamoDB attribute value format, e.g. `{"pk": {"S": "value"}, "count": {"N": "42"}}`. The item must contain the `hashKey` attribute (and the `rangeKey` attribute if set). Supported type descriptors: `S`, `N`, `B`, `BOOL`, `NULL`, `L`, `M`, `SS`, `NS`, `BS`.
     *
     * > **NOTE:** Changing the key attribute values inside `item` results in a new item being written; the resource ID will be recomputed accordingly.
     */
    item: pulumi.Input<string>;
    /**
     * The sort key (range key) attribute name of the item. Required if the table has a range key.
     */
    rangeKey?: pulumi.Input<string | undefined>;
    /**
     * The name of the DynamoDB-compatible table that contains the item.
     */
    tableName: pulumi.Input<string>;
}
