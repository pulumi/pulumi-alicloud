// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * Provides a PolarDB DynamoDB-compatible table resource to manage tables through the DynamoDB-compatible endpoint of a PolarDB for PostgreSQL cluster.
 *
 * > **NOTE:** Available since v1.287.0.
 *
 * > **NOTE:** This resource requires a PolarDB for PostgreSQL cluster with `enableDynamodb` set to `true`, a PolarDB account of type `DynamoDB`, and a cluster endpoint of type `DynamoDB` with a reachable (e.g. public) endpoint address.
 *
 * > **NOTE:** All operations are performed against the DynamoDB-compatible endpoint (`http://<connection_string>:5432`) using the DynamoDB API, not the PolarDB OpenAPI.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const _default = alicloud.polardb.getNodeClasses({
 *     dbType: "PostgreSQL",
 *     dbVersion: "16",
 *     payType: "PostPaid",
 *     dbNodeClass: "polar.pg.x4.medium",
 * });
 * const defaultNetwork = new alicloud.vpc.Network("default", {
 *     vpcName: name,
 *     cidrBlock: "172.16.0.0/16",
 * });
 * const defaultSwitch = new alicloud.vpc.Switch("default", {
 *     vpcId: defaultNetwork.id,
 *     cidrBlock: "172.16.0.0/24",
 *     zoneId: _default.then(_default => _default.classes?.[0]?.zoneId),
 *     vswitchName: name,
 * });
 * const defaultGlobalSecurityIpGroup = new alicloud.polardb.GlobalSecurityIpGroup("default", {
 *     globalIpGroupName: "tf_dynamo_whitelist",
 *     globalIpList: "0.0.0.0/0",
 * });
 * const defaultCluster = new alicloud.polardb.Cluster("default", {
 *     dbType: "PostgreSQL",
 *     dbVersion: "16",
 *     dbNodeClass: "polar.pg.x4.medium",
 *     payType: "PostPaid",
 *     vswitchId: defaultSwitch.id,
 *     description: name,
 *     enableDynamodb: true,
 *     globalSecurityGroupLists: [defaultGlobalSecurityIpGroup.id],
 * });
 * const dynamo = new alicloud.polardb.Account("dynamo", {
 *     dbClusterId: defaultCluster.id,
 *     accountName: "tf_dynamo_acc",
 *     accountPassword: "Example1234!",
 *     accountType: "DynamoDB",
 * });
 * const dynamoEndpoint = new alicloud.polardb.Endpoint("dynamo", {
 *     dbClusterId: dynamo.dbClusterId,
 *     endpointType: "DynamoDB",
 *     readWriteMode: "ReadWrite",
 * });
 * const dynamoPublic = new alicloud.polardb.EndpointAddress("dynamo_public", {
 *     dbClusterId: defaultCluster.id,
 *     dbEndpointId: dynamoEndpoint.dbEndpointId,
 *     netType: "Public",
 * });
 * const defaultDynamoTable = new alicloud.polardb.DynamoTable("default", {
 *     endpoint: pulumi.interpolate`http://${dynamoPublic.connectionString}:5432`,
 *     dbClusterId: defaultCluster.id,
 *     accountName: dynamo.accountName,
 *     accountAuth: dynamo.dynamodbAuthPassword,
 *     tableName: name,
 *     hashKey: "pk",
 *     rangeKey: "sk",
 *     billingMode: "PAY_PER_REQUEST",
 *     attributes: [
 *         {
 *             name: "pk",
 *             type: "S",
 *         },
 *         {
 *             name: "sk",
 *             type: "S",
 *         },
 *     ],
 * });
 * ```
 *
 * 📚 Need more examples? VIEW MORE EXAMPLES
 *
 * ## Import
 *
 * PolarDB DynamoDB-compatible table can be imported using the id, e.g.
 *
 * ```sh
 * $ pulumi import alicloud:polardb/dynamoTable:DynamoTable example pc-abc123456:table_name
 * ```
 *
 * > **NOTE:** On import, `accountName`, `accountAuth` and the endpoint address are resolved from the cluster automatically, but `endpoint` is a required argument and must still be present in the resource block. In addition, the DynamoDB-compatible endpoint does not return billing and capacity information, so `billingMode`, `readCapacity` and `writeCapacity` are not populated on import and the first plan may show a diff for them.
 */
export class DynamoTable extends pulumi.CustomResource {
    /**
     * Get an existing DynamoTable resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: DynamoTableState, opts?: pulumi.CustomResourceOptions): DynamoTable {
        return new DynamoTable(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'alicloud:polardb/dynamoTable:DynamoTable';

    /**
     * Returns true if the given object is an instance of DynamoTable.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is DynamoTable {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === DynamoTable.__pulumiType;
    }

    /**
     * The authentication password for PolarDB DynamoDB. Usually references the `dynamodbAuthPassword` attribute of an `alicloud.polardb.Account` with `accountType = "DynamoDB"`. If not set, it is resolved from the cluster's DynamoDB-type account automatically.
     */
    declare public readonly accountAuth: pulumi.Output<string>;
    /**
     * The account name for PolarDB DynamoDB authentication. If not set, it is resolved from the cluster's DynamoDB-type account automatically.
     */
    declare public readonly accountName: pulumi.Output<string>;
    /**
     * The ARN of the table, if returned by the endpoint.
     */
    declare public /*out*/ readonly arn: pulumi.Output<string>;
    /**
     * List of attribute definitions for the table key schema and indexes. See `attribute` below.
     */
    declare public readonly attributes: pulumi.Output<outputs.polardb.DynamoTableAttribute[]>;
    /**
     * The billing mode of the table. Valid values: `PROVISIONED`, `PAY_PER_REQUEST`. Default to `PROVISIONED`.
     */
    declare public readonly billingMode: pulumi.Output<string | undefined>;
    /**
     * The ID of the PolarDB cluster where DynamoDB is enabled.
     */
    declare public readonly dbClusterId: pulumi.Output<string>;
    /**
     * The PolarDB DynamoDB-compatible endpoint URL, in the format `http://<connection_string>:5432`.
     */
    declare public readonly endpoint: pulumi.Output<string>;
    /**
     * Describe a GSI for the table. See `globalSecondaryIndex` below. Changing the key schema or projection of an existing index recreates that index.
     */
    declare public readonly globalSecondaryIndices: pulumi.Output<outputs.polardb.DynamoTableGlobalSecondaryIndex[]>;
    /**
     * The attribute name used as the partition key (hash key) of the table.
     */
    declare public readonly hashKey: pulumi.Output<string>;
    /**
     * Describe an LSI on the table. See `localSecondaryIndex` below.
     */
    declare public readonly localSecondaryIndices: pulumi.Output<outputs.polardb.DynamoTableLocalSecondaryIndex[] | undefined>;
    /**
     * The attribute name used as the sort key (range key) of the table.
     */
    declare public readonly rangeKey: pulumi.Output<string>;
    /**
     * The number of read capacity units. Required when `billingMode` is `PROVISIONED`.
     */
    declare public readonly readCapacity: pulumi.Output<number>;
    /**
     * The name of the DynamoDB-compatible table.
     */
    declare public readonly tableName: pulumi.Output<string>;
    /**
     * Configuration block for TTL. See `ttl` below.
     */
    declare public readonly ttl: pulumi.Output<outputs.polardb.DynamoTableTtl>;
    /**
     * The number of write capacity units. Required when `billingMode` is `PROVISIONED`.
     */
    declare public readonly writeCapacity: pulumi.Output<number>;

    /**
     * Create a DynamoTable resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: DynamoTableArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: DynamoTableArgs | DynamoTableState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as DynamoTableState | undefined;
            resourceInputs["accountAuth"] = state?.accountAuth;
            resourceInputs["accountName"] = state?.accountName;
            resourceInputs["arn"] = state?.arn;
            resourceInputs["attributes"] = state?.attributes;
            resourceInputs["billingMode"] = state?.billingMode;
            resourceInputs["dbClusterId"] = state?.dbClusterId;
            resourceInputs["endpoint"] = state?.endpoint;
            resourceInputs["globalSecondaryIndices"] = state?.globalSecondaryIndices;
            resourceInputs["hashKey"] = state?.hashKey;
            resourceInputs["localSecondaryIndices"] = state?.localSecondaryIndices;
            resourceInputs["rangeKey"] = state?.rangeKey;
            resourceInputs["readCapacity"] = state?.readCapacity;
            resourceInputs["tableName"] = state?.tableName;
            resourceInputs["ttl"] = state?.ttl;
            resourceInputs["writeCapacity"] = state?.writeCapacity;
        } else {
            const args = argsOrState as DynamoTableArgs | undefined;
            if (args?.dbClusterId === undefined && !opts.urn) {
                throw new Error("Missing required property 'dbClusterId'");
            }
            if (args?.endpoint === undefined && !opts.urn) {
                throw new Error("Missing required property 'endpoint'");
            }
            if (args?.tableName === undefined && !opts.urn) {
                throw new Error("Missing required property 'tableName'");
            }
            resourceInputs["accountAuth"] = args?.accountAuth ? pulumi.secret(args.accountAuth) : undefined;
            resourceInputs["accountName"] = args?.accountName ? pulumi.secret(args.accountName) : undefined;
            resourceInputs["attributes"] = args?.attributes;
            resourceInputs["billingMode"] = args?.billingMode;
            resourceInputs["dbClusterId"] = args?.dbClusterId;
            resourceInputs["endpoint"] = args?.endpoint;
            resourceInputs["globalSecondaryIndices"] = args?.globalSecondaryIndices;
            resourceInputs["hashKey"] = args?.hashKey;
            resourceInputs["localSecondaryIndices"] = args?.localSecondaryIndices;
            resourceInputs["rangeKey"] = args?.rangeKey;
            resourceInputs["readCapacity"] = args?.readCapacity;
            resourceInputs["tableName"] = args?.tableName;
            resourceInputs["ttl"] = args?.ttl;
            resourceInputs["writeCapacity"] = args?.writeCapacity;
            resourceInputs["arn"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        const secretOpts = { additionalSecretOutputs: ["accountAuth", "accountName"] };
        opts = pulumi.mergeOptions(opts, secretOpts);
        super(DynamoTable.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering DynamoTable resources.
 */
export interface DynamoTableState {
    /**
     * The authentication password for PolarDB DynamoDB. Usually references the `dynamodbAuthPassword` attribute of an `alicloud.polardb.Account` with `accountType = "DynamoDB"`. If not set, it is resolved from the cluster's DynamoDB-type account automatically.
     */
    accountAuth?: pulumi.Input<string | undefined>;
    /**
     * The account name for PolarDB DynamoDB authentication. If not set, it is resolved from the cluster's DynamoDB-type account automatically.
     */
    accountName?: pulumi.Input<string | undefined>;
    /**
     * The ARN of the table, if returned by the endpoint.
     */
    arn?: pulumi.Input<string | undefined>;
    /**
     * List of attribute definitions for the table key schema and indexes. See `attribute` below.
     */
    attributes?: pulumi.Input<pulumi.Input<inputs.polardb.DynamoTableAttribute>[] | undefined>;
    /**
     * The billing mode of the table. Valid values: `PROVISIONED`, `PAY_PER_REQUEST`. Default to `PROVISIONED`.
     */
    billingMode?: pulumi.Input<string | undefined>;
    /**
     * The ID of the PolarDB cluster where DynamoDB is enabled.
     */
    dbClusterId?: pulumi.Input<string | undefined>;
    /**
     * The PolarDB DynamoDB-compatible endpoint URL, in the format `http://<connection_string>:5432`.
     */
    endpoint?: pulumi.Input<string | undefined>;
    /**
     * Describe a GSI for the table. See `globalSecondaryIndex` below. Changing the key schema or projection of an existing index recreates that index.
     */
    globalSecondaryIndices?: pulumi.Input<pulumi.Input<inputs.polardb.DynamoTableGlobalSecondaryIndex>[] | undefined>;
    /**
     * The attribute name used as the partition key (hash key) of the table.
     */
    hashKey?: pulumi.Input<string | undefined>;
    /**
     * Describe an LSI on the table. See `localSecondaryIndex` below.
     */
    localSecondaryIndices?: pulumi.Input<pulumi.Input<inputs.polardb.DynamoTableLocalSecondaryIndex>[] | undefined>;
    /**
     * The attribute name used as the sort key (range key) of the table.
     */
    rangeKey?: pulumi.Input<string | undefined>;
    /**
     * The number of read capacity units. Required when `billingMode` is `PROVISIONED`.
     */
    readCapacity?: pulumi.Input<number | undefined>;
    /**
     * The name of the DynamoDB-compatible table.
     */
    tableName?: pulumi.Input<string | undefined>;
    /**
     * Configuration block for TTL. See `ttl` below.
     */
    ttl?: pulumi.Input<inputs.polardb.DynamoTableTtl | undefined>;
    /**
     * The number of write capacity units. Required when `billingMode` is `PROVISIONED`.
     */
    writeCapacity?: pulumi.Input<number | undefined>;
}

/**
 * The set of arguments for constructing a DynamoTable resource.
 */
export interface DynamoTableArgs {
    /**
     * The authentication password for PolarDB DynamoDB. Usually references the `dynamodbAuthPassword` attribute of an `alicloud.polardb.Account` with `accountType = "DynamoDB"`. If not set, it is resolved from the cluster's DynamoDB-type account automatically.
     */
    accountAuth?: pulumi.Input<string | undefined>;
    /**
     * The account name for PolarDB DynamoDB authentication. If not set, it is resolved from the cluster's DynamoDB-type account automatically.
     */
    accountName?: pulumi.Input<string | undefined>;
    /**
     * List of attribute definitions for the table key schema and indexes. See `attribute` below.
     */
    attributes?: pulumi.Input<pulumi.Input<inputs.polardb.DynamoTableAttribute>[] | undefined>;
    /**
     * The billing mode of the table. Valid values: `PROVISIONED`, `PAY_PER_REQUEST`. Default to `PROVISIONED`.
     */
    billingMode?: pulumi.Input<string | undefined>;
    /**
     * The ID of the PolarDB cluster where DynamoDB is enabled.
     */
    dbClusterId: pulumi.Input<string>;
    /**
     * The PolarDB DynamoDB-compatible endpoint URL, in the format `http://<connection_string>:5432`.
     */
    endpoint: pulumi.Input<string>;
    /**
     * Describe a GSI for the table. See `globalSecondaryIndex` below. Changing the key schema or projection of an existing index recreates that index.
     */
    globalSecondaryIndices?: pulumi.Input<pulumi.Input<inputs.polardb.DynamoTableGlobalSecondaryIndex>[] | undefined>;
    /**
     * The attribute name used as the partition key (hash key) of the table.
     */
    hashKey?: pulumi.Input<string | undefined>;
    /**
     * Describe an LSI on the table. See `localSecondaryIndex` below.
     */
    localSecondaryIndices?: pulumi.Input<pulumi.Input<inputs.polardb.DynamoTableLocalSecondaryIndex>[] | undefined>;
    /**
     * The attribute name used as the sort key (range key) of the table.
     */
    rangeKey?: pulumi.Input<string | undefined>;
    /**
     * The number of read capacity units. Required when `billingMode` is `PROVISIONED`.
     */
    readCapacity?: pulumi.Input<number | undefined>;
    /**
     * The name of the DynamoDB-compatible table.
     */
    tableName: pulumi.Input<string>;
    /**
     * Configuration block for TTL. See `ttl` below.
     */
    ttl?: pulumi.Input<inputs.polardb.DynamoTableTtl | undefined>;
    /**
     * The number of write capacity units. Required when `billingMode` is `PROVISIONED`.
     */
    writeCapacity?: pulumi.Input<number | undefined>;
}
