// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provides a Threat Detection Monitor Account resource.
 *
 * Multi-account management account. The member accounts in the resource directory are added to the monitor account list of Threat Detection (Security Center), so that the security administrator account can manage the security of these member accounts in a unified manner.
 *
 * For information about Threat Detection Monitor Account and how to use it, see [What is Monitor Account](https://next.api.alibabacloud.com/document/Sas/2018-12-03/CreateMonitorAccount).
 *
 * > **NOTE:** Available since v1.292.0.
 *
 * ## Example Usage
 *
 * Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * // The IDs of the member accounts in the resource directory. Multiple IDs must be separated by commas (,).
 * const memberAccountIds = config.require("memberAccountIds");
 * const _default = new alicloud.threatdetection.MonitorAccount("default", {accountIds: memberAccountIds});
 * ```
 *
 * ### Deleting `alicloud.threatdetection.MonitorAccount` or removing it from your configuration
 *
 * Terraform cannot destroy resource `alicloud.threatdetection.MonitorAccount`. Terraform will remove this resource from the state file, however resources may remain.
 *
 * 📚 Need more examples? VIEW MORE EXAMPLES
 *
 * ## Import
 *
 * Threat Detection Monitor Account can be imported using the id, e.g.
 *
 * ```sh
 * $ terraform import alicloud_threat_detection_monitor_account.example <Alibaba Cloud Account ID>
 * ```
 */
export class MonitorAccount extends pulumi.CustomResource {
    /**
     * Get an existing MonitorAccount resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: MonitorAccountState, opts?: pulumi.CustomResourceOptions): MonitorAccount {
        return new MonitorAccount(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'alicloud:threatdetection/monitorAccount:MonitorAccount';

    /**
     * Returns true if the given object is an instance of MonitorAccount.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is MonitorAccount {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === MonitorAccount.__pulumiType;
    }

    /**
     * The list of member account IDs in the resource directory. You can call [ListAccountsInResourceDirectory](https://next.api.alibabacloud.com/document/Sas/2018-12-03/ListAccountsInResourceDirectory) to obtain the member account IDs. Multiple member account IDs must be separated by commas (,). The monitor account list is fully replaced by the incoming list. If this parameter is not specified, the existing monitor account list is cleared.
     */
    declare public readonly accountIds: pulumi.Output<string | undefined>;

    /**
     * Create a MonitorAccount resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args?: MonitorAccountArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: MonitorAccountArgs | MonitorAccountState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as MonitorAccountState | undefined;
            resourceInputs["accountIds"] = state?.accountIds;
        } else {
            const args = argsOrState as MonitorAccountArgs | undefined;
            resourceInputs["accountIds"] = args?.accountIds;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(MonitorAccount.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering MonitorAccount resources.
 */
export interface MonitorAccountState {
    /**
     * The list of member account IDs in the resource directory. You can call [ListAccountsInResourceDirectory](https://next.api.alibabacloud.com/document/Sas/2018-12-03/ListAccountsInResourceDirectory) to obtain the member account IDs. Multiple member account IDs must be separated by commas (,). The monitor account list is fully replaced by the incoming list. If this parameter is not specified, the existing monitor account list is cleared.
     */
    accountIds?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a MonitorAccount resource.
 */
export interface MonitorAccountArgs {
    /**
     * The list of member account IDs in the resource directory. You can call [ListAccountsInResourceDirectory](https://next.api.alibabacloud.com/document/Sas/2018-12-03/ListAccountsInResourceDirectory) to obtain the member account IDs. Multiple member account IDs must be separated by commas (,). The monitor account list is fully replaced by the incoming list. If this parameter is not specified, the existing monitor account list is cleared.
     */
    accountIds?: pulumi.Input<string | undefined>;
}
