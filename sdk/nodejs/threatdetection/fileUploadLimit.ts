// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provides a Threat Detection File Upload Limit resource. User-defined file upload limit.
 *
 * For information about Threat Detection File Upload Limit and how to use it, see [What is File Upload Limit](https://next.api.alibabacloud.com/document/Sas/2018-12-03/GetFileUploadLimit).
 *
 * > **NOTE:** Available since v1.212.0.
 *
 * ## Example Usage
 *
 * Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const _default = new alicloud.threatdetection.FileUploadLimit("default", {limit: 100});
 * ```
 *
 * ### Deleting `alicloud.threatdetection.FileUploadLimit` or removing it from your configuration
 *
 * Terraform cannot destroy resource `alicloud.threatdetection.FileUploadLimit`. Terraform will remove this resource from the state file, however resources may remain.
 *
 * 📚 Need more examples? VIEW MORE EXAMPLES
 *
 * ## Import
 *
 * Threat Detection File Upload Limit can be imported using the id, e.g.
 *
 * ```sh
 * $ terraform import alicloud_threat_detection_file_upload_limit.example 
 * ```
 */
export class FileUploadLimit extends pulumi.CustomResource {
    /**
     * Get an existing FileUploadLimit resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: FileUploadLimitState, opts?: pulumi.CustomResourceOptions): FileUploadLimit {
        return new FileUploadLimit(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'alicloud:threatdetection/fileUploadLimit:FileUploadLimit';

    /**
     * Returns true if the given object is an instance of FileUploadLimit.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is FileUploadLimit {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === FileUploadLimit.__pulumiType;
    }

    /**
     * File Upload Threshold.
     */
    declare public readonly limit: pulumi.Output<number>;

    /**
     * Create a FileUploadLimit resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: FileUploadLimitArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: FileUploadLimitArgs | FileUploadLimitState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as FileUploadLimitState | undefined;
            resourceInputs["limit"] = state?.limit;
        } else {
            const args = argsOrState as FileUploadLimitArgs | undefined;
            if (args?.limit === undefined && !opts.urn) {
                throw new Error("Missing required property 'limit'");
            }
            resourceInputs["limit"] = args?.limit;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(FileUploadLimit.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering FileUploadLimit resources.
 */
export interface FileUploadLimitState {
    /**
     * File Upload Threshold.
     */
    limit?: pulumi.Input<number>;
}

/**
 * The set of arguments for constructing a FileUploadLimit resource.
 */
export interface FileUploadLimitArgs {
    /**
     * File Upload Threshold.
     */
    limit: pulumi.Input<number>;
}
