// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provides a Message Service Account Logging resource.
 *
 * Account Logging Configuration.
 *
 * For information about Message Service Account Logging and how to use it, see [What is Account Logging](https://next.api.alibabacloud.com/document/Mns-open/2022-01-19/SetAccountAttributes).
 *
 * > **NOTE:** Available since v1.287.0.
 *
 * ## Example Usage
 *
 * Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const _default = new alicloud.log.Project("default", {
 *     projectName: name,
 *     description: "example project for account logging",
 * });
 * const defaultStore = new alicloud.log.Store("default", {
 *     projectName: _default.projectName,
 *     logstoreName: name,
 *     retentionPeriod: 30,
 *     shardCount: 2,
 * });
 * const defaultServiceAccountLogging = new alicloud.message.ServiceAccountLogging("default", {
 *     logEnabled: true,
 *     messageTraceEnabled: false,
 *     projectName: _default.projectName,
 *     logStoreName: defaultStore.logstoreName,
 * });
 * ```
 *
 * ### Deleting `alicloud.message.ServiceAccountLogging` or removing it from your configuration
 *
 * Terraform cannot destroy resource `alicloud.message.ServiceAccountLogging`. Terraform will remove this resource from the state file, however resources may remain.
 *
 * 📚 Need more examples? VIEW MORE EXAMPLES
 *
 * ## Import
 *
 * Message Service Account Logging can be imported using the id, e.g.
 *
 * ```sh
 * $ terraform import alicloud_message_service_account_logging.example <Alibaba Cloud Account ID>
 * ```
 */
export class ServiceAccountLogging extends pulumi.CustomResource {
    /**
     * Get an existing ServiceAccountLogging resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: ServiceAccountLoggingState, opts?: pulumi.CustomResourceOptions): ServiceAccountLogging {
        return new ServiceAccountLogging(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'alicloud:message/serviceAccountLogging:ServiceAccountLogging';

    /**
     * Returns true if the given object is an instance of ServiceAccountLogging.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is ServiceAccountLogging {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === ServiceAccountLogging.__pulumiType;
    }

    /**
     * Whether to enable delivering the account operation logs to Simple Log Service (SLS).
     */
    declare public readonly logEnabled: pulumi.Output<boolean>;
    /**
     * The name of the SLS Logstore that receives the logs. Required when `logEnabled` is `true`.
     */
    declare public readonly logStoreName: pulumi.Output<string>;
    /**
     * Whether to enable the message trace feature.
     */
    declare public readonly messageTraceEnabled: pulumi.Output<boolean>;
    /**
     * The name of the SLS Project that the Logstore belongs to. Required when `logEnabled` is `true`.
     */
    declare public readonly projectName: pulumi.Output<string>;

    /**
     * Create a ServiceAccountLogging resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: ServiceAccountLoggingArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: ServiceAccountLoggingArgs | ServiceAccountLoggingState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as ServiceAccountLoggingState | undefined;
            resourceInputs["logEnabled"] = state?.logEnabled;
            resourceInputs["logStoreName"] = state?.logStoreName;
            resourceInputs["messageTraceEnabled"] = state?.messageTraceEnabled;
            resourceInputs["projectName"] = state?.projectName;
        } else {
            const args = argsOrState as ServiceAccountLoggingArgs | undefined;
            if (args?.logEnabled === undefined && !opts.urn) {
                throw new Error("Missing required property 'logEnabled'");
            }
            resourceInputs["logEnabled"] = args?.logEnabled;
            resourceInputs["logStoreName"] = args?.logStoreName;
            resourceInputs["messageTraceEnabled"] = args?.messageTraceEnabled;
            resourceInputs["projectName"] = args?.projectName;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(ServiceAccountLogging.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering ServiceAccountLogging resources.
 */
export interface ServiceAccountLoggingState {
    /**
     * Whether to enable delivering the account operation logs to Simple Log Service (SLS).
     */
    logEnabled?: pulumi.Input<boolean | undefined>;
    /**
     * The name of the SLS Logstore that receives the logs. Required when `logEnabled` is `true`.
     */
    logStoreName?: pulumi.Input<string | undefined>;
    /**
     * Whether to enable the message trace feature.
     */
    messageTraceEnabled?: pulumi.Input<boolean | undefined>;
    /**
     * The name of the SLS Project that the Logstore belongs to. Required when `logEnabled` is `true`.
     */
    projectName?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a ServiceAccountLogging resource.
 */
export interface ServiceAccountLoggingArgs {
    /**
     * Whether to enable delivering the account operation logs to Simple Log Service (SLS).
     */
    logEnabled: pulumi.Input<boolean>;
    /**
     * The name of the SLS Logstore that receives the logs. Required when `logEnabled` is `true`.
     */
    logStoreName?: pulumi.Input<string | undefined>;
    /**
     * Whether to enable the message trace feature.
     */
    messageTraceEnabled?: pulumi.Input<boolean | undefined>;
    /**
     * The name of the SLS Project that the Logstore belongs to. Required when `logEnabled` is `true`.
     */
    projectName?: pulumi.Input<string | undefined>;
}
