// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provides a Container Service for Kubernetes (ACK) Policy Instance resource.
 *
 * For information about Container Service for Kubernetes (ACK) Policy Instance and how to use it, see [What is Policy Instance](https://next.api.alibabacloud.com/document/CS/2015-12-15/DeployPolicyInstance).
 *
 * > **NOTE:** Available since v1.266.0.
 *
 * ## Example Usage
 *
 * Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 * import * as random from "@pulumi/random";
 * import * as std from "@pulumi/std";
 *
 * const config = new pulumi.Config();
 * const vpcCidr = config.get("vpcCidr") || "10.0.0.0/8";
 * const vswitchCidrs = config.getObject<Array<string>>("vswitchCidrs") || [
 *     "10.1.0.0/16",
 *     "10.2.0.0/16",
 * ];
 * const clusterName = config.get("clusterName") || "example-create-cluster";
 * const podCidr = config.get("podCidr") || "172.16.0.0/16";
 * const serviceCidr = config.get("serviceCidr") || "192.168.0.0/16";
 * const policyName = config.get("policyName") || "ACKPSPHostNetworkingPorts";
 * const enhanced = alicloud.vpc.getEnhancedNatAvailableZones({});
 * const _default = new random.index.Integer("default", {
 *     max: 99999,
 *     min: 10000,
 * });
 * const createVPC = new alicloud.vpc.Network("CreateVPC", {cidrBlock: vpcCidr});
 * // According to the vswitch cidr blocks to launch several vswitches
 * const createVSwitch: alicloud.vpc.Switch[] = [];
 * for (const range = {value: 0}; range.value < vswitchCidrs.length; range.value++) {
 *     createVSwitch.push(new alicloud.vpc.Switch(`CreateVSwitch-${range.value}`, {
 *         vpcId: createVPC.id,
 *         cidrBlock: vswitchCidrs[range.value],
 *         zoneId: enhanced.then(enhanced => enhanced.zones[range.value].zoneId),
 *     }));
 * }
 * const createCluster = new alicloud.cs.ManagedKubernetes("CreateCluster", {
 *     name: `${clusterName}-${_default.result}`,
 *     clusterSpec: "ack.pro.small",
 *     profile: "Default",
 *     vswitchIds: std.joinOutput({
 *         separator: ",",
 *         input: createVSwitch.map(__item => __item.id),
 *     }).apply(invoke => std.splitOutput({
 *         separator: ",",
 *         text: invoke.result,
 *     })).apply(invoke => invoke.result),
 *     podCidr: podCidr,
 *     serviceCidr: serviceCidr,
 *     isEnterpriseSecurityGroup: true,
 *     ipStack: "ipv4",
 *     proxyMode: "ipvs",
 *     deletionProtection: false,
 *     addons: [
 *         {
 *             name: "gatekeeper",
 *         },
 *         {
 *             name: "loongcollector",
 *         },
 *         {
 *             name: "policy-template-controller",
 *         },
 *     ],
 *     operationPolicy: {
 *         clusterAutoUpgrade: {
 *             enabled: false,
 *         },
 *     },
 *     maintenanceWindow: {
 *         enable: false,
 *     },
 * });
 * const defaultKubernetesPolicyInstance = new alicloud.cs.KubernetesPolicyInstance("default", {
 *     clusterId: createCluster.id,
 *     policyName: policyName,
 *     action: "deny",
 *     namespaces: ["test"],
 *     parameters: {
 *         hostNetwork: "true",
 *         min: "20",
 *         max: "200",
 *     },
 * });
 * ```
 *
 * 📚 Need more examples? VIEW MORE EXAMPLES
 *
 * ## Import
 *
 * Container Service for Kubernetes (ACK) Policy Instance can be imported using the id, e.g.
 *
 * ```sh
 * $ pulumi import alicloud:cs/kubernetesPolicyInstance:KubernetesPolicyInstance example <cluster_id>:<policy_name>:<instance_name>
 * ```
 */
export class KubernetesPolicyInstance extends pulumi.CustomResource {
    /**
     * Get an existing KubernetesPolicyInstance resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: KubernetesPolicyInstanceState, opts?: pulumi.CustomResourceOptions): KubernetesPolicyInstance {
        return new KubernetesPolicyInstance(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'alicloud:cs/kubernetesPolicyInstance:KubernetesPolicyInstance';

    /**
     * Returns true if the given object is an instance of KubernetesPolicyInstance.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is KubernetesPolicyInstance {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === KubernetesPolicyInstance.__pulumiType;
    }

    /**
     * Policy Governance Implementation Actions
     */
    declare public readonly action: pulumi.Output<string | undefined>;
    /**
     * Target cluster ID
     */
    declare public readonly clusterId: pulumi.Output<string>;
    /**
     * Rule Instance Name
     */
    declare public /*out*/ readonly instanceName: pulumi.Output<string>;
    /**
     * Limits the namespace of the policy implementation. Empty indicates all namespaces.
     */
    declare public readonly namespaces: pulumi.Output<string[] | undefined>;
    /**
     * The parameter configuration of the current rule instance. For more information about the parameters supported by each policy rule, see [Container Security Policy Rule Base Description](https://www.alibabacloud.com/help/doc-detail/359819.html).
     */
    declare public readonly parameters: pulumi.Output<{[key: string]: string} | undefined>;
    /**
     * Policy Name
     */
    declare public readonly policyName: pulumi.Output<string>;

    /**
     * Create a KubernetesPolicyInstance resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: KubernetesPolicyInstanceArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: KubernetesPolicyInstanceArgs | KubernetesPolicyInstanceState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as KubernetesPolicyInstanceState | undefined;
            resourceInputs["action"] = state?.action;
            resourceInputs["clusterId"] = state?.clusterId;
            resourceInputs["instanceName"] = state?.instanceName;
            resourceInputs["namespaces"] = state?.namespaces;
            resourceInputs["parameters"] = state?.parameters;
            resourceInputs["policyName"] = state?.policyName;
        } else {
            const args = argsOrState as KubernetesPolicyInstanceArgs | undefined;
            if (args?.clusterId === undefined && !opts.urn) {
                throw new Error("Missing required property 'clusterId'");
            }
            if (args?.policyName === undefined && !opts.urn) {
                throw new Error("Missing required property 'policyName'");
            }
            resourceInputs["action"] = args?.action;
            resourceInputs["clusterId"] = args?.clusterId;
            resourceInputs["namespaces"] = args?.namespaces;
            resourceInputs["parameters"] = args?.parameters;
            resourceInputs["policyName"] = args?.policyName;
            resourceInputs["instanceName"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(KubernetesPolicyInstance.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering KubernetesPolicyInstance resources.
 */
export interface KubernetesPolicyInstanceState {
    /**
     * Policy Governance Implementation Actions
     */
    action?: pulumi.Input<string>;
    /**
     * Target cluster ID
     */
    clusterId?: pulumi.Input<string>;
    /**
     * Rule Instance Name
     */
    instanceName?: pulumi.Input<string>;
    /**
     * Limits the namespace of the policy implementation. Empty indicates all namespaces.
     */
    namespaces?: pulumi.Input<pulumi.Input<string>[]>;
    /**
     * The parameter configuration of the current rule instance. For more information about the parameters supported by each policy rule, see [Container Security Policy Rule Base Description](https://www.alibabacloud.com/help/doc-detail/359819.html).
     */
    parameters?: pulumi.Input<{[key: string]: pulumi.Input<string>}>;
    /**
     * Policy Name
     */
    policyName?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a KubernetesPolicyInstance resource.
 */
export interface KubernetesPolicyInstanceArgs {
    /**
     * Policy Governance Implementation Actions
     */
    action?: pulumi.Input<string>;
    /**
     * Target cluster ID
     */
    clusterId: pulumi.Input<string>;
    /**
     * Limits the namespace of the policy implementation. Empty indicates all namespaces.
     */
    namespaces?: pulumi.Input<pulumi.Input<string>[]>;
    /**
     * The parameter configuration of the current rule instance. For more information about the parameters supported by each policy rule, see [Container Security Policy Rule Base Description](https://www.alibabacloud.com/help/doc-detail/359819.html).
     */
    parameters?: pulumi.Input<{[key: string]: pulumi.Input<string>}>;
    /**
     * Policy Name
     */
    policyName: pulumi.Input<string>;
}
