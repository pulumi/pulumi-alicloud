// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provides a Certificate Management Service (Original SSL Certificate) Certificate Validation resource.
 *
 * Waits until the certificate application submitted on a certificate instance has been issued, and exposes the resulting certificate for downstream services to reference.
 *
 * Certificate validation is the step between applying and issuance: the certificate authority verifies that the applicant controls the domain — for a DV certificate typically by checking a DNS record published under it, carrying the values that `alicloud.sslcertificatesservicecertificate.Apply` reports in `domainValidationList`. Once that verification passes, the authority issues the certificate a few minutes later. This resource represents the wait for that outcome.
 *
 * For information about Certificate Management Service (Original SSL Certificate) and domain validation, see [SSL Certificates Service](https://www.alibabacloud.com/help/product/28533.html).
 *
 * > **NOTE:** Available since v1.287.0.
 *
 * > **NOTE:** This resource creates nothing in the cloud. It only waits: after the application has been submitted and the domain validation records have been published, it polls the instance until the certificate is issued. Destroying it calls no API and leaves the certificate untouched. It exists so that downstream resources can depend on a certificate that has actually been issued, rather than on one that has merely been requested.
 *
 * ## Example Usage
 *
 * Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const _default = new alicloud.sslcertificatesserviceinstance.SslCertificatesServiceInstance("default", {
 *     productType: "cas",
 *     period: 12,
 *     pricingCycle: 2,
 *     instanceName: name,
 *     domain: "example.com",
 *     validationMethod: "DNS",
 *     parameters: [
 *         {
 *             code: "fullSpec",
 *             value: "ws.dv.f",
 *         },
 *         {
 *             code: "fullDomainCount",
 *             value: "1",
 *         },
 *     ],
 * });
 * const defaultApply = new alicloud.sslcertificatesservicecertificate.Apply("default", {
 *     instanceId: _default.id,
 *     domain: _default.domain,
 *     validationMethod: _default.validationMethod,
 * });
 * const defaultAlidnsRecord: {[key: string]: alicloud.dns.AlidnsRecord} = {};
 * defaultApply.domainValidationLists.apply(domainValidationLists => {
 *     const defaultAlidnsRecord: {[key: string]: alicloud.dns.AlidnsRecord} = {};
 * pulumi.all(domainValidationLists.reduce((__obj, v) => ({ ...__obj, [v.domain]: v }), {})).apply(rangeBody => {
 *         for (const range of Object.entries(rangeBody).sort().map(([k, v]) => ({key: k, value: v}))) {
 *             defaultAlidnsRecord[range.key] = new alicloud.dns.AlidnsRecord(`default-${range.key}`, {
 *                 domainName: range.value.rootDomain,
 *                 rr: range.value.validationKey,
 *                 type: range.value.validationType,
 *                 value: range.value.validationValue,
 *                 ttl: 600,
 *             });
 *         }
 *     });
 * });
 * const defaultValidation = new alicloud.sslcertificatesservicecertificate.Validation("default", {
 *     instanceId: _default.id,
 *     validationRecordIds: defaultAlidnsRecord.apply(defaultAlidnsRecord => Object.values(defaultAlidnsRecord).map(r => (r.id))),
 * });
 * ```
 *
 * Downstream services should reference the certificate exposed by this resource, so that the reference is only resolved once the certificate actually exists:
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const _default = new alicloud.alb.Listener("default", {certificates: {
 *     certificateId: defaultAlicloudSslCertificatesServiceCertificateValidation.certIdentifier,
 * }});
 * ```
 *
 * ### Deleting `alicloud.sslcertificatesservicecertificate.Validation` or removing it from your configuration
 *
 * Terraform cannot destroy resource `alicloud.sslcertificatesservicecertificate.Validation`. Terraform will remove this resource from the state file, however resources may remain.
 *
 * 📚 Need more examples? VIEW MORE EXAMPLES
 *
 * ## Import
 *
 * Certificate Management Service (Original SSL Certificate) Certificate Validation can be imported using the id, e.g.
 *
 * ```sh
 * $ pulumi import alicloud:sslcertificatesservicecertificate/validation:Validation example <instance_id>
 * ```
 */
export class Validation extends pulumi.CustomResource {
    /**
     * Get an existing Validation resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: ValidationState, opts?: pulumi.CustomResourceOptions): Validation {
        return new Validation(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'alicloud:sslcertificatesservicecertificate/validation:Validation';

    /**
     * Returns true if the given object is an instance of Validation.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is Validation {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === Validation.__pulumiType;
    }

    /**
     * The global certificate identifier, formatted as the certificate ID plus `-` plus the site region ID. Alibaba Cloud services such as ALB, CDN and WAF reference a certificate by this value.
     */
    declare public /*out*/ readonly certIdentifier: pulumi.Output<string>;
    /**
     * The ID of the issued certificate.
     */
    declare public /*out*/ readonly certificateId: pulumi.Output<number>;
    /**
     * The status of the issued certificate.
     */
    declare public /*out*/ readonly certificateStatus: pulumi.Output<string>;
    /**
     * The ID of the certificate instance whose application is being waited on.
     */
    declare public readonly instanceId: pulumi.Output<string>;
    /**
     * The IDs of the DNS records carrying the domain ownership validation information. The values themselves are never read; they exist so that Terraform creates the validation records before it starts waiting for issuance.
     */
    declare public readonly validationRecordIds: pulumi.Output<string[] | undefined>;

    /**
     * Create a Validation resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: ValidationArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: ValidationArgs | ValidationState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as ValidationState | undefined;
            resourceInputs["certIdentifier"] = state?.certIdentifier;
            resourceInputs["certificateId"] = state?.certificateId;
            resourceInputs["certificateStatus"] = state?.certificateStatus;
            resourceInputs["instanceId"] = state?.instanceId;
            resourceInputs["validationRecordIds"] = state?.validationRecordIds;
        } else {
            const args = argsOrState as ValidationArgs | undefined;
            if (args?.instanceId === undefined && !opts.urn) {
                throw new Error("Missing required property 'instanceId'");
            }
            resourceInputs["instanceId"] = args?.instanceId;
            resourceInputs["validationRecordIds"] = args?.validationRecordIds;
            resourceInputs["certIdentifier"] = undefined /*out*/;
            resourceInputs["certificateId"] = undefined /*out*/;
            resourceInputs["certificateStatus"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(Validation.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering Validation resources.
 */
export interface ValidationState {
    /**
     * The global certificate identifier, formatted as the certificate ID plus `-` plus the site region ID. Alibaba Cloud services such as ALB, CDN and WAF reference a certificate by this value.
     */
    certIdentifier?: pulumi.Input<string | undefined>;
    /**
     * The ID of the issued certificate.
     */
    certificateId?: pulumi.Input<number | undefined>;
    /**
     * The status of the issued certificate.
     */
    certificateStatus?: pulumi.Input<string | undefined>;
    /**
     * The ID of the certificate instance whose application is being waited on.
     */
    instanceId?: pulumi.Input<string | undefined>;
    /**
     * The IDs of the DNS records carrying the domain ownership validation information. The values themselves are never read; they exist so that Terraform creates the validation records before it starts waiting for issuance.
     */
    validationRecordIds?: pulumi.Input<pulumi.Input<string>[] | undefined>;
}

/**
 * The set of arguments for constructing a Validation resource.
 */
export interface ValidationArgs {
    /**
     * The ID of the certificate instance whose application is being waited on.
     */
    instanceId: pulumi.Input<string>;
    /**
     * The IDs of the DNS records carrying the domain ownership validation information. The values themselves are never read; they exist so that Terraform creates the validation records before it starts waiting for issuance.
     */
    validationRecordIds?: pulumi.Input<pulumi.Input<string>[] | undefined>;
}
