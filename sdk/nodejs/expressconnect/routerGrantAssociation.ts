// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provides a Express Connect Router Grant Association resource.
 *
 * Network instances authorized to the leased line Gateway.
 *
 * For information about Express Connect Router Grant Association and how to use it, see [What is Grant Association](https://www.alibabacloud.com/help/en/express-connect/developer-reference/api-expressconnectrouter-2023-09-01-grantinstancetoexpressconnectrouter).
 *
 * > **NOTE:** Available since v1.239.0.
 *
 * ## Example Usage
 *
 * Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const vpcId = config.get("vpcId") || "vpc-xxx";
 * const ecrOwnerUid = config.get("ecrOwnerUid") || "18xxx";
 * const ecrId = config.get("ecrId") || "ecr-xxx";
 * const region = config.get("region") || "cn-hangzhou";
 * const _default = new alicloud.expressconnect.RouterGrantAssociation("default", {
 *     ecrId: ecrId,
 *     instanceRegionId: region,
 *     instanceId: vpcId,
 *     ecrOwnerAliUid: ecrOwnerUid,
 *     instanceType: "VPC",
 * });
 * ```
 *
 * ## Import
 *
 * Express Connect Router Grant Association can be imported using the id, e.g.
 *
 * ```sh
 * $ pulumi import alicloud:expressconnect/routerGrantAssociation:RouterGrantAssociation example <ecr_id>:<instance_id>:<instance_region_id>:<ecr_owner_ali_uid>:<instance_type>
 * ```
 */
export class RouterGrantAssociation extends pulumi.CustomResource {
    /**
     * Get an existing RouterGrantAssociation resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: RouterGrantAssociationState, opts?: pulumi.CustomResourceOptions): RouterGrantAssociation {
        return new RouterGrantAssociation(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'alicloud:expressconnect/routerGrantAssociation:RouterGrantAssociation';

    /**
     * Returns true if the given object is an instance of RouterGrantAssociation.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is RouterGrantAssociation {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === RouterGrantAssociation.__pulumiType;
    }

    /**
     * The ID of the associated Leased Line Gateway instance.
     */
    declare public readonly ecrId: pulumi.Output<string>;
    /**
     * The ID of the Alibaba Cloud account (primary account) to which the leased line gateway instance is authorized.
     */
    declare public readonly ecrOwnerAliUid: pulumi.Output<number>;
    /**
     * The ID of the network instance.
     */
    declare public readonly instanceId: pulumi.Output<string>;
    /**
     * The ID of the region where the authorized network instance is located.
     */
    declare public readonly instanceRegionId: pulumi.Output<string>;
    /**
     * The type of the network instance. Value:
     * - `VBR`: the VBR instance.
     * - `VPC`: VPC instance.
     */
    declare public readonly instanceType: pulumi.Output<string>;
    /**
     * The status of the resource
     */
    declare public /*out*/ readonly status: pulumi.Output<string>;

    /**
     * Create a RouterGrantAssociation resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: RouterGrantAssociationArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: RouterGrantAssociationArgs | RouterGrantAssociationState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as RouterGrantAssociationState | undefined;
            resourceInputs["ecrId"] = state?.ecrId;
            resourceInputs["ecrOwnerAliUid"] = state?.ecrOwnerAliUid;
            resourceInputs["instanceId"] = state?.instanceId;
            resourceInputs["instanceRegionId"] = state?.instanceRegionId;
            resourceInputs["instanceType"] = state?.instanceType;
            resourceInputs["status"] = state?.status;
        } else {
            const args = argsOrState as RouterGrantAssociationArgs | undefined;
            if (args?.ecrId === undefined && !opts.urn) {
                throw new Error("Missing required property 'ecrId'");
            }
            if (args?.ecrOwnerAliUid === undefined && !opts.urn) {
                throw new Error("Missing required property 'ecrOwnerAliUid'");
            }
            if (args?.instanceId === undefined && !opts.urn) {
                throw new Error("Missing required property 'instanceId'");
            }
            if (args?.instanceRegionId === undefined && !opts.urn) {
                throw new Error("Missing required property 'instanceRegionId'");
            }
            if (args?.instanceType === undefined && !opts.urn) {
                throw new Error("Missing required property 'instanceType'");
            }
            resourceInputs["ecrId"] = args?.ecrId;
            resourceInputs["ecrOwnerAliUid"] = args?.ecrOwnerAliUid;
            resourceInputs["instanceId"] = args?.instanceId;
            resourceInputs["instanceRegionId"] = args?.instanceRegionId;
            resourceInputs["instanceType"] = args?.instanceType;
            resourceInputs["status"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(RouterGrantAssociation.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering RouterGrantAssociation resources.
 */
export interface RouterGrantAssociationState {
    /**
     * The ID of the associated Leased Line Gateway instance.
     */
    ecrId?: pulumi.Input<string>;
    /**
     * The ID of the Alibaba Cloud account (primary account) to which the leased line gateway instance is authorized.
     */
    ecrOwnerAliUid?: pulumi.Input<number>;
    /**
     * The ID of the network instance.
     */
    instanceId?: pulumi.Input<string>;
    /**
     * The ID of the region where the authorized network instance is located.
     */
    instanceRegionId?: pulumi.Input<string>;
    /**
     * The type of the network instance. Value:
     * - `VBR`: the VBR instance.
     * - `VPC`: VPC instance.
     */
    instanceType?: pulumi.Input<string>;
    /**
     * The status of the resource
     */
    status?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a RouterGrantAssociation resource.
 */
export interface RouterGrantAssociationArgs {
    /**
     * The ID of the associated Leased Line Gateway instance.
     */
    ecrId: pulumi.Input<string>;
    /**
     * The ID of the Alibaba Cloud account (primary account) to which the leased line gateway instance is authorized.
     */
    ecrOwnerAliUid: pulumi.Input<number>;
    /**
     * The ID of the network instance.
     */
    instanceId: pulumi.Input<string>;
    /**
     * The ID of the region where the authorized network instance is located.
     */
    instanceRegionId: pulumi.Input<string>;
    /**
     * The type of the network instance. Value:
     * - `VBR`: the VBR instance.
     * - `VPC`: VPC instance.
     */
    instanceType: pulumi.Input<string>;
}
