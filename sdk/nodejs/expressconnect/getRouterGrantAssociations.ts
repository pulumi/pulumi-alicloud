// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * This data source provides the Express Connect Router Grant Association of the current Alibaba Cloud user.
 *
 * > **NOTE:** Available since v1.291.0.
 *
 * ## Example Usage
 *
 * Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const vpcId = config.get("vpcId") || "vpc-xxx";
 * const ecrOwnerUid = config.get("ecrOwnerUid") || "18xxx";
 * const ecrId = config.get("ecrId") || "ecr-xxx";
 * const region = config.get("region") || "cn-hangzhou";
 * const _default = new alicloud.expressconnect.RouterGrantAssociation("default", {
 *     ecrId: ecrId,
 *     instanceRegionId: region,
 *     instanceId: vpcId,
 *     ecrOwnerAliUid: Number(ecrOwnerUid),
 *     instanceType: "VPC",
 * });
 * const ids = alicloud.expressconnect.getRouterGrantAssociationsOutput({
 *     ids: [_default.id],
 *     ecrId: _default.ecrId,
 * });
 * export const expressConnectRouterGrantAssociationsId0 = ids.apply(ids => ids.associations?.[0]?.id);
 * ```
 */
export function getRouterGrantAssociations(args: GetRouterGrantAssociationsArgs, opts?: pulumi.InvokeOptions): Promise<GetRouterGrantAssociationsResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("alicloud:expressconnect/getRouterGrantAssociations:getRouterGrantAssociations", {
        "callerType": args.callerType,
        "ecrId": args.ecrId,
        "ids": args.ids,
        "instanceId": args.instanceId,
        "instanceRegionId": args.instanceRegionId,
        "instanceType": args.instanceType,
        "outputFile": args.outputFile,
    }, opts);
}

/**
 * A collection of arguments for invoking getRouterGrantAssociations.
 */
export interface GetRouterGrantAssociationsArgs {
    /**
     * The type of the caller. Default value: `ECR`. Valid values: `ECR`, `OTHER`.
     */
    callerType?: string;
    /**
     * The ID of the Express Connect Router instance.
     */
    ecrId: string;
    /**
     * A list of Tr Association IDs.
     */
    ids?: string[];
    /**
     * The ID of the authorized instance.
     */
    instanceId?: string;
    /**
     * The region where the authorized network instance is located.
     */
    instanceRegionId?: string;
    /**
     * The type of the network instance. Valid values:
     * - `VBR`: virtual border router (VBR).
     * - `VPC`: virtual private cloud (VPC).
     */
    instanceType?: string;
    /**
     * File name where to save data source results (after running `pulumi preview`).
     */
    outputFile?: string;
}

/**
 * A collection of values returned by getRouterGrantAssociations.
 */
export interface GetRouterGrantAssociationsResult {
    /**
     * A list of Grant Associations. Each element contains the following attributes:
     */
    readonly associations: outputs.expressconnect.GetRouterGrantAssociationsAssociation[];
    readonly callerType?: string;
    /**
     * The ID of the Express Connect Router instance.
     */
    readonly ecrId: string;
    /**
     * The provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
    readonly ids: string[];
    /**
     * The ID of the authorized instance.
     */
    readonly instanceId?: string;
    /**
     * The ID of the region to which the authorized instance belongs.
     */
    readonly instanceRegionId?: string;
    /**
     * The type of the network instance.
     */
    readonly instanceType?: string;
    readonly outputFile?: string;
}
/**
 * This data source provides the Express Connect Router Grant Association of the current Alibaba Cloud user.
 *
 * > **NOTE:** Available since v1.291.0.
 *
 * ## Example Usage
 *
 * Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const vpcId = config.get("vpcId") || "vpc-xxx";
 * const ecrOwnerUid = config.get("ecrOwnerUid") || "18xxx";
 * const ecrId = config.get("ecrId") || "ecr-xxx";
 * const region = config.get("region") || "cn-hangzhou";
 * const _default = new alicloud.expressconnect.RouterGrantAssociation("default", {
 *     ecrId: ecrId,
 *     instanceRegionId: region,
 *     instanceId: vpcId,
 *     ecrOwnerAliUid: Number(ecrOwnerUid),
 *     instanceType: "VPC",
 * });
 * const ids = alicloud.expressconnect.getRouterGrantAssociationsOutput({
 *     ids: [_default.id],
 *     ecrId: _default.ecrId,
 * });
 * export const expressConnectRouterGrantAssociationsId0 = ids.apply(ids => ids.associations?.[0]?.id);
 * ```
 */
export function getRouterGrantAssociationsOutput(args: GetRouterGrantAssociationsOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetRouterGrantAssociationsResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("alicloud:expressconnect/getRouterGrantAssociations:getRouterGrantAssociations", {
        "callerType": args.callerType,
        "ecrId": args.ecrId,
        "ids": args.ids,
        "instanceId": args.instanceId,
        "instanceRegionId": args.instanceRegionId,
        "instanceType": args.instanceType,
        "outputFile": args.outputFile,
    }, opts);
}

/**
 * A collection of arguments for invoking getRouterGrantAssociations.
 */
export interface GetRouterGrantAssociationsOutputArgs {
    /**
     * The type of the caller. Default value: `ECR`. Valid values: `ECR`, `OTHER`.
     */
    callerType?: pulumi.Input<string | undefined>;
    /**
     * The ID of the Express Connect Router instance.
     */
    ecrId: pulumi.Input<string>;
    /**
     * A list of Tr Association IDs.
     */
    ids?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * The ID of the authorized instance.
     */
    instanceId?: pulumi.Input<string | undefined>;
    /**
     * The region where the authorized network instance is located.
     */
    instanceRegionId?: pulumi.Input<string | undefined>;
    /**
     * The type of the network instance. Valid values:
     * - `VBR`: virtual border router (VBR).
     * - `VPC`: virtual private cloud (VPC).
     */
    instanceType?: pulumi.Input<string | undefined>;
    /**
     * File name where to save data source results (after running `pulumi preview`).
     */
    outputFile?: pulumi.Input<string | undefined>;
}
