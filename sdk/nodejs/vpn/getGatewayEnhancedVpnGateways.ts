// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * This data source provides Vpn Gateway Enhanced Vpn Gateway available to the user.[What is Enhanced Vpn Gateway](https://next.api.alibabacloud.com/document/Vpc/2016-04-28/CreateEnhancedVpnGateway)
 *
 * > **NOTE:** Available since v1.280.0.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const region = config.get("region") || "ap-southeast-3";
 * const zone2 = config.get("zone2") || "ap-southeast-3a";
 * const zone1 = config.get("zone1") || "ap-southeast-3b";
 * const defaulttYTx5F = new alicloud.vpc.Network("defaulttYTx5F", {
 *     cidrBlock: "192.168.0.0/16",
 *     isDefault: false,
 * });
 * const defaultTRk7k3 = new alicloud.vpc.Switch("defaultTRk7k3", {
 *     vpcId: defaulttYTx5F.id,
 *     zoneId: zone1,
 *     cidrBlock: "192.168.10.0/24",
 * });
 * const default23kGFr = new alicloud.vpc.Switch("default23kGFr", {
 *     vpcId: defaulttYTx5F.id,
 *     zoneId: zone2,
 *     cidrBlock: "192.168.20.0/24",
 * });
 * const defaultGatewayEnhancedVpnGateway = new alicloud.vpn.GatewayEnhancedVpnGateway("default", {
 *     vpnType: "Normal",
 *     description: "default",
 *     disasterRecoveryVswitchId: default23kGFr.id,
 *     vpcId: defaulttYTx5F.id,
 *     vpnGatewayName: "default",
 *     networkType: "public",
 *     vswitchId: defaultTRk7k3.id,
 *     gatewayType: "Enhanced.SiteToSite",
 *     autoPropagate: false,
 * });
 * const _default = alicloud.vpn.getGatewayEnhancedVpnGatewaysOutput({
 *     ids: [defaultGatewayEnhancedVpnGateway.id],
 *     vpcId: defaulttYTx5F.id,
 * });
 * export const alicloudVpnGatewayEnhancedVpnGatewayExampleId = _default.apply(_default => _default.gateways?.[0]?.id);
 * ```
 */
export function getGatewayEnhancedVpnGateways(args?: GetGatewayEnhancedVpnGatewaysArgs, opts?: pulumi.InvokeOptions): Promise<GetGatewayEnhancedVpnGatewaysResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("alicloud:vpn/getGatewayEnhancedVpnGateways:getGatewayEnhancedVpnGateways", {
        "enableDetails": args.enableDetails,
        "ids": args.ids,
        "outputFile": args.outputFile,
        "status": args.status,
        "vpcId": args.vpcId,
        "vpnInstanceId": args.vpnInstanceId,
    }, opts);
}

/**
 * A collection of arguments for invoking getGatewayEnhancedVpnGateways.
 */
export interface GetGatewayEnhancedVpnGatewaysArgs {
    /**
     * Default to `false`. Set it to `true` to query detailed attributes.
     */
    enableDetails?: boolean;
    /**
     * A list of Enhanced Vpn Gateway IDs.
     */
    ids?: string[];
    /**
     * File name where to save data source results (after running `pulumi preview`).
     */
    outputFile?: string;
    /**
     * The status of the resource
     */
    status?: string;
    /**
     * The ID of the VPC to which the VPN gateway belongs.
     */
    vpcId?: string;
    /**
     * The ID of the VPN gateway.
     */
    vpnInstanceId?: string;
}

/**
 * A collection of values returned by getGatewayEnhancedVpnGateways.
 */
export interface GetGatewayEnhancedVpnGatewaysResult {
    readonly enableDetails?: boolean;
    /**
     * A list of Enhanced Vpn Gateway Entries. Each element contains the following attributes:
     */
    readonly gateways: outputs.vpn.GetGatewayEnhancedVpnGatewaysGateway[];
    /**
     * The provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
    /**
     * A list of Enhanced Vpn Gateway IDs.
     */
    readonly ids: string[];
    readonly outputFile?: string;
    /**
     * The status of the resource.
     */
    readonly status?: string;
    /**
     * The ID of the VPC to which the VPN gateway belongs.
     */
    readonly vpcId?: string;
    /**
     * The ID of the VPN gateway.
     */
    readonly vpnInstanceId?: string;
}
/**
 * This data source provides Vpn Gateway Enhanced Vpn Gateway available to the user.[What is Enhanced Vpn Gateway](https://next.api.alibabacloud.com/document/Vpc/2016-04-28/CreateEnhancedVpnGateway)
 *
 * > **NOTE:** Available since v1.280.0.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const region = config.get("region") || "ap-southeast-3";
 * const zone2 = config.get("zone2") || "ap-southeast-3a";
 * const zone1 = config.get("zone1") || "ap-southeast-3b";
 * const defaulttYTx5F = new alicloud.vpc.Network("defaulttYTx5F", {
 *     cidrBlock: "192.168.0.0/16",
 *     isDefault: false,
 * });
 * const defaultTRk7k3 = new alicloud.vpc.Switch("defaultTRk7k3", {
 *     vpcId: defaulttYTx5F.id,
 *     zoneId: zone1,
 *     cidrBlock: "192.168.10.0/24",
 * });
 * const default23kGFr = new alicloud.vpc.Switch("default23kGFr", {
 *     vpcId: defaulttYTx5F.id,
 *     zoneId: zone2,
 *     cidrBlock: "192.168.20.0/24",
 * });
 * const defaultGatewayEnhancedVpnGateway = new alicloud.vpn.GatewayEnhancedVpnGateway("default", {
 *     vpnType: "Normal",
 *     description: "default",
 *     disasterRecoveryVswitchId: default23kGFr.id,
 *     vpcId: defaulttYTx5F.id,
 *     vpnGatewayName: "default",
 *     networkType: "public",
 *     vswitchId: defaultTRk7k3.id,
 *     gatewayType: "Enhanced.SiteToSite",
 *     autoPropagate: false,
 * });
 * const _default = alicloud.vpn.getGatewayEnhancedVpnGatewaysOutput({
 *     ids: [defaultGatewayEnhancedVpnGateway.id],
 *     vpcId: defaulttYTx5F.id,
 * });
 * export const alicloudVpnGatewayEnhancedVpnGatewayExampleId = _default.apply(_default => _default.gateways?.[0]?.id);
 * ```
 */
export function getGatewayEnhancedVpnGatewaysOutput(args?: GetGatewayEnhancedVpnGatewaysOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetGatewayEnhancedVpnGatewaysResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("alicloud:vpn/getGatewayEnhancedVpnGateways:getGatewayEnhancedVpnGateways", {
        "enableDetails": args.enableDetails,
        "ids": args.ids,
        "outputFile": args.outputFile,
        "status": args.status,
        "vpcId": args.vpcId,
        "vpnInstanceId": args.vpnInstanceId,
    }, opts);
}

/**
 * A collection of arguments for invoking getGatewayEnhancedVpnGateways.
 */
export interface GetGatewayEnhancedVpnGatewaysOutputArgs {
    /**
     * Default to `false`. Set it to `true` to query detailed attributes.
     */
    enableDetails?: pulumi.Input<boolean | undefined>;
    /**
     * A list of Enhanced Vpn Gateway IDs.
     */
    ids?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * File name where to save data source results (after running `pulumi preview`).
     */
    outputFile?: pulumi.Input<string | undefined>;
    /**
     * The status of the resource
     */
    status?: pulumi.Input<string | undefined>;
    /**
     * The ID of the VPC to which the VPN gateway belongs.
     */
    vpcId?: pulumi.Input<string | undefined>;
    /**
     * The ID of the VPN gateway.
     */
    vpnInstanceId?: pulumi.Input<string | undefined>;
}
