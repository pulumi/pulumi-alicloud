// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provides a File Storage (NAS) Protocol Service resource.
 *
 * For information about File Storage (NAS) Protocol Service and how to use it, see [What is Protocol Service](https://next.api.alibabacloud.com/document/NAS/2017-06-26/CreateProtocolService).
 *
 * > **NOTE:** Available since v1.267.0.
 *
 * ## Example Usage
 *
 * Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const example = new alicloud.vpc.Network("example", {
 *     isDefault: false,
 *     cidrBlock: "192.168.0.0/16",
 *     vpcName: "nas-examplee1031-vpc",
 *     enableIpv6: true,
 * });
 * const exampleSwitch = new alicloud.vpc.Switch("example", {
 *     isDefault: false,
 *     vpcId: example.id,
 *     zoneId: "cn-beijing-i",
 *     cidrBlock: "192.168.2.0/24",
 *     vswitchName: "nas-examplee1031-vsw1sdw-F",
 * });
 * const exampleFileSystem = new alicloud.nas.FileSystem("example", {
 *     description: name,
 *     storageType: "advance_100",
 *     zoneId: "cn-beijing-i",
 *     encryptType: 0,
 *     vpcId: example.id,
 *     capacity: 3600,
 *     protocolType: "cpfs",
 *     vswitchId: exampleSwitch.id,
 *     fileSystemType: "cpfs",
 * });
 * const _default = new alicloud.nas.ProtocolService("default", {
 *     vpcId: example.id,
 *     protocolType: "NFS",
 *     protocolSpec: "General",
 *     vswitchId: exampleSwitch.id,
 *     dryRun: false,
 *     fileSystemId: exampleFileSystem.id,
 * });
 * ```
 *
 * 📚 Need more examples? VIEW MORE EXAMPLES
 *
 * ## Import
 *
 * File Storage (NAS) Protocol Service can be imported using the id, e.g.
 *
 * ```sh
 * $ pulumi import alicloud:nas/protocolService:ProtocolService example <file_system_id>:<protocol_service_id>
 * ```
 */
export class ProtocolService extends pulumi.CustomResource {
    /**
     * Get an existing ProtocolService resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: ProtocolServiceState, opts?: pulumi.CustomResourceOptions): ProtocolService {
        return new ProtocolService(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'alicloud:nas/protocolService:ProtocolService';

    /**
     * Returns true if the given object is an instance of ProtocolService.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is ProtocolService {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === ProtocolService.__pulumiType;
    }

    /**
     * The time when the protocol server service was created. The UTC time.
     */
    declare public /*out*/ readonly createTime: pulumi.Output<string>;
    /**
     * Description of the agreement service.
     *
     * Limitations:
     * - Length is 2~128 English or Chinese characters.
     * - It must start with an uppercase or lowercase letter or Chinese, and cannot start with `http://` and `https://`.
     * - Can contain numbers, colons (:), underscores (_), or dashes (-).
     */
    declare public readonly description: pulumi.Output<string | undefined>;
    /**
     * Whether to PreCheck the creation request.
     *
     * The pre-check operation helps you check the validity of parameters and dependency conditions, and does not actually create an instance, nor does it incur costs.
     *
     * Value:
     * - true: The check request is sent and the protocol service is not created. The check items include whether the required parameters, request format, and business restriction dependency conditions are filled in. If the check does not pass, the corresponding error is returned. If the check passes, the 200 HttpCode is returned, but the ProtocolServiceId is empty.
     * - false (default): Send a normal request and directly create an instance after passing the check.
     *
     * > **NOTE:** This parameter only applies during resource creation, update or deletion. If modified in isolation without other property changes, Terraform will not trigger any action.
     */
    declare public readonly dryRun: pulumi.Output<boolean | undefined>;
    /**
     * The ID of the file system.
     */
    declare public readonly fileSystemId: pulumi.Output<string>;
    /**
     * Protocol Service ID
     */
    declare public /*out*/ readonly protocolServiceId: pulumi.Output<string>;
    /**
     * The specification of the protocol machine cluster.
     * - Value range: General、CL1、CL2
     * - Default value: General
     */
    declare public readonly protocolSpec: pulumi.Output<string>;
    /**
     * The throughput of the protocol service. Unit: MB/s.
     */
    declare public readonly protocolThroughput: pulumi.Output<number>;
    /**
     * The protocol type supported by the protocol service.
     *
     * Value range:
     * - NFS: Protocol Service supports NFS protocol access.
     */
    declare public readonly protocolType: pulumi.Output<string>;
    /**
     * Agreement service status.
     */
    declare public /*out*/ readonly status: pulumi.Output<string>;
    /**
     * The VpcId of the protocol service, which must be consistent with the VPC of the file system.
     */
    declare public readonly vpcId: pulumi.Output<string | undefined>;
    /**
     * The VSwitchId of the protocol service.
     */
    declare public readonly vswitchId: pulumi.Output<string | undefined>;

    /**
     * Create a ProtocolService resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: ProtocolServiceArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: ProtocolServiceArgs | ProtocolServiceState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as ProtocolServiceState | undefined;
            resourceInputs["createTime"] = state?.createTime;
            resourceInputs["description"] = state?.description;
            resourceInputs["dryRun"] = state?.dryRun;
            resourceInputs["fileSystemId"] = state?.fileSystemId;
            resourceInputs["protocolServiceId"] = state?.protocolServiceId;
            resourceInputs["protocolSpec"] = state?.protocolSpec;
            resourceInputs["protocolThroughput"] = state?.protocolThroughput;
            resourceInputs["protocolType"] = state?.protocolType;
            resourceInputs["status"] = state?.status;
            resourceInputs["vpcId"] = state?.vpcId;
            resourceInputs["vswitchId"] = state?.vswitchId;
        } else {
            const args = argsOrState as ProtocolServiceArgs | undefined;
            if (args?.fileSystemId === undefined && !opts.urn) {
                throw new Error("Missing required property 'fileSystemId'");
            }
            if (args?.protocolSpec === undefined && !opts.urn) {
                throw new Error("Missing required property 'protocolSpec'");
            }
            if (args?.protocolType === undefined && !opts.urn) {
                throw new Error("Missing required property 'protocolType'");
            }
            resourceInputs["description"] = args?.description;
            resourceInputs["dryRun"] = args?.dryRun;
            resourceInputs["fileSystemId"] = args?.fileSystemId;
            resourceInputs["protocolSpec"] = args?.protocolSpec;
            resourceInputs["protocolThroughput"] = args?.protocolThroughput;
            resourceInputs["protocolType"] = args?.protocolType;
            resourceInputs["vpcId"] = args?.vpcId;
            resourceInputs["vswitchId"] = args?.vswitchId;
            resourceInputs["createTime"] = undefined /*out*/;
            resourceInputs["protocolServiceId"] = undefined /*out*/;
            resourceInputs["status"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(ProtocolService.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering ProtocolService resources.
 */
export interface ProtocolServiceState {
    /**
     * The time when the protocol server service was created. The UTC time.
     */
    createTime?: pulumi.Input<string>;
    /**
     * Description of the agreement service.
     *
     * Limitations:
     * - Length is 2~128 English or Chinese characters.
     * - It must start with an uppercase or lowercase letter or Chinese, and cannot start with `http://` and `https://`.
     * - Can contain numbers, colons (:), underscores (_), or dashes (-).
     */
    description?: pulumi.Input<string>;
    /**
     * Whether to PreCheck the creation request.
     *
     * The pre-check operation helps you check the validity of parameters and dependency conditions, and does not actually create an instance, nor does it incur costs.
     *
     * Value:
     * - true: The check request is sent and the protocol service is not created. The check items include whether the required parameters, request format, and business restriction dependency conditions are filled in. If the check does not pass, the corresponding error is returned. If the check passes, the 200 HttpCode is returned, but the ProtocolServiceId is empty.
     * - false (default): Send a normal request and directly create an instance after passing the check.
     *
     * > **NOTE:** This parameter only applies during resource creation, update or deletion. If modified in isolation without other property changes, Terraform will not trigger any action.
     */
    dryRun?: pulumi.Input<boolean>;
    /**
     * The ID of the file system.
     */
    fileSystemId?: pulumi.Input<string>;
    /**
     * Protocol Service ID
     */
    protocolServiceId?: pulumi.Input<string>;
    /**
     * The specification of the protocol machine cluster.
     * - Value range: General、CL1、CL2
     * - Default value: General
     */
    protocolSpec?: pulumi.Input<string>;
    /**
     * The throughput of the protocol service. Unit: MB/s.
     */
    protocolThroughput?: pulumi.Input<number>;
    /**
     * The protocol type supported by the protocol service.
     *
     * Value range:
     * - NFS: Protocol Service supports NFS protocol access.
     */
    protocolType?: pulumi.Input<string>;
    /**
     * Agreement service status.
     */
    status?: pulumi.Input<string>;
    /**
     * The VpcId of the protocol service, which must be consistent with the VPC of the file system.
     */
    vpcId?: pulumi.Input<string>;
    /**
     * The VSwitchId of the protocol service.
     */
    vswitchId?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a ProtocolService resource.
 */
export interface ProtocolServiceArgs {
    /**
     * Description of the agreement service.
     *
     * Limitations:
     * - Length is 2~128 English or Chinese characters.
     * - It must start with an uppercase or lowercase letter or Chinese, and cannot start with `http://` and `https://`.
     * - Can contain numbers, colons (:), underscores (_), or dashes (-).
     */
    description?: pulumi.Input<string>;
    /**
     * Whether to PreCheck the creation request.
     *
     * The pre-check operation helps you check the validity of parameters and dependency conditions, and does not actually create an instance, nor does it incur costs.
     *
     * Value:
     * - true: The check request is sent and the protocol service is not created. The check items include whether the required parameters, request format, and business restriction dependency conditions are filled in. If the check does not pass, the corresponding error is returned. If the check passes, the 200 HttpCode is returned, but the ProtocolServiceId is empty.
     * - false (default): Send a normal request and directly create an instance after passing the check.
     *
     * > **NOTE:** This parameter only applies during resource creation, update or deletion. If modified in isolation without other property changes, Terraform will not trigger any action.
     */
    dryRun?: pulumi.Input<boolean>;
    /**
     * The ID of the file system.
     */
    fileSystemId: pulumi.Input<string>;
    /**
     * The specification of the protocol machine cluster.
     * - Value range: General、CL1、CL2
     * - Default value: General
     */
    protocolSpec: pulumi.Input<string>;
    /**
     * The throughput of the protocol service. Unit: MB/s.
     */
    protocolThroughput?: pulumi.Input<number>;
    /**
     * The protocol type supported by the protocol service.
     *
     * Value range:
     * - NFS: Protocol Service supports NFS protocol access.
     */
    protocolType: pulumi.Input<string>;
    /**
     * The VpcId of the protocol service, which must be consistent with the VPC of the file system.
     */
    vpcId?: pulumi.Input<string>;
    /**
     * The VSwitchId of the protocol service.
     */
    vswitchId?: pulumi.Input<string>;
}
