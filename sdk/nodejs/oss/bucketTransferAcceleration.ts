// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provides a OSS Bucket Transfer Acceleration resource. Transfer acceleration configuration of a bucket.
 *
 * For information about OSS Bucket Transfer Acceleration and how to use it, see [What is Bucket Transfer Acceleration](https://www.alibabacloud.com/help/en/oss/developer-reference/putbuckettransferacceleration).
 *
 * > **NOTE:** Available since v1.224.0.
 *
 * ## Example Usage
 *
 * Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 * import * as random from "@pulumi/random";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const _default = new random.index.Integer("default", {
 *     min: 10000,
 *     max: 99999,
 * });
 * const createBucket = new alicloud.oss.Bucket("CreateBucket", {
 *     storageClass: "Standard",
 *     bucket: `${name}-${_default.result}`,
 * });
 * const defaultBucketTransferAcceleration = new alicloud.oss.BucketTransferAcceleration("default", {
 *     bucket: createBucket.bucket,
 *     enabled: true,
 * });
 * ```
 *
 * ### Deleting `alicloud.oss.BucketTransferAcceleration` or removing it from your configuration
 *
 * Terraform cannot destroy resource `alicloud.oss.BucketTransferAcceleration`. Terraform will remove this resource from the state file, however resources may remain.
 *
 * 📚 Need more examples? VIEW MORE EXAMPLES
 *
 * ## Import
 *
 * OSS Bucket Transfer Acceleration can be imported using the id, e.g.
 *
 * ```sh
 * $ pulumi import alicloud:oss/bucketTransferAcceleration:BucketTransferAcceleration example <id>
 * ```
 */
export class BucketTransferAcceleration extends pulumi.CustomResource {
    /**
     * Get an existing BucketTransferAcceleration resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: BucketTransferAccelerationState, opts?: pulumi.CustomResourceOptions): BucketTransferAcceleration {
        return new BucketTransferAcceleration(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'alicloud:oss/bucketTransferAcceleration:BucketTransferAcceleration';

    /**
     * Returns true if the given object is an instance of BucketTransferAcceleration.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is BucketTransferAcceleration {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === BucketTransferAcceleration.__pulumiType;
    }

    /**
     * The name of the Bucket.
     */
    declare public readonly bucket: pulumi.Output<string>;
    /**
     * Specifies whether to enable transfer acceleration for the bucket. Valid values: true: transfer acceleration for the bucket is enabled. false: transfer acceleration for the bucket is disabled.
     */
    declare public readonly enabled: pulumi.Output<boolean | undefined>;

    /**
     * Create a BucketTransferAcceleration resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: BucketTransferAccelerationArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: BucketTransferAccelerationArgs | BucketTransferAccelerationState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as BucketTransferAccelerationState | undefined;
            resourceInputs["bucket"] = state?.bucket;
            resourceInputs["enabled"] = state?.enabled;
        } else {
            const args = argsOrState as BucketTransferAccelerationArgs | undefined;
            if (args?.bucket === undefined && !opts.urn) {
                throw new Error("Missing required property 'bucket'");
            }
            resourceInputs["bucket"] = args?.bucket;
            resourceInputs["enabled"] = args?.enabled;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(BucketTransferAcceleration.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering BucketTransferAcceleration resources.
 */
export interface BucketTransferAccelerationState {
    /**
     * The name of the Bucket.
     */
    bucket?: pulumi.Input<string>;
    /**
     * Specifies whether to enable transfer acceleration for the bucket. Valid values: true: transfer acceleration for the bucket is enabled. false: transfer acceleration for the bucket is disabled.
     */
    enabled?: pulumi.Input<boolean>;
}

/**
 * The set of arguments for constructing a BucketTransferAcceleration resource.
 */
export interface BucketTransferAccelerationArgs {
    /**
     * The name of the Bucket.
     */
    bucket: pulumi.Input<string>;
    /**
     * Specifies whether to enable transfer acceleration for the bucket. Valid values: true: transfer acceleration for the bucket is enabled. false: transfer acceleration for the bucket is disabled.
     */
    enabled?: pulumi.Input<boolean>;
}
