// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * This data source provides Oss Bucket Inventory available to the user.[What is Bucket Inventory](https://next.api.alibabacloud.com/document/Oss/2019-05-17/PutBucketInventory)
 *
 * > **NOTE:** Available since v1.284.0.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const createBucket = new alicloud.oss.Bucket("CreateBucket", {storageClass: "Standard"});
 * const defaultBucketInventory = new alicloud.oss.BucketInventory("default", {
 *     destination: {},
 *     optionalFields: {
 *         fields: [
 *             "Size",
 *             "LastModifiedDate",
 *             "ETag",
 *         ],
 *     },
 *     bucket: createBucket.id,
 *     filter: {
 *         prefix: "Pics/",
 *         lowerSizeBound: 256,
 *         upperSizeBound: 999999,
 *         storageClass: "Standard",
 *     },
 *     includedObjectVersions: "Current",
 *     schedule: {
 *         frequency: "Daily",
 *     },
 *     inventoryId: "report01",
 *     isEnabled: false,
 * });
 * const _default = alicloud.oss.getBucketInventoriesOutput({
 *     ids: [defaultBucketInventory.id],
 *     bucket: createBucket.id,
 * });
 * export const alicloudOssBucketInventoryExampleId = _default.apply(_default => _default.inventories?.[0]?.id);
 * ```
 */
export function getBucketInventories(args: GetBucketInventoriesArgs, opts?: pulumi.InvokeOptions): Promise<GetBucketInventoriesResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("alicloud:oss/getBucketInventories:getBucketInventories", {
        "bucket": args.bucket,
        "ids": args.ids,
        "outputFile": args.outputFile,
    }, opts);
}

/**
 * A collection of arguments for invoking getBucketInventories.
 */
export interface GetBucketInventoriesArgs {
    /**
     * The name of the bucket.
     */
    bucket: string;
    /**
     * A list of Bucket Inventory IDs. The value is formulated as `<bucket>:<inventory_id>`.
     */
    ids?: string[];
    /**
     * File name where to save data source results (after running `pulumi preview`).
     */
    outputFile?: string;
}

/**
 * A collection of values returned by getBucketInventories.
 */
export interface GetBucketInventoriesResult {
    /**
     * The Bucket where the exported manifest file is stored.
     */
    readonly bucket: string;
    /**
     * The provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
    /**
     * A list of Bucket Inventory IDs.
     */
    readonly ids: string[];
    /**
     * A list of Bucket Inventory Entries. Each element contains the following attributes:
     */
    readonly inventories: outputs.oss.GetBucketInventoriesInventory[];
    readonly outputFile?: string;
}
/**
 * This data source provides Oss Bucket Inventory available to the user.[What is Bucket Inventory](https://next.api.alibabacloud.com/document/Oss/2019-05-17/PutBucketInventory)
 *
 * > **NOTE:** Available since v1.284.0.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const createBucket = new alicloud.oss.Bucket("CreateBucket", {storageClass: "Standard"});
 * const defaultBucketInventory = new alicloud.oss.BucketInventory("default", {
 *     destination: {},
 *     optionalFields: {
 *         fields: [
 *             "Size",
 *             "LastModifiedDate",
 *             "ETag",
 *         ],
 *     },
 *     bucket: createBucket.id,
 *     filter: {
 *         prefix: "Pics/",
 *         lowerSizeBound: 256,
 *         upperSizeBound: 999999,
 *         storageClass: "Standard",
 *     },
 *     includedObjectVersions: "Current",
 *     schedule: {
 *         frequency: "Daily",
 *     },
 *     inventoryId: "report01",
 *     isEnabled: false,
 * });
 * const _default = alicloud.oss.getBucketInventoriesOutput({
 *     ids: [defaultBucketInventory.id],
 *     bucket: createBucket.id,
 * });
 * export const alicloudOssBucketInventoryExampleId = _default.apply(_default => _default.inventories?.[0]?.id);
 * ```
 */
export function getBucketInventoriesOutput(args: GetBucketInventoriesOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetBucketInventoriesResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("alicloud:oss/getBucketInventories:getBucketInventories", {
        "bucket": args.bucket,
        "ids": args.ids,
        "outputFile": args.outputFile,
    }, opts);
}

/**
 * A collection of arguments for invoking getBucketInventories.
 */
export interface GetBucketInventoriesOutputArgs {
    /**
     * The name of the bucket.
     */
    bucket: pulumi.Input<string>;
    /**
     * A list of Bucket Inventory IDs. The value is formulated as `<bucket>:<inventory_id>`.
     */
    ids?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * File name where to save data source results (after running `pulumi preview`).
     */
    outputFile?: pulumi.Input<string | undefined>;
}
