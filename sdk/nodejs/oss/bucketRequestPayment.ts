// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provides a OSS Bucket Request Payment resource. Whether to enable pay-by-requester for a bucket.
 *
 * For information about OSS Bucket Request Payment and how to use it, see [What is Bucket Request Payment](https://www.alibabacloud.com/help/en/oss/developer-reference/putbucketrequestpayment).
 *
 * > **NOTE:** Available since v1.222.0.
 *
 * ## Example Usage
 *
 * Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 * import * as random from "@pulumi/random";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const _default = new random.index.Integer("default", {
 *     min: 10000,
 *     max: 99999,
 * });
 * const createBucket = new alicloud.oss.Bucket("CreateBucket", {
 *     storageClass: "Standard",
 *     bucket: `${name}-${_default.result}`,
 * });
 * const defaultBucketRequestPayment = new alicloud.oss.BucketRequestPayment("default", {
 *     payer: "Requester",
 *     bucket: createBucket.bucket,
 * });
 * ```
 *
 * ### Deleting `alicloud.oss.BucketRequestPayment` or removing it from your configuration
 *
 * Terraform cannot destroy resource `alicloud.oss.BucketRequestPayment`. Terraform will remove this resource from the state file, however resources may remain.
 *
 * 📚 Need more examples? VIEW MORE EXAMPLES
 *
 * ## Import
 *
 * OSS Bucket Request Payment can be imported using the id, e.g.
 *
 * ```sh
 * $ pulumi import alicloud:oss/bucketRequestPayment:BucketRequestPayment example <id>
 * ```
 */
export class BucketRequestPayment extends pulumi.CustomResource {
    /**
     * Get an existing BucketRequestPayment resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: BucketRequestPaymentState, opts?: pulumi.CustomResourceOptions): BucketRequestPayment {
        return new BucketRequestPayment(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'alicloud:oss/bucketRequestPayment:BucketRequestPayment';

    /**
     * Returns true if the given object is an instance of BucketRequestPayment.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is BucketRequestPayment {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === BucketRequestPayment.__pulumiType;
    }

    /**
     * The name of the bucket.
     */
    declare public readonly bucket: pulumi.Output<string>;
    /**
     * The payer of the request and traffic fees.Valid values: BucketOwner: request and traffic fees are paid by the bucket owner. Requester: request and traffic fees are paid by the requester.
     */
    declare public readonly payer: pulumi.Output<string>;

    /**
     * Create a BucketRequestPayment resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: BucketRequestPaymentArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: BucketRequestPaymentArgs | BucketRequestPaymentState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as BucketRequestPaymentState | undefined;
            resourceInputs["bucket"] = state?.bucket;
            resourceInputs["payer"] = state?.payer;
        } else {
            const args = argsOrState as BucketRequestPaymentArgs | undefined;
            if (args?.bucket === undefined && !opts.urn) {
                throw new Error("Missing required property 'bucket'");
            }
            resourceInputs["bucket"] = args?.bucket;
            resourceInputs["payer"] = args?.payer;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(BucketRequestPayment.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering BucketRequestPayment resources.
 */
export interface BucketRequestPaymentState {
    /**
     * The name of the bucket.
     */
    bucket?: pulumi.Input<string>;
    /**
     * The payer of the request and traffic fees.Valid values: BucketOwner: request and traffic fees are paid by the bucket owner. Requester: request and traffic fees are paid by the requester.
     */
    payer?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a BucketRequestPayment resource.
 */
export interface BucketRequestPaymentArgs {
    /**
     * The name of the bucket.
     */
    bucket: pulumi.Input<string>;
    /**
     * The payer of the request and traffic fees.Valid values: BucketOwner: request and traffic fees are paid by the bucket owner. Requester: request and traffic fees are paid by the requester.
     */
    payer?: pulumi.Input<string>;
}
