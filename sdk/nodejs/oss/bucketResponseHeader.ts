// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * Provides a OSS Bucket Response Header resource.
 *
 * Response header configuration of a bucket.
 *
 * For information about OSS Bucket Response Header and how to use it, see [What is Bucket Response Header](https://next.api.alibabacloud.com/document/Oss/2019-05-17/PutBucketResponseHeader).
 *
 * > **NOTE:** Available since v1.271.0.
 *
 * ## Example Usage
 *
 * Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const defaultrdrM3m = new alicloud.oss.Bucket("defaultrdrM3m", {storageClass: "Standard"});
 * const _default = new alicloud.oss.BucketOverwriteConfig("default", {
 *     bucket: defaultrdrM3m.id,
 *     rules: [
 *         {
 *             id: "rule1",
 *             action: "forbid",
 *             prefix: "rule1-prefix/",
 *             suffix: "rule1-suffix/",
 *             principals: {
 *                 principals: [
 *                     "a",
 *                     "b",
 *                     "c",
 *                 ],
 *             },
 *         },
 *         {
 *             id: "rule2",
 *             action: "forbid",
 *             prefix: "rule2-prefix/",
 *             suffix: "rule2-suffix/",
 *             principals: {
 *                 principals: [
 *                     "d",
 *                     "e",
 *                     "f",
 *                 ],
 *             },
 *         },
 *         {
 *             id: "rule3",
 *             action: "forbid",
 *             prefix: "rule3-prefix/",
 *             suffix: "rule3-suffix/",
 *             principals: {
 *                 principals: [
 *                     "1",
 *                     "2",
 *                     "3",
 *                 ],
 *             },
 *         },
 *     ],
 * });
 * ```
 *
 * 📚 Need more examples? VIEW MORE EXAMPLES
 *
 * ## Import
 *
 * OSS Bucket Response Header can be imported using the id, e.g.
 *
 * ```sh
 * $ pulumi import alicloud:oss/bucketResponseHeader:BucketResponseHeader example <bucket>
 * ```
 */
export class BucketResponseHeader extends pulumi.CustomResource {
    /**
     * Get an existing BucketResponseHeader resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: BucketResponseHeaderState, opts?: pulumi.CustomResourceOptions): BucketResponseHeader {
        return new BucketResponseHeader(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'alicloud:oss/bucketResponseHeader:BucketResponseHeader';

    /**
     * Returns true if the given object is an instance of BucketResponseHeader.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is BucketResponseHeader {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === BucketResponseHeader.__pulumiType;
    }

    /**
     * The name of the bucket
     */
    declare public readonly bucket: pulumi.Output<string>;
    /**
     * The container that holds the response header rules. See `rule` below.
     */
    declare public readonly rules: pulumi.Output<outputs.oss.BucketResponseHeaderRule[] | undefined>;

    /**
     * Create a BucketResponseHeader resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: BucketResponseHeaderArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: BucketResponseHeaderArgs | BucketResponseHeaderState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as BucketResponseHeaderState | undefined;
            resourceInputs["bucket"] = state?.bucket;
            resourceInputs["rules"] = state?.rules;
        } else {
            const args = argsOrState as BucketResponseHeaderArgs | undefined;
            if (args?.bucket === undefined && !opts.urn) {
                throw new Error("Missing required property 'bucket'");
            }
            resourceInputs["bucket"] = args?.bucket;
            resourceInputs["rules"] = args?.rules;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(BucketResponseHeader.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering BucketResponseHeader resources.
 */
export interface BucketResponseHeaderState {
    /**
     * The name of the bucket
     */
    bucket?: pulumi.Input<string>;
    /**
     * The container that holds the response header rules. See `rule` below.
     */
    rules?: pulumi.Input<pulumi.Input<inputs.oss.BucketResponseHeaderRule>[]>;
}

/**
 * The set of arguments for constructing a BucketResponseHeader resource.
 */
export interface BucketResponseHeaderArgs {
    /**
     * The name of the bucket
     */
    bucket: pulumi.Input<string>;
    /**
     * The container that holds the response header rules. See `rule` below.
     */
    rules?: pulumi.Input<pulumi.Input<inputs.oss.BucketResponseHeaderRule>[]>;
}
