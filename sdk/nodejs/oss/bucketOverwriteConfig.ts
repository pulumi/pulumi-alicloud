// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * Provides a OSS Bucket Overwrite Config resource.
 *
 * Bucket Overwrite Configuration.
 *
 * For information about OSS Bucket Overwrite Config and how to use it, see [What is Bucket Overwrite Config](https://next.api.alibabacloud.com/document/Oss/2019-05-17/PutBucketOverwriteConfig).
 *
 * > **NOTE:** Available since v1.271.0.
 *
 * ## Example Usage
 *
 * Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const defaultrdrM3m = new alicloud.oss.Bucket("defaultrdrM3m", {storageClass: "Standard"});
 * const _default = new alicloud.oss.BucketOverwriteConfig("default", {
 *     bucket: defaultrdrM3m.id,
 *     rules: [
 *         {
 *             id: "rule1",
 *             action: "forbid",
 *             prefix: "rule1-prefix/",
 *             suffix: "rule1-suffix/",
 *             principals: {
 *                 principals: [
 *                     "a",
 *                     "b",
 *                     "c",
 *                 ],
 *             },
 *         },
 *         {
 *             id: "rule2",
 *             action: "forbid",
 *             prefix: "rule2-prefix/",
 *             suffix: "rule2-suffix/",
 *             principals: {
 *                 principals: [
 *                     "d",
 *                     "e",
 *                     "f",
 *                 ],
 *             },
 *         },
 *         {
 *             id: "rule3",
 *             action: "forbid",
 *             prefix: "rule3-prefix/",
 *             suffix: "rule3-suffix/",
 *             principals: {
 *                 principals: [
 *                     "1",
 *                     "2",
 *                     "3",
 *                 ],
 *             },
 *         },
 *     ],
 * });
 * ```
 *
 * 📚 Need more examples? VIEW MORE EXAMPLES
 *
 * ## Import
 *
 * OSS Bucket Overwrite Config can be imported using the id, e.g.
 *
 * ```sh
 * $ pulumi import alicloud:oss/bucketOverwriteConfig:BucketOverwriteConfig example <bucket>
 * ```
 */
export class BucketOverwriteConfig extends pulumi.CustomResource {
    /**
     * Get an existing BucketOverwriteConfig resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: BucketOverwriteConfigState, opts?: pulumi.CustomResourceOptions): BucketOverwriteConfig {
        return new BucketOverwriteConfig(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'alicloud:oss/bucketOverwriteConfig:BucketOverwriteConfig';

    /**
     * Returns true if the given object is an instance of BucketOverwriteConfig.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is BucketOverwriteConfig {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === BucketOverwriteConfig.__pulumiType;
    }

    /**
     * The name of the bucket
     */
    declare public readonly bucket: pulumi.Output<string>;
    /**
     * Forbid overwrite rule See `rule` below.
     */
    declare public readonly rules: pulumi.Output<outputs.oss.BucketOverwriteConfigRule[] | undefined>;

    /**
     * Create a BucketOverwriteConfig resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: BucketOverwriteConfigArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: BucketOverwriteConfigArgs | BucketOverwriteConfigState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as BucketOverwriteConfigState | undefined;
            resourceInputs["bucket"] = state?.bucket;
            resourceInputs["rules"] = state?.rules;
        } else {
            const args = argsOrState as BucketOverwriteConfigArgs | undefined;
            if (args?.bucket === undefined && !opts.urn) {
                throw new Error("Missing required property 'bucket'");
            }
            resourceInputs["bucket"] = args?.bucket;
            resourceInputs["rules"] = args?.rules;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(BucketOverwriteConfig.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering BucketOverwriteConfig resources.
 */
export interface BucketOverwriteConfigState {
    /**
     * The name of the bucket
     */
    bucket?: pulumi.Input<string>;
    /**
     * Forbid overwrite rule See `rule` below.
     */
    rules?: pulumi.Input<pulumi.Input<inputs.oss.BucketOverwriteConfigRule>[]>;
}

/**
 * The set of arguments for constructing a BucketOverwriteConfig resource.
 */
export interface BucketOverwriteConfigArgs {
    /**
     * The name of the bucket
     */
    bucket: pulumi.Input<string>;
    /**
     * Forbid overwrite rule See `rule` below.
     */
    rules?: pulumi.Input<pulumi.Input<inputs.oss.BucketOverwriteConfigRule>[]>;
}
