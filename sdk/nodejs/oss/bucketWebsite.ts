// *** WARNING: this file was generated by the Pulumi Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * Provides a OSS Bucket Website resource.
 *
 * the static website configuration and mirror configuration of the bucket.
 *
 * For information about OSS Bucket Website and how to use it, see [What is Bucket Website](https://www.alibabacloud.com/help/en/oss/developer-reference/putbucketwebsite).
 *
 * > **NOTE:** Available since v1.237.0.
 *
 * ## Example Usage
 *
 * Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 * import * as random from "@pulumi/random";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const _default = new random.index.Uuid("default", {});
 * const defaultnVj9x3 = new alicloud.oss.Bucket("defaultnVj9x3", {
 *     bucket: `${name}-${_default.result}`,
 *     storageClass: "Standard",
 * });
 * const defaultBucketWebsite = new alicloud.oss.BucketWebsite("default", {
 *     indexDocument: {
 *         suffix: "index.html",
 *         supportSubDir: true,
 *         type: "0",
 *     },
 *     errorDocument: {
 *         key: "error.html",
 *         httpStatus: 404,
 *     },
 *     bucket: defaultnVj9x3.bucket,
 *     routingRules: {
 *         routingRules: [{
 *             ruleNumber: 1,
 *             condition: {
 *                 httpErrorCodeReturnedEquals: "404",
 *             },
 *             redirect: {
 *                 protocol: "https",
 *                 httpRedirectCode: "305",
 *                 redirectType: "AliCDN",
 *                 hostName: "www.alicdn-master.com",
 *             },
 *             luaConfig: {
 *                 script: "example.lua",
 *             },
 *         }],
 *     },
 * });
 * ```
 *
 * ## Import
 *
 * OSS Bucket Website can be imported using the id, e.g.
 *
 * ```sh
 * $ pulumi import alicloud:oss/bucketWebsite:BucketWebsite example <id>
 * ```
 */
export class BucketWebsite extends pulumi.CustomResource {
    /**
     * Get an existing BucketWebsite resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: BucketWebsiteState, opts?: pulumi.CustomResourceOptions): BucketWebsite {
        return new BucketWebsite(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'alicloud:oss/bucketWebsite:BucketWebsite';

    /**
     * Returns true if the given object is an instance of BucketWebsite.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is BucketWebsite {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === BucketWebsite.__pulumiType;
    }

    /**
     * The name of the bucket
     */
    public readonly bucket!: pulumi.Output<string>;
    /**
     * The container that holds the error page configuration information. See `errorDocument` below.
     */
    public readonly errorDocument!: pulumi.Output<outputs.oss.BucketWebsiteErrorDocument | undefined>;
    /**
     * Static Website Default Home Page Configuration See `indexDocument` below.
     */
    public readonly indexDocument!: pulumi.Output<outputs.oss.BucketWebsiteIndexDocument | undefined>;
    /**
     * The container that holds the jump rule or the mirroring back-to-origin rule. See `routingRules` below.
     */
    public readonly routingRules!: pulumi.Output<outputs.oss.BucketWebsiteRoutingRules | undefined>;

    /**
     * Create a BucketWebsite resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: BucketWebsiteArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: BucketWebsiteArgs | BucketWebsiteState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as BucketWebsiteState | undefined;
            resourceInputs["bucket"] = state ? state.bucket : undefined;
            resourceInputs["errorDocument"] = state ? state.errorDocument : undefined;
            resourceInputs["indexDocument"] = state ? state.indexDocument : undefined;
            resourceInputs["routingRules"] = state ? state.routingRules : undefined;
        } else {
            const args = argsOrState as BucketWebsiteArgs | undefined;
            if ((!args || args.bucket === undefined) && !opts.urn) {
                throw new Error("Missing required property 'bucket'");
            }
            resourceInputs["bucket"] = args ? args.bucket : undefined;
            resourceInputs["errorDocument"] = args ? args.errorDocument : undefined;
            resourceInputs["indexDocument"] = args ? args.indexDocument : undefined;
            resourceInputs["routingRules"] = args ? args.routingRules : undefined;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(BucketWebsite.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering BucketWebsite resources.
 */
export interface BucketWebsiteState {
    /**
     * The name of the bucket
     */
    bucket?: pulumi.Input<string>;
    /**
     * The container that holds the error page configuration information. See `errorDocument` below.
     */
    errorDocument?: pulumi.Input<inputs.oss.BucketWebsiteErrorDocument>;
    /**
     * Static Website Default Home Page Configuration See `indexDocument` below.
     */
    indexDocument?: pulumi.Input<inputs.oss.BucketWebsiteIndexDocument>;
    /**
     * The container that holds the jump rule or the mirroring back-to-origin rule. See `routingRules` below.
     */
    routingRules?: pulumi.Input<inputs.oss.BucketWebsiteRoutingRules>;
}

/**
 * The set of arguments for constructing a BucketWebsite resource.
 */
export interface BucketWebsiteArgs {
    /**
     * The name of the bucket
     */
    bucket: pulumi.Input<string>;
    /**
     * The container that holds the error page configuration information. See `errorDocument` below.
     */
    errorDocument?: pulumi.Input<inputs.oss.BucketWebsiteErrorDocument>;
    /**
     * Static Website Default Home Page Configuration See `indexDocument` below.
     */
    indexDocument?: pulumi.Input<inputs.oss.BucketWebsiteIndexDocument>;
    /**
     * The container that holds the jump rule or the mirroring back-to-origin rule. See `routingRules` below.
     */
    routingRules?: pulumi.Input<inputs.oss.BucketWebsiteRoutingRules>;
}
