// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provides a RabbitMQ (AMQP) Open Source Account resource.
 *
 * An account under the open-source authentication and permission management system.  .
 *
 * For information about RabbitMQ (AMQP) Open Source Account and how to use it, see [What is Open Source Account](https://next.api.alibabacloud.com/document/amqp-open/2019-12-12/CreateOpenSourceAccount).
 *
 * > **NOTE:** Available since v1.280.0.
 *
 * ## Example Usage
 *
 * Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const instanceName = config.get("instanceName") || "example开源鉴权实例";
 * const userName = config.get("userName") || "Suhao123_";
 * const userNameUpdate = config.get("userNameUpdate") || "Suhao456_";
 * const _default = new alicloud.vpc.Network("default", {
 *     vpcName: name,
 *     cidrBlock: "172.16.0.0/16",
 * });
 * const defaultB = new alicloud.vpc.Switch("default_b", {
 *     vswitchName: `${name}-b`,
 *     cidrBlock: "172.16.0.0/24",
 *     vpcId: _default.id,
 *     zoneId: "cn-hangzhou-b",
 * });
 * const defaultG = new alicloud.vpc.Switch("default_g", {
 *     vswitchName: `${name}-g`,
 *     cidrBlock: "172.16.1.0/24",
 *     vpcId: _default.id,
 *     zoneId: "cn-hangzhou-g",
 * });
 * const defaultSecurityGroup = new alicloud.ecs.SecurityGroup("default", {
 *     securityGroupName: name,
 *     vpcId: _default.id,
 * });
 * const createInstance = new alicloud.amqp.Instance("CreateInstance", {
 *     renewalDuration: 1,
 *     maxTps: "3000",
 *     periodCycle: "Month",
 *     maxConnections: 2000,
 *     supportEip: true,
 *     autoRenew: false,
 *     renewalStatus: "AutoRenewal",
 *     period: 12,
 *     instanceName: instanceName,
 *     supportTracing: false,
 *     paymentType: "Subscription",
 *     renewalDurationUnit: "Month",
 *     instanceType: "enterprise",
 *     queueCapacity: "200",
 *     maxEipTps: "128",
 *     vpcId: _default.id,
 *     vswitchIds: [
 *         defaultB.id,
 *         defaultG.id,
 *     ],
 *     securityGroupId: defaultSecurityGroup.id,
 * });
 * const defaultOpenSourceAccount = new alicloud.amqp.OpenSourceAccount("default", {
 *     userName: userName,
 *     description: userName,
 *     password: userName,
 *     instanceId: createInstance.id,
 * });
 * ```
 *
 * 📚 Need more examples? VIEW MORE EXAMPLES
 *
 * ## Import
 *
 * RabbitMQ (AMQP) Open Source Account can be imported using the id, e.g.
 *
 * ```sh
 * $ pulumi import alicloud:amqp/openSourceAccount:OpenSourceAccount example <user_name>:<instance_id>
 * ```
 */
export class OpenSourceAccount extends pulumi.CustomResource {
    /**
     * Get an existing OpenSourceAccount resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: OpenSourceAccountState, opts?: pulumi.CustomResourceOptions): OpenSourceAccount {
        return new OpenSourceAccount(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'alicloud:amqp/openSourceAccount:OpenSourceAccount';

    /**
     * Returns true if the given object is an instance of OpenSourceAccount.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is OpenSourceAccount {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === OpenSourceAccount.__pulumiType;
    }

    /**
     * Description
     */
    declare public readonly description: pulumi.Output<string | undefined>;
    /**
     * Instance ID
     */
    declare public readonly instanceId: pulumi.Output<string>;
    /**
     * User password
     */
    declare public readonly password: pulumi.Output<string>;
    /**
     * User name
     */
    declare public readonly userName: pulumi.Output<string>;

    /**
     * Create a OpenSourceAccount resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: OpenSourceAccountArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: OpenSourceAccountArgs | OpenSourceAccountState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as OpenSourceAccountState | undefined;
            resourceInputs["description"] = state?.description;
            resourceInputs["instanceId"] = state?.instanceId;
            resourceInputs["password"] = state?.password;
            resourceInputs["userName"] = state?.userName;
        } else {
            const args = argsOrState as OpenSourceAccountArgs | undefined;
            if (args?.instanceId === undefined && !opts.urn) {
                throw new Error("Missing required property 'instanceId'");
            }
            if (args?.password === undefined && !opts.urn) {
                throw new Error("Missing required property 'password'");
            }
            if (args?.userName === undefined && !opts.urn) {
                throw new Error("Missing required property 'userName'");
            }
            resourceInputs["description"] = args?.description;
            resourceInputs["instanceId"] = args?.instanceId;
            resourceInputs["password"] = args?.password ? pulumi.secret(args.password) : undefined;
            resourceInputs["userName"] = args?.userName;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        const secretOpts = { additionalSecretOutputs: ["password"] };
        opts = pulumi.mergeOptions(opts, secretOpts);
        super(OpenSourceAccount.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering OpenSourceAccount resources.
 */
export interface OpenSourceAccountState {
    /**
     * Description
     */
    description?: pulumi.Input<string | undefined>;
    /**
     * Instance ID
     */
    instanceId?: pulumi.Input<string | undefined>;
    /**
     * User password
     */
    password?: pulumi.Input<string | undefined>;
    /**
     * User name
     */
    userName?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a OpenSourceAccount resource.
 */
export interface OpenSourceAccountArgs {
    /**
     * Description
     */
    description?: pulumi.Input<string | undefined>;
    /**
     * Instance ID
     */
    instanceId: pulumi.Input<string>;
    /**
     * User password
     */
    password: pulumi.Input<string>;
    /**
     * User name
     */
    userName: pulumi.Input<string>;
}
