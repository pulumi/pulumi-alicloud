// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * This data source provides RabbitMQ (AMQP) Open Source Permission available to the user.[What is Open Source Permission](https://next.api.alibabacloud.com/document/amqp-open/2019-12-12/CreateOpenSourcePermission)
 *
 * > **NOTE:** Available since v1.280.0.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const instanceName = config.get("instanceName") || "测试开源鉴权实例";
 * const vhost = config.get("vhost") || "/";
 * const userName = config.get("userName") || "Suhao123_WithPer";
 * const defaultNetwork = new alicloud.vpc.Network("default", {
 *     vpcName: name,
 *     cidrBlock: "172.16.0.0/16",
 * });
 * const defaultB = new alicloud.vpc.Switch("default_b", {
 *     vswitchName: `${name}-b`,
 *     cidrBlock: "172.16.0.0/24",
 *     vpcId: defaultNetwork.id,
 *     zoneId: "cn-hangzhou-b",
 * });
 * const defaultG = new alicloud.vpc.Switch("default_g", {
 *     vswitchName: `${name}-g`,
 *     cidrBlock: "172.16.1.0/24",
 *     vpcId: defaultNetwork.id,
 *     zoneId: "cn-hangzhou-g",
 * });
 * const defaultSecurityGroup = new alicloud.ecs.SecurityGroup("default", {
 *     securityGroupName: name,
 *     vpcId: defaultNetwork.id,
 * });
 * const createInstance = new alicloud.amqp.Instance("CreateInstance", {
 *     renewalDuration: 1,
 *     maxTps: "3000",
 *     periodCycle: "Month",
 *     maxConnections: 2000,
 *     supportEip: true,
 *     autoRenew: false,
 *     renewalStatus: "AutoRenewal",
 *     period: 12,
 *     instanceName: instanceName,
 *     supportTracing: false,
 *     paymentType: "Subscription",
 *     renewalDurationUnit: "Month",
 *     instanceType: "enterprise",
 *     queueCapacity: "200",
 *     maxEipTps: "128",
 *     vpcId: defaultNetwork.id,
 *     vswitchIds: [
 *         defaultB.id,
 *         defaultG.id,
 *     ],
 *     securityGroupId: defaultSecurityGroup.id,
 * });
 * const defaultOpenSourcePermission = new alicloud.amqp.OpenSourcePermission("default", {
 *     write: ".*",
 *     read: ".*",
 *     vhost: vhost,
 *     userName: userName,
 *     instanceId: createInstance.id,
 *     configure: ".*",
 * });
 * const _default = alicloud.amqp.getOpenSourcePermissionsOutput({
 *     ids: [defaultOpenSourcePermission.id],
 *     instanceId: createInstance.id,
 *     userName: userName,
 * });
 * export const alicloudAmqpOpenSourcePermissionExampleId = _default.apply(_default => _default.permissions?.[0]?.id);
 * ```
 */
export function getOpenSourcePermissions(args: GetOpenSourcePermissionsArgs, opts?: pulumi.InvokeOptions): Promise<GetOpenSourcePermissionsResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("alicloud:amqp/getOpenSourcePermissions:getOpenSourcePermissions", {
        "enableDetails": args.enableDetails,
        "ids": args.ids,
        "instanceId": args.instanceId,
        "outputFile": args.outputFile,
        "userName": args.userName,
    }, opts);
}

/**
 * A collection of arguments for invoking getOpenSourcePermissions.
 */
export interface GetOpenSourcePermissionsArgs {
    /**
     * Default to `false`. Set it to `true` to query detailed attributes for each permission.
     */
    enableDetails?: boolean;
    /**
     * A list of Open Source Permission IDs. The value is formulated as `<user_name>:<vhost>:<instance_id>`.
     */
    ids?: string[];
    /**
     * Instance ID.
     */
    instanceId: string;
    /**
     * File name where to save data source results (after running `pulumi preview`).
     */
    outputFile?: string;
    /**
     * Username.
     */
    userName: string;
}

/**
 * A collection of values returned by getOpenSourcePermissions.
 */
export interface GetOpenSourcePermissionsResult {
    readonly enableDetails?: boolean;
    /**
     * The provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
    /**
     * A list of Open Source Permission IDs.
     */
    readonly ids: string[];
    /**
     * Instance ID.
     */
    readonly instanceId: string;
    readonly outputFile?: string;
    /**
     * A list of Open Source Permission Entries. Each element contains the following attributes:
     */
    readonly permissions: outputs.amqp.GetOpenSourcePermissionsPermission[];
    /**
     * Username.
     */
    readonly userName: string;
}
/**
 * This data source provides RabbitMQ (AMQP) Open Source Permission available to the user.[What is Open Source Permission](https://next.api.alibabacloud.com/document/amqp-open/2019-12-12/CreateOpenSourcePermission)
 *
 * > **NOTE:** Available since v1.280.0.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const instanceName = config.get("instanceName") || "测试开源鉴权实例";
 * const vhost = config.get("vhost") || "/";
 * const userName = config.get("userName") || "Suhao123_WithPer";
 * const defaultNetwork = new alicloud.vpc.Network("default", {
 *     vpcName: name,
 *     cidrBlock: "172.16.0.0/16",
 * });
 * const defaultB = new alicloud.vpc.Switch("default_b", {
 *     vswitchName: `${name}-b`,
 *     cidrBlock: "172.16.0.0/24",
 *     vpcId: defaultNetwork.id,
 *     zoneId: "cn-hangzhou-b",
 * });
 * const defaultG = new alicloud.vpc.Switch("default_g", {
 *     vswitchName: `${name}-g`,
 *     cidrBlock: "172.16.1.0/24",
 *     vpcId: defaultNetwork.id,
 *     zoneId: "cn-hangzhou-g",
 * });
 * const defaultSecurityGroup = new alicloud.ecs.SecurityGroup("default", {
 *     securityGroupName: name,
 *     vpcId: defaultNetwork.id,
 * });
 * const createInstance = new alicloud.amqp.Instance("CreateInstance", {
 *     renewalDuration: 1,
 *     maxTps: "3000",
 *     periodCycle: "Month",
 *     maxConnections: 2000,
 *     supportEip: true,
 *     autoRenew: false,
 *     renewalStatus: "AutoRenewal",
 *     period: 12,
 *     instanceName: instanceName,
 *     supportTracing: false,
 *     paymentType: "Subscription",
 *     renewalDurationUnit: "Month",
 *     instanceType: "enterprise",
 *     queueCapacity: "200",
 *     maxEipTps: "128",
 *     vpcId: defaultNetwork.id,
 *     vswitchIds: [
 *         defaultB.id,
 *         defaultG.id,
 *     ],
 *     securityGroupId: defaultSecurityGroup.id,
 * });
 * const defaultOpenSourcePermission = new alicloud.amqp.OpenSourcePermission("default", {
 *     write: ".*",
 *     read: ".*",
 *     vhost: vhost,
 *     userName: userName,
 *     instanceId: createInstance.id,
 *     configure: ".*",
 * });
 * const _default = alicloud.amqp.getOpenSourcePermissionsOutput({
 *     ids: [defaultOpenSourcePermission.id],
 *     instanceId: createInstance.id,
 *     userName: userName,
 * });
 * export const alicloudAmqpOpenSourcePermissionExampleId = _default.apply(_default => _default.permissions?.[0]?.id);
 * ```
 */
export function getOpenSourcePermissionsOutput(args: GetOpenSourcePermissionsOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetOpenSourcePermissionsResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("alicloud:amqp/getOpenSourcePermissions:getOpenSourcePermissions", {
        "enableDetails": args.enableDetails,
        "ids": args.ids,
        "instanceId": args.instanceId,
        "outputFile": args.outputFile,
        "userName": args.userName,
    }, opts);
}

/**
 * A collection of arguments for invoking getOpenSourcePermissions.
 */
export interface GetOpenSourcePermissionsOutputArgs {
    /**
     * Default to `false`. Set it to `true` to query detailed attributes for each permission.
     */
    enableDetails?: pulumi.Input<boolean | undefined>;
    /**
     * A list of Open Source Permission IDs. The value is formulated as `<user_name>:<vhost>:<instance_id>`.
     */
    ids?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * Instance ID.
     */
    instanceId: pulumi.Input<string>;
    /**
     * File name where to save data source results (after running `pulumi preview`).
     */
    outputFile?: pulumi.Input<string | undefined>;
    /**
     * Username.
     */
    userName: pulumi.Input<string>;
}
