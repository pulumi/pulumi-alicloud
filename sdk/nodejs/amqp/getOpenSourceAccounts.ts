// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * This data source provides RabbitMQ (AMQP) Open Source Account available to the user.[What is Open Source Account](https://next.api.alibabacloud.com/document/amqp-open/2019-12-12/CreateOpenSourceAccount)
 *
 * > **NOTE:** Available since v1.280.0.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const instanceName = config.get("instanceName") || "测试开源鉴权实例";
 * const userName = config.get("userName") || "Suhao123_";
 * const userNameUpdate = config.get("userNameUpdate") || "Suhao456_";
 * const defaultNetwork = new alicloud.vpc.Network("default", {
 *     vpcName: name,
 *     cidrBlock: "172.16.0.0/16",
 * });
 * const defaultB = new alicloud.vpc.Switch("default_b", {
 *     vswitchName: `${name}-b`,
 *     cidrBlock: "172.16.0.0/24",
 *     vpcId: defaultNetwork.id,
 *     zoneId: "cn-hangzhou-b",
 * });
 * const defaultG = new alicloud.vpc.Switch("default_g", {
 *     vswitchName: `${name}-g`,
 *     cidrBlock: "172.16.1.0/24",
 *     vpcId: defaultNetwork.id,
 *     zoneId: "cn-hangzhou-g",
 * });
 * const defaultSecurityGroup = new alicloud.ecs.SecurityGroup("default", {
 *     securityGroupName: name,
 *     vpcId: defaultNetwork.id,
 * });
 * const createInstance = new alicloud.amqp.Instance("CreateInstance", {
 *     renewalDuration: 1,
 *     maxTps: "3000",
 *     periodCycle: "Month",
 *     maxConnections: 2000,
 *     supportEip: true,
 *     autoRenew: false,
 *     renewalStatus: "AutoRenewal",
 *     period: 12,
 *     instanceName: instanceName,
 *     supportTracing: false,
 *     paymentType: "Subscription",
 *     renewalDurationUnit: "Month",
 *     instanceType: "enterprise",
 *     queueCapacity: "200",
 *     maxEipTps: "128",
 *     vpcId: defaultNetwork.id,
 *     vswitchIds: [
 *         defaultB.id,
 *         defaultG.id,
 *     ],
 *     securityGroupId: defaultSecurityGroup.id,
 * });
 * const defaultOpenSourceAccount = new alicloud.amqp.OpenSourceAccount("default", {
 *     userName: userName,
 *     description: userName,
 *     password: userName,
 *     instanceId: createInstance.id,
 * });
 * const _default = alicloud.amqp.getOpenSourceAccountsOutput({
 *     ids: [defaultOpenSourceAccount.id],
 *     instanceId: createInstance.id,
 * });
 * export const alicloudAmqpOpenSourceAccountExampleId = _default.apply(_default => _default.accounts?.[0]?.id);
 * ```
 */
export function getOpenSourceAccounts(args: GetOpenSourceAccountsArgs, opts?: pulumi.InvokeOptions): Promise<GetOpenSourceAccountsResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("alicloud:amqp/getOpenSourceAccounts:getOpenSourceAccounts", {
        "ids": args.ids,
        "instanceId": args.instanceId,
        "outputFile": args.outputFile,
    }, opts);
}

/**
 * A collection of arguments for invoking getOpenSourceAccounts.
 */
export interface GetOpenSourceAccountsArgs {
    /**
     * A list of Open Source Account IDs. The value is formulated as `<user_name>:<instance_id>`.
     */
    ids?: string[];
    /**
     * Instance ID.
     */
    instanceId: string;
    /**
     * File name where to save data source results (after running `pulumi preview`).
     */
    outputFile?: string;
}

/**
 * A collection of values returned by getOpenSourceAccounts.
 */
export interface GetOpenSourceAccountsResult {
    /**
     * A list of Open Source Account Entries. Each element contains the following attributes:
     */
    readonly accounts: outputs.amqp.GetOpenSourceAccountsAccount[];
    /**
     * The provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
    /**
     * A list of Open Source Account IDs.
     */
    readonly ids: string[];
    /**
     * Instance ID.
     */
    readonly instanceId: string;
    readonly outputFile?: string;
}
/**
 * This data source provides RabbitMQ (AMQP) Open Source Account available to the user.[What is Open Source Account](https://next.api.alibabacloud.com/document/amqp-open/2019-12-12/CreateOpenSourceAccount)
 *
 * > **NOTE:** Available since v1.280.0.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const instanceName = config.get("instanceName") || "测试开源鉴权实例";
 * const userName = config.get("userName") || "Suhao123_";
 * const userNameUpdate = config.get("userNameUpdate") || "Suhao456_";
 * const defaultNetwork = new alicloud.vpc.Network("default", {
 *     vpcName: name,
 *     cidrBlock: "172.16.0.0/16",
 * });
 * const defaultB = new alicloud.vpc.Switch("default_b", {
 *     vswitchName: `${name}-b`,
 *     cidrBlock: "172.16.0.0/24",
 *     vpcId: defaultNetwork.id,
 *     zoneId: "cn-hangzhou-b",
 * });
 * const defaultG = new alicloud.vpc.Switch("default_g", {
 *     vswitchName: `${name}-g`,
 *     cidrBlock: "172.16.1.0/24",
 *     vpcId: defaultNetwork.id,
 *     zoneId: "cn-hangzhou-g",
 * });
 * const defaultSecurityGroup = new alicloud.ecs.SecurityGroup("default", {
 *     securityGroupName: name,
 *     vpcId: defaultNetwork.id,
 * });
 * const createInstance = new alicloud.amqp.Instance("CreateInstance", {
 *     renewalDuration: 1,
 *     maxTps: "3000",
 *     periodCycle: "Month",
 *     maxConnections: 2000,
 *     supportEip: true,
 *     autoRenew: false,
 *     renewalStatus: "AutoRenewal",
 *     period: 12,
 *     instanceName: instanceName,
 *     supportTracing: false,
 *     paymentType: "Subscription",
 *     renewalDurationUnit: "Month",
 *     instanceType: "enterprise",
 *     queueCapacity: "200",
 *     maxEipTps: "128",
 *     vpcId: defaultNetwork.id,
 *     vswitchIds: [
 *         defaultB.id,
 *         defaultG.id,
 *     ],
 *     securityGroupId: defaultSecurityGroup.id,
 * });
 * const defaultOpenSourceAccount = new alicloud.amqp.OpenSourceAccount("default", {
 *     userName: userName,
 *     description: userName,
 *     password: userName,
 *     instanceId: createInstance.id,
 * });
 * const _default = alicloud.amqp.getOpenSourceAccountsOutput({
 *     ids: [defaultOpenSourceAccount.id],
 *     instanceId: createInstance.id,
 * });
 * export const alicloudAmqpOpenSourceAccountExampleId = _default.apply(_default => _default.accounts?.[0]?.id);
 * ```
 */
export function getOpenSourceAccountsOutput(args: GetOpenSourceAccountsOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetOpenSourceAccountsResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("alicloud:amqp/getOpenSourceAccounts:getOpenSourceAccounts", {
        "ids": args.ids,
        "instanceId": args.instanceId,
        "outputFile": args.outputFile,
    }, opts);
}

/**
 * A collection of arguments for invoking getOpenSourceAccounts.
 */
export interface GetOpenSourceAccountsOutputArgs {
    /**
     * A list of Open Source Account IDs. The value is formulated as `<user_name>:<instance_id>`.
     */
    ids?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * Instance ID.
     */
    instanceId: pulumi.Input<string>;
    /**
     * File name where to save data source results (after running `pulumi preview`).
     */
    outputFile?: pulumi.Input<string | undefined>;
}
