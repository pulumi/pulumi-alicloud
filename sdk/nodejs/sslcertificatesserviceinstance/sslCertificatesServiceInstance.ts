// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * Provides a Certificate Management Service (Original SSL Certificate) Instance resource.
 *
 * For information about Certificate Management Service (Original SSL Certificate) Instance and how to use it, see [What is Instance](https://next.api.alibabacloud.com/document/BssOpenApi/2017-12-14/CreateInstance).
 *
 * > **NOTE:** Available since v1.287.0.
 *
 * > **NOTE:** This resource manages the certificate **instance** — the purchased subscription slot that a certificate can later be issued into — and the certificate application details recorded on it. It does **not** submit the certificate application and does **not** issue a certificate. A newly created instance stays in the `inactive` state. Submitting the application against this instance is done with `alicloud.sslcertificatesservicecertificate.Apply`; publishing the domain validation records it reports, and waiting for the certificate authority to issue the certificate, are separate steps again.
 *
 * > **NOTE:** Creating this resource places a real prepaid subscription order through `BssOpenApi::CreateInstance` and is billed on creation. The available specifications and their prices differ per account; call [DescribePricingModule](https://next.api.alibabacloud.com/document/BssOpenApi/2017-12-14/DescribePricingModule) with `ProductCode` and `ProductType` set to `cas` to list what your account can order. Destroying the resource refunds the order when the instance is still protected, then deletes it.
 *
 * ## Example Usage
 *
 * Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const _default = new alicloud.sslcertificatesserviceinstance.SslCertificatesServiceInstance("default", {
 *     productType: "cas",
 *     period: 12,
 *     pricingCycle: 2,
 *     instanceName: name,
 *     parameters: [
 *         {
 *             code: "fullSpec",
 *             value: "ws.dv.f",
 *         },
 *         {
 *             code: "fullDomainCount",
 *             value: "1",
 *         },
 *     ],
 * });
 * ```
 *
 * 📚 Need more examples? VIEW MORE EXAMPLES
 *
 * ## Import
 *
 * Certificate Management Service (Original SSL Certificate) Instance can be imported using the id, e.g.
 *
 * ```sh
 * $ pulumi import alicloud:sslcertificatesserviceinstance/sslCertificatesServiceInstance:SslCertificatesServiceInstance example <instance_id>
 * ```
 */
export class SslCertificatesServiceInstance extends pulumi.CustomResource {
    /**
     * Get an existing SslCertificatesServiceInstance resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: SslCertificatesServiceInstanceState, opts?: pulumi.CustomResourceOptions): SslCertificatesServiceInstance {
        return new SslCertificatesServiceInstance(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'alicloud:sslcertificatesserviceinstance/sslCertificatesServiceInstance:SslCertificatesServiceInstance';

    /**
     * Returns true if the given object is an instance of SslCertificatesServiceInstance.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is SslCertificatesServiceInstance {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === SslCertificatesServiceInstance.__pulumiType;
    }

    /**
     * Specifies whether to enable managed renewal.
     * enable: Enabled
     * disable: Disabled
     *
     * > **NOTE:** When managed renewal is enabled, the certificate authority issues a **new certificate** for the instance instead of extending the existing one. The renewed certificate has a different certificate ID and certificate identifier, so any value read from this instance or referenced by downstream resources changes after a renewal. Make sure the resources that consume the certificate take their value from the certificate attributes rather than from a hardcoded ID.
     */
    declare public readonly autoReissue: pulumi.Output<string>;
    /**
     * Average waiting time for issuing a certificate of this specification, in seconds.
     */
    declare public /*out*/ readonly averageWaitingTime: pulumi.Output<string>;
    /**
     * The certificate brand.
     */
    declare public /*out*/ readonly brand: pulumi.Output<string>;
    /**
     * The type of the certificate.
     */
    declare public /*out*/ readonly certificateType: pulumi.Output<string>;
    /**
     * The city of the address recorded on the instance.
     */
    declare public /*out*/ readonly city: pulumi.Output<string>;
    /**
     * The company information ID. Required for OV and EV certificates — without it the certificate application cannot be submitted for this instance.
     *
     * > **NOTE:** The address recorded on the instance — `city` and `province` — comes from the company referenced by `companyId`, and from a server-side default when no company is referenced. Certificate authorities reject non-standard addresses, so the address is taken from the company record rather than accepted per instance; set it on `alicloud.sslcertificatesservice.Company` instead. Both attributes are read-only here.
     */
    declare public readonly companyId: pulumi.Output<string>;
    /**
     * The list of contact IDs. At least one contact ID is required, otherwise the certificate application cannot be submitted for this instance.
     */
    declare public readonly contactIdLists: pulumi.Output<number[] | undefined>;
    /**
     * The code of the country or region where the certificate organization is located. For example, CN indicates China and US indicates the United States. This field is required when a Certificate Signing Request (CSR) is generated for a DV certificate. The default value is CN.
     */
    declare public readonly countryCode: pulumi.Output<string>;
    /**
     * The CSR content. You can generate a CSR by using OpenSSL or Keytool. For more information, see [How to create a CSR file](https://help.aliyun.com/document_detail/42218.html).
     */
    declare public readonly csr: pulumi.Output<string>;
    /**
     * The domain names to be bound to the certificate. Specific requirements are as follows:
     * - You can specify a single domain name or a wildcard domain name (such as `*.aliyundoc.com`).
     * - You can specify multiple domain names. Separate multiple domain names with commas (,). Whether free domain names are provided is determined based on the first domain name.
     *
     * > **NOTE:** When you bind multiple domain names to a certificate, this parameter is required. This parameter and `csr` cannot both be empty.
     *
     * > **NOTE:** Which of `domain` and `csr` is authoritative depends on `generateCsrMethod`:
     *
     * | `generateCsrMethod` | Which value wins |
     * | --- | --- |
     * | `online` | The system generates the CSR and `domain` is used as you set it. Leave `csr` unset; the generated value is read back into state. |
     * | `upload` | The domain is taken from the `CN` field of the CSR you upload, so leave `domain` unset and let it be read back from the certificate. Setting it to a value that disagrees with the CSR is rejected by the API. |
     */
    declare public readonly domain: pulumi.Output<string>;
    /**
     * The number of single domain names included in the instance.
     */
    declare public /*out*/ readonly fullDomainCount: pulumi.Output<number>;
    /**
     * The method used to generate the Certificate Signing Request (CSR). Default value: online.
     * online: The system generates the CSR. In this case, the Csr parameter is ignored.
     * upload: You upload the CSR. In this case, the Csr parameter is required.
     */
    declare public readonly generateCsrMethod: pulumi.Output<string>;
    /**
     * Instance expiration time, a UNIX timestamp in seconds.
     */
    declare public /*out*/ readonly instanceEndTime: pulumi.Output<number>;
    /**
     * The name of the instance. When a certificate is issued, this value is used as the default name of the certificate.
     */
    declare public readonly instanceName: pulumi.Output<string>;
    /**
     * Instance start time, a UNIX timestamp in seconds.
     */
    declare public /*out*/ readonly instanceStartTime: pulumi.Output<number>;
    /**
     * The instance type.
     */
    declare public /*out*/ readonly instanceType: pulumi.Output<string>;
    /**
     * The certificate algorithm. Default value: RSA_2048.
     * - `RSA_2048`
     * - `RSA_3072`
     * - `RSA_4096`
     * - `ECC_256`
     * - `SM2`
     */
    declare public readonly keyAlgorithm: pulumi.Output<string>;
    /**
     * Order end time, a UNIX timestamp in seconds.
     */
    declare public /*out*/ readonly orderEndTime: pulumi.Output<number>;
    /**
     * Order start time, a UNIX timestamp in seconds.
     */
    declare public /*out*/ readonly orderStartTime: pulumi.Output<number>;
    /**
     * The list of modules. See `parameter` below.
     *
     * > **NOTE:** This parameter is immutable. Changing it after creation has no effect.
     */
    declare public readonly parameters: pulumi.Output<outputs.sslcertificatesserviceinstance.SslCertificatesServiceInstanceParameter[] | undefined>;
    /**
     * The subscription period. Unit: months. For products billed on a yearly basis, enter an integer multiple of 12.
     *
     * > **NOTE:** This parameter is immutable. Changing it after creation has no effect.
     */
    declare public readonly period: pulumi.Output<number | undefined>;
    /**
     * The unit of the subscription period.
     * - PricingCycle=1: The unit of the subscription period is year.
     * - PricingCycle=2: The unit of the subscription period is month.
     * - PricingCycle=3: The unit of the subscription period is day.
     *
     * Default value: PricingCycle=2
     *
     * This parameter applies only to specific product types (ProductType is ddos_originpre_public_cn, ddosDip, ddoscoo, ddos_originpre_public_intl, ddosDip_intl, or ddoscoo_intl).
     *
     * > **NOTE:** This parameter is immutable. Changing it after creation has no effect.
     */
    declare public readonly pricingCycle: pulumi.Output<number | undefined>;
    /**
     * The product type. Valid values: `cas` (China site) and `casIntl` (international site). Defaults to `casIntl` for international accounts and `cas` otherwise.
     *
     * > **NOTE:** This parameter is immutable. Changing it after creation has no effect.
     */
    declare public readonly productType: pulumi.Output<string>;
    /**
     * The province or region of the address recorded on the instance.
     */
    declare public /*out*/ readonly province: pulumi.Output<string>;
    /**
     * The ID of the resource group.
     */
    declare public readonly resourceGroupId: pulumi.Output<string>;
    /**
     * The specification of the purchased instance.
     */
    declare public /*out*/ readonly spec: pulumi.Output<string>;
    /**
     * The instance status.
     */
    declare public /*out*/ readonly status: pulumi.Output<string>;
    /**
     * The list of tags.
     */
    declare public readonly tags: pulumi.Output<{[key: string]: string} | undefined>;
    /**
     * The upgrade status of the instance.
     */
    declare public /*out*/ readonly upgradeStatus: pulumi.Output<string>;
    /**
     * The verification method for the certificate application.
     * DNS: DNS verification, using TXT or CNAME records.
     * HTTP: File verification.
     */
    declare public readonly validationMethod: pulumi.Output<string>;
    /**
     * The number of wildcard domain names included in the instance.
     */
    declare public /*out*/ readonly wildcardDomainCount: pulumi.Output<number>;

    /**
     * Create a SslCertificatesServiceInstance resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args?: SslCertificatesServiceInstanceArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: SslCertificatesServiceInstanceArgs | SslCertificatesServiceInstanceState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as SslCertificatesServiceInstanceState | undefined;
            resourceInputs["autoReissue"] = state?.autoReissue;
            resourceInputs["averageWaitingTime"] = state?.averageWaitingTime;
            resourceInputs["brand"] = state?.brand;
            resourceInputs["certificateType"] = state?.certificateType;
            resourceInputs["city"] = state?.city;
            resourceInputs["companyId"] = state?.companyId;
            resourceInputs["contactIdLists"] = state?.contactIdLists;
            resourceInputs["countryCode"] = state?.countryCode;
            resourceInputs["csr"] = state?.csr;
            resourceInputs["domain"] = state?.domain;
            resourceInputs["fullDomainCount"] = state?.fullDomainCount;
            resourceInputs["generateCsrMethod"] = state?.generateCsrMethod;
            resourceInputs["instanceEndTime"] = state?.instanceEndTime;
            resourceInputs["instanceName"] = state?.instanceName;
            resourceInputs["instanceStartTime"] = state?.instanceStartTime;
            resourceInputs["instanceType"] = state?.instanceType;
            resourceInputs["keyAlgorithm"] = state?.keyAlgorithm;
            resourceInputs["orderEndTime"] = state?.orderEndTime;
            resourceInputs["orderStartTime"] = state?.orderStartTime;
            resourceInputs["parameters"] = state?.parameters;
            resourceInputs["period"] = state?.period;
            resourceInputs["pricingCycle"] = state?.pricingCycle;
            resourceInputs["productType"] = state?.productType;
            resourceInputs["province"] = state?.province;
            resourceInputs["resourceGroupId"] = state?.resourceGroupId;
            resourceInputs["spec"] = state?.spec;
            resourceInputs["status"] = state?.status;
            resourceInputs["tags"] = state?.tags;
            resourceInputs["upgradeStatus"] = state?.upgradeStatus;
            resourceInputs["validationMethod"] = state?.validationMethod;
            resourceInputs["wildcardDomainCount"] = state?.wildcardDomainCount;
        } else {
            const args = argsOrState as SslCertificatesServiceInstanceArgs | undefined;
            resourceInputs["autoReissue"] = args?.autoReissue;
            resourceInputs["companyId"] = args?.companyId;
            resourceInputs["contactIdLists"] = args?.contactIdLists;
            resourceInputs["countryCode"] = args?.countryCode;
            resourceInputs["csr"] = args?.csr;
            resourceInputs["domain"] = args?.domain;
            resourceInputs["generateCsrMethod"] = args?.generateCsrMethod;
            resourceInputs["instanceName"] = args?.instanceName;
            resourceInputs["keyAlgorithm"] = args?.keyAlgorithm;
            resourceInputs["parameters"] = args?.parameters;
            resourceInputs["period"] = args?.period;
            resourceInputs["pricingCycle"] = args?.pricingCycle;
            resourceInputs["productType"] = args?.productType;
            resourceInputs["resourceGroupId"] = args?.resourceGroupId;
            resourceInputs["tags"] = args?.tags;
            resourceInputs["validationMethod"] = args?.validationMethod;
            resourceInputs["averageWaitingTime"] = undefined /*out*/;
            resourceInputs["brand"] = undefined /*out*/;
            resourceInputs["certificateType"] = undefined /*out*/;
            resourceInputs["city"] = undefined /*out*/;
            resourceInputs["fullDomainCount"] = undefined /*out*/;
            resourceInputs["instanceEndTime"] = undefined /*out*/;
            resourceInputs["instanceStartTime"] = undefined /*out*/;
            resourceInputs["instanceType"] = undefined /*out*/;
            resourceInputs["orderEndTime"] = undefined /*out*/;
            resourceInputs["orderStartTime"] = undefined /*out*/;
            resourceInputs["province"] = undefined /*out*/;
            resourceInputs["spec"] = undefined /*out*/;
            resourceInputs["status"] = undefined /*out*/;
            resourceInputs["upgradeStatus"] = undefined /*out*/;
            resourceInputs["wildcardDomainCount"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(SslCertificatesServiceInstance.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering SslCertificatesServiceInstance resources.
 */
export interface SslCertificatesServiceInstanceState {
    /**
     * Specifies whether to enable managed renewal.
     * enable: Enabled
     * disable: Disabled
     *
     * > **NOTE:** When managed renewal is enabled, the certificate authority issues a **new certificate** for the instance instead of extending the existing one. The renewed certificate has a different certificate ID and certificate identifier, so any value read from this instance or referenced by downstream resources changes after a renewal. Make sure the resources that consume the certificate take their value from the certificate attributes rather than from a hardcoded ID.
     */
    autoReissue?: pulumi.Input<string | undefined>;
    /**
     * Average waiting time for issuing a certificate of this specification, in seconds.
     */
    averageWaitingTime?: pulumi.Input<string | undefined>;
    /**
     * The certificate brand.
     */
    brand?: pulumi.Input<string | undefined>;
    /**
     * The type of the certificate.
     */
    certificateType?: pulumi.Input<string | undefined>;
    /**
     * The city of the address recorded on the instance.
     */
    city?: pulumi.Input<string | undefined>;
    /**
     * The company information ID. Required for OV and EV certificates — without it the certificate application cannot be submitted for this instance.
     *
     * > **NOTE:** The address recorded on the instance — `city` and `province` — comes from the company referenced by `companyId`, and from a server-side default when no company is referenced. Certificate authorities reject non-standard addresses, so the address is taken from the company record rather than accepted per instance; set it on `alicloud.sslcertificatesservice.Company` instead. Both attributes are read-only here.
     */
    companyId?: pulumi.Input<string | undefined>;
    /**
     * The list of contact IDs. At least one contact ID is required, otherwise the certificate application cannot be submitted for this instance.
     */
    contactIdLists?: pulumi.Input<pulumi.Input<number>[] | undefined>;
    /**
     * The code of the country or region where the certificate organization is located. For example, CN indicates China and US indicates the United States. This field is required when a Certificate Signing Request (CSR) is generated for a DV certificate. The default value is CN.
     */
    countryCode?: pulumi.Input<string | undefined>;
    /**
     * The CSR content. You can generate a CSR by using OpenSSL or Keytool. For more information, see [How to create a CSR file](https://help.aliyun.com/document_detail/42218.html).
     */
    csr?: pulumi.Input<string | undefined>;
    /**
     * The domain names to be bound to the certificate. Specific requirements are as follows:
     * - You can specify a single domain name or a wildcard domain name (such as `*.aliyundoc.com`).
     * - You can specify multiple domain names. Separate multiple domain names with commas (,). Whether free domain names are provided is determined based on the first domain name.
     *
     * > **NOTE:** When you bind multiple domain names to a certificate, this parameter is required. This parameter and `csr` cannot both be empty.
     *
     * > **NOTE:** Which of `domain` and `csr` is authoritative depends on `generateCsrMethod`:
     *
     * | `generateCsrMethod` | Which value wins |
     * | --- | --- |
     * | `online` | The system generates the CSR and `domain` is used as you set it. Leave `csr` unset; the generated value is read back into state. |
     * | `upload` | The domain is taken from the `CN` field of the CSR you upload, so leave `domain` unset and let it be read back from the certificate. Setting it to a value that disagrees with the CSR is rejected by the API. |
     */
    domain?: pulumi.Input<string | undefined>;
    /**
     * The number of single domain names included in the instance.
     */
    fullDomainCount?: pulumi.Input<number | undefined>;
    /**
     * The method used to generate the Certificate Signing Request (CSR). Default value: online.
     * online: The system generates the CSR. In this case, the Csr parameter is ignored.
     * upload: You upload the CSR. In this case, the Csr parameter is required.
     */
    generateCsrMethod?: pulumi.Input<string | undefined>;
    /**
     * Instance expiration time, a UNIX timestamp in seconds.
     */
    instanceEndTime?: pulumi.Input<number | undefined>;
    /**
     * The name of the instance. When a certificate is issued, this value is used as the default name of the certificate.
     */
    instanceName?: pulumi.Input<string | undefined>;
    /**
     * Instance start time, a UNIX timestamp in seconds.
     */
    instanceStartTime?: pulumi.Input<number | undefined>;
    /**
     * The instance type.
     */
    instanceType?: pulumi.Input<string | undefined>;
    /**
     * The certificate algorithm. Default value: RSA_2048.
     * - `RSA_2048`
     * - `RSA_3072`
     * - `RSA_4096`
     * - `ECC_256`
     * - `SM2`
     */
    keyAlgorithm?: pulumi.Input<string | undefined>;
    /**
     * Order end time, a UNIX timestamp in seconds.
     */
    orderEndTime?: pulumi.Input<number | undefined>;
    /**
     * Order start time, a UNIX timestamp in seconds.
     */
    orderStartTime?: pulumi.Input<number | undefined>;
    /**
     * The list of modules. See `parameter` below.
     *
     * > **NOTE:** This parameter is immutable. Changing it after creation has no effect.
     */
    parameters?: pulumi.Input<pulumi.Input<inputs.sslcertificatesserviceinstance.SslCertificatesServiceInstanceParameter>[] | undefined>;
    /**
     * The subscription period. Unit: months. For products billed on a yearly basis, enter an integer multiple of 12.
     *
     * > **NOTE:** This parameter is immutable. Changing it after creation has no effect.
     */
    period?: pulumi.Input<number | undefined>;
    /**
     * The unit of the subscription period.
     * - PricingCycle=1: The unit of the subscription period is year.
     * - PricingCycle=2: The unit of the subscription period is month.
     * - PricingCycle=3: The unit of the subscription period is day.
     *
     * Default value: PricingCycle=2
     *
     * This parameter applies only to specific product types (ProductType is ddos_originpre_public_cn, ddosDip, ddoscoo, ddos_originpre_public_intl, ddosDip_intl, or ddoscoo_intl).
     *
     * > **NOTE:** This parameter is immutable. Changing it after creation has no effect.
     */
    pricingCycle?: pulumi.Input<number | undefined>;
    /**
     * The product type. Valid values: `cas` (China site) and `casIntl` (international site). Defaults to `casIntl` for international accounts and `cas` otherwise.
     *
     * > **NOTE:** This parameter is immutable. Changing it after creation has no effect.
     */
    productType?: pulumi.Input<string | undefined>;
    /**
     * The province or region of the address recorded on the instance.
     */
    province?: pulumi.Input<string | undefined>;
    /**
     * The ID of the resource group.
     */
    resourceGroupId?: pulumi.Input<string | undefined>;
    /**
     * The specification of the purchased instance.
     */
    spec?: pulumi.Input<string | undefined>;
    /**
     * The instance status.
     */
    status?: pulumi.Input<string | undefined>;
    /**
     * The list of tags.
     */
    tags?: pulumi.Input<{[key: string]: pulumi.Input<string>} | undefined>;
    /**
     * The upgrade status of the instance.
     */
    upgradeStatus?: pulumi.Input<string | undefined>;
    /**
     * The verification method for the certificate application.
     * DNS: DNS verification, using TXT or CNAME records.
     * HTTP: File verification.
     */
    validationMethod?: pulumi.Input<string | undefined>;
    /**
     * The number of wildcard domain names included in the instance.
     */
    wildcardDomainCount?: pulumi.Input<number | undefined>;
}

/**
 * The set of arguments for constructing a SslCertificatesServiceInstance resource.
 */
export interface SslCertificatesServiceInstanceArgs {
    /**
     * Specifies whether to enable managed renewal.
     * enable: Enabled
     * disable: Disabled
     *
     * > **NOTE:** When managed renewal is enabled, the certificate authority issues a **new certificate** for the instance instead of extending the existing one. The renewed certificate has a different certificate ID and certificate identifier, so any value read from this instance or referenced by downstream resources changes after a renewal. Make sure the resources that consume the certificate take their value from the certificate attributes rather than from a hardcoded ID.
     */
    autoReissue?: pulumi.Input<string | undefined>;
    /**
     * The company information ID. Required for OV and EV certificates — without it the certificate application cannot be submitted for this instance.
     *
     * > **NOTE:** The address recorded on the instance — `city` and `province` — comes from the company referenced by `companyId`, and from a server-side default when no company is referenced. Certificate authorities reject non-standard addresses, so the address is taken from the company record rather than accepted per instance; set it on `alicloud.sslcertificatesservice.Company` instead. Both attributes are read-only here.
     */
    companyId?: pulumi.Input<string | undefined>;
    /**
     * The list of contact IDs. At least one contact ID is required, otherwise the certificate application cannot be submitted for this instance.
     */
    contactIdLists?: pulumi.Input<pulumi.Input<number>[] | undefined>;
    /**
     * The code of the country or region where the certificate organization is located. For example, CN indicates China and US indicates the United States. This field is required when a Certificate Signing Request (CSR) is generated for a DV certificate. The default value is CN.
     */
    countryCode?: pulumi.Input<string | undefined>;
    /**
     * The CSR content. You can generate a CSR by using OpenSSL or Keytool. For more information, see [How to create a CSR file](https://help.aliyun.com/document_detail/42218.html).
     */
    csr?: pulumi.Input<string | undefined>;
    /**
     * The domain names to be bound to the certificate. Specific requirements are as follows:
     * - You can specify a single domain name or a wildcard domain name (such as `*.aliyundoc.com`).
     * - You can specify multiple domain names. Separate multiple domain names with commas (,). Whether free domain names are provided is determined based on the first domain name.
     *
     * > **NOTE:** When you bind multiple domain names to a certificate, this parameter is required. This parameter and `csr` cannot both be empty.
     *
     * > **NOTE:** Which of `domain` and `csr` is authoritative depends on `generateCsrMethod`:
     *
     * | `generateCsrMethod` | Which value wins |
     * | --- | --- |
     * | `online` | The system generates the CSR and `domain` is used as you set it. Leave `csr` unset; the generated value is read back into state. |
     * | `upload` | The domain is taken from the `CN` field of the CSR you upload, so leave `domain` unset and let it be read back from the certificate. Setting it to a value that disagrees with the CSR is rejected by the API. |
     */
    domain?: pulumi.Input<string | undefined>;
    /**
     * The method used to generate the Certificate Signing Request (CSR). Default value: online.
     * online: The system generates the CSR. In this case, the Csr parameter is ignored.
     * upload: You upload the CSR. In this case, the Csr parameter is required.
     */
    generateCsrMethod?: pulumi.Input<string | undefined>;
    /**
     * The name of the instance. When a certificate is issued, this value is used as the default name of the certificate.
     */
    instanceName?: pulumi.Input<string | undefined>;
    /**
     * The certificate algorithm. Default value: RSA_2048.
     * - `RSA_2048`
     * - `RSA_3072`
     * - `RSA_4096`
     * - `ECC_256`
     * - `SM2`
     */
    keyAlgorithm?: pulumi.Input<string | undefined>;
    /**
     * The list of modules. See `parameter` below.
     *
     * > **NOTE:** This parameter is immutable. Changing it after creation has no effect.
     */
    parameters?: pulumi.Input<pulumi.Input<inputs.sslcertificatesserviceinstance.SslCertificatesServiceInstanceParameter>[] | undefined>;
    /**
     * The subscription period. Unit: months. For products billed on a yearly basis, enter an integer multiple of 12.
     *
     * > **NOTE:** This parameter is immutable. Changing it after creation has no effect.
     */
    period?: pulumi.Input<number | undefined>;
    /**
     * The unit of the subscription period.
     * - PricingCycle=1: The unit of the subscription period is year.
     * - PricingCycle=2: The unit of the subscription period is month.
     * - PricingCycle=3: The unit of the subscription period is day.
     *
     * Default value: PricingCycle=2
     *
     * This parameter applies only to specific product types (ProductType is ddos_originpre_public_cn, ddosDip, ddoscoo, ddos_originpre_public_intl, ddosDip_intl, or ddoscoo_intl).
     *
     * > **NOTE:** This parameter is immutable. Changing it after creation has no effect.
     */
    pricingCycle?: pulumi.Input<number | undefined>;
    /**
     * The product type. Valid values: `cas` (China site) and `casIntl` (international site). Defaults to `casIntl` for international accounts and `cas` otherwise.
     *
     * > **NOTE:** This parameter is immutable. Changing it after creation has no effect.
     */
    productType?: pulumi.Input<string | undefined>;
    /**
     * The ID of the resource group.
     */
    resourceGroupId?: pulumi.Input<string | undefined>;
    /**
     * The list of tags.
     */
    tags?: pulumi.Input<{[key: string]: pulumi.Input<string>} | undefined>;
    /**
     * The verification method for the certificate application.
     * DNS: DNS verification, using TXT or CNAME records.
     * HTTP: File verification.
     */
    validationMethod?: pulumi.Input<string | undefined>;
}
