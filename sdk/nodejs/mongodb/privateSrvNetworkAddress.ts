// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provides a Mongo D B Private Srv Network Address resource.
 *
 * Private network SRV highly available link address.
 *
 * For information about Mongo D B Private Srv Network Address and how to use it, see [What is Private Srv Network Address](https://www.alibabacloud.com/help/en/).
 *
 * > **NOTE:** Available since v1.240.0.
 *
 * ## Example Usage
 *
 * Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const zoneId = config.get("zoneId") || "cn-shanghai-b";
 * const regionId = config.get("regionId") || "cn-shanghai";
 * const defaultie35CW = new alicloud.vpc.Network("defaultie35CW", {
 *     cidrBlock: "10.0.0.0/8",
 *     vpcName: name,
 * });
 * const defaultg0DCAR = new alicloud.vpc.Switch("defaultg0DCAR", {
 *     vpcId: defaultie35CW.id,
 *     zoneId: zoneId,
 *     cidrBlock: "10.0.0.0/24",
 * });
 * const defaultHrZmxC = new alicloud.mongodb.Instance("defaultHrZmxC", {
 *     engineVersion: "4.4",
 *     storageType: "cloud_essd1",
 *     vswitchId: defaultg0DCAR.id,
 *     dbInstanceStorage: 20,
 *     vpcId: defaultie35CW.id,
 *     dbInstanceClass: "mdb.shard.4x.large.d",
 *     storageEngine: "WiredTiger",
 *     networkType: "VPC",
 *     zoneId: zoneId,
 * });
 * const _default = new alicloud.mongodb.PrivateSrvNetworkAddress("default", {dbInstanceId: defaultHrZmxC.id});
 * ```
 *
 * ## Import
 *
 * Mongo D B Private Srv Network Address can be imported using the id, e.g.
 *
 * ```sh
 * $ pulumi import alicloud:mongodb/privateSrvNetworkAddress:PrivateSrvNetworkAddress example <id>
 * ```
 */
export class PrivateSrvNetworkAddress extends pulumi.CustomResource {
    /**
     * Get an existing PrivateSrvNetworkAddress resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: PrivateSrvNetworkAddressState, opts?: pulumi.CustomResourceOptions): PrivateSrvNetworkAddress {
        return new PrivateSrvNetworkAddress(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'alicloud:mongodb/privateSrvNetworkAddress:PrivateSrvNetworkAddress';

    /**
     * Returns true if the given object is an instance of PrivateSrvNetworkAddress.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is PrivateSrvNetworkAddress {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === PrivateSrvNetworkAddress.__pulumiType;
    }

    /**
     * The instance ID.
     */
    public readonly dbInstanceId!: pulumi.Output<string>;
    /**
     * Private network SRV highly available connection address
     */
    public /*out*/ readonly privateSrvConnectionStringUri!: pulumi.Output<string>;

    /**
     * Create a PrivateSrvNetworkAddress resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: PrivateSrvNetworkAddressArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: PrivateSrvNetworkAddressArgs | PrivateSrvNetworkAddressState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as PrivateSrvNetworkAddressState | undefined;
            resourceInputs["dbInstanceId"] = state ? state.dbInstanceId : undefined;
            resourceInputs["privateSrvConnectionStringUri"] = state ? state.privateSrvConnectionStringUri : undefined;
        } else {
            const args = argsOrState as PrivateSrvNetworkAddressArgs | undefined;
            if ((!args || args.dbInstanceId === undefined) && !opts.urn) {
                throw new Error("Missing required property 'dbInstanceId'");
            }
            resourceInputs["dbInstanceId"] = args ? args.dbInstanceId : undefined;
            resourceInputs["privateSrvConnectionStringUri"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(PrivateSrvNetworkAddress.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering PrivateSrvNetworkAddress resources.
 */
export interface PrivateSrvNetworkAddressState {
    /**
     * The instance ID.
     */
    dbInstanceId?: pulumi.Input<string>;
    /**
     * Private network SRV highly available connection address
     */
    privateSrvConnectionStringUri?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a PrivateSrvNetworkAddress resource.
 */
export interface PrivateSrvNetworkAddressArgs {
    /**
     * The instance ID.
     */
    dbInstanceId: pulumi.Input<string>;
}
