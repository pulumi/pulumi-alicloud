// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * This data source provides the Mongodb Sharding Audit Filter of the current Alibaba Cloud user.
 *
 * > **NOTE:** Available since v1.290.0.
 *
 * ## Example Usage
 *
 * Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const _default = alicloud.mongodb.getZones({});
 * const defaultGetNetworks = alicloud.vpc.getNetworks({
 *     nameRegex: "^default-NODELETING$",
 * });
 * const defaultGetSwitches = Promise.all([defaultGetNetworks, _default]).then(([defaultGetNetworks, _default]) => alicloud.vpc.getSwitches({
 *     vpcId: defaultGetNetworks.ids?.[0],
 *     zoneId: _default.zones?.[0]?.id,
 * }));
 * const defaultShardingInstance = new alicloud.mongodb.ShardingInstance("default", {
 *     zoneId: _default.then(_default => _default.zones?.[0]?.id),
 *     vswitchId: defaultGetSwitches.then(defaultGetSwitches => defaultGetSwitches.ids?.[0]),
 *     engineVersion: "4.2",
 *     name: name,
 *     mongoLists: [
 *         {
 *             nodeClass: "dds.mongos.mid",
 *         },
 *         {
 *             nodeClass: "dds.mongos.mid",
 *         },
 *     ],
 *     shardLists: [
 *         {
 *             nodeClass: "dds.shard.mid",
 *             nodeStorage: 10,
 *         },
 *         {
 *             nodeClass: "dds.shard.mid",
 *             nodeStorage: 10,
 *         },
 *     ],
 * });
 * const defaultShardingAuditFilter = new alicloud.mongodb.ShardingAuditFilter("default", {
 *     dbInstanceId: defaultShardingInstance.id,
 *     auditStatus: "enable",
 *     filter: "admin,slow",
 * });
 * const defaultGetShardingAuditFilters = alicloud.mongodb.getShardingAuditFiltersOutput({
 *     dbInstanceId: defaultShardingAuditFilter.dbInstanceId,
 * });
 * export const shardingAuditFilterId = defaultGetShardingAuditFilters.apply(defaultGetShardingAuditFilters => defaultGetShardingAuditFilters.filters?.[0]?.id);
 * ```
 */
export function getShardingAuditFilters(args: GetShardingAuditFiltersArgs, opts?: pulumi.InvokeOptions): Promise<GetShardingAuditFiltersResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("alicloud:mongodb/getShardingAuditFilters:getShardingAuditFilters", {
        "dbInstanceId": args.dbInstanceId,
        "outputFile": args.outputFile,
    }, opts);
}

/**
 * A collection of arguments for invoking getShardingAuditFilters.
 */
export interface GetShardingAuditFiltersArgs {
    /**
     * The ID of the sharding cluster instance.
     */
    dbInstanceId: string;
    /**
     * File name where to write the output of the data source invocation (when set, the file is created before the resource is applied).
     */
    outputFile?: string;
}

/**
 * A collection of values returned by getShardingAuditFilters.
 */
export interface GetShardingAuditFiltersResult {
    /**
     * The ID of the sharding cluster instance.
     */
    readonly dbInstanceId: string;
    /**
     * A list of Sharding Audit Filter. Each element contains the following attributes:
     */
    readonly filters: outputs.mongodb.GetShardingAuditFiltersFilter[];
    /**
     * The provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
    readonly outputFile?: string;
}
/**
 * This data source provides the Mongodb Sharding Audit Filter of the current Alibaba Cloud user.
 *
 * > **NOTE:** Available since v1.290.0.
 *
 * ## Example Usage
 *
 * Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const _default = alicloud.mongodb.getZones({});
 * const defaultGetNetworks = alicloud.vpc.getNetworks({
 *     nameRegex: "^default-NODELETING$",
 * });
 * const defaultGetSwitches = Promise.all([defaultGetNetworks, _default]).then(([defaultGetNetworks, _default]) => alicloud.vpc.getSwitches({
 *     vpcId: defaultGetNetworks.ids?.[0],
 *     zoneId: _default.zones?.[0]?.id,
 * }));
 * const defaultShardingInstance = new alicloud.mongodb.ShardingInstance("default", {
 *     zoneId: _default.then(_default => _default.zones?.[0]?.id),
 *     vswitchId: defaultGetSwitches.then(defaultGetSwitches => defaultGetSwitches.ids?.[0]),
 *     engineVersion: "4.2",
 *     name: name,
 *     mongoLists: [
 *         {
 *             nodeClass: "dds.mongos.mid",
 *         },
 *         {
 *             nodeClass: "dds.mongos.mid",
 *         },
 *     ],
 *     shardLists: [
 *         {
 *             nodeClass: "dds.shard.mid",
 *             nodeStorage: 10,
 *         },
 *         {
 *             nodeClass: "dds.shard.mid",
 *             nodeStorage: 10,
 *         },
 *     ],
 * });
 * const defaultShardingAuditFilter = new alicloud.mongodb.ShardingAuditFilter("default", {
 *     dbInstanceId: defaultShardingInstance.id,
 *     auditStatus: "enable",
 *     filter: "admin,slow",
 * });
 * const defaultGetShardingAuditFilters = alicloud.mongodb.getShardingAuditFiltersOutput({
 *     dbInstanceId: defaultShardingAuditFilter.dbInstanceId,
 * });
 * export const shardingAuditFilterId = defaultGetShardingAuditFilters.apply(defaultGetShardingAuditFilters => defaultGetShardingAuditFilters.filters?.[0]?.id);
 * ```
 */
export function getShardingAuditFiltersOutput(args: GetShardingAuditFiltersOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetShardingAuditFiltersResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("alicloud:mongodb/getShardingAuditFilters:getShardingAuditFilters", {
        "dbInstanceId": args.dbInstanceId,
        "outputFile": args.outputFile,
    }, opts);
}

/**
 * A collection of arguments for invoking getShardingAuditFilters.
 */
export interface GetShardingAuditFiltersOutputArgs {
    /**
     * The ID of the sharding cluster instance.
     */
    dbInstanceId: pulumi.Input<string>;
    /**
     * File name where to write the output of the data source invocation (when set, the file is created before the resource is applied).
     */
    outputFile?: pulumi.Input<string | undefined>;
}
