// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provides a Mongodb Sharding Audit Filter resource.
 *
 * For information about Mongodb Sharding Audit Filter and how to use it, see [ModifyAuditLogFilter](https://www.alibabacloud.com/help/en/mongodb/developer-reference/api-mongodb-2015-12-01-modifyauditlogfilter).
 *
 * > **NOTE:** Available since v1.290.0.
 *
 * ## Example Usage
 *
 * Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const _default = alicloud.mongodb.getZones({});
 * const defaultGetNetworks = alicloud.vpc.getNetworks({
 *     nameRegex: "^default-NODELETING$",
 * });
 * const defaultGetSwitches = Promise.all([defaultGetNetworks, _default]).then(([defaultGetNetworks, _default]) => alicloud.vpc.getSwitches({
 *     vpcId: defaultGetNetworks.ids?.[0],
 *     zoneId: _default.zones?.[0]?.id,
 * }));
 * const defaultShardingInstance = new alicloud.mongodb.ShardingInstance("default", {
 *     zoneId: _default.then(_default => _default.zones?.[0]?.id),
 *     vswitchId: defaultGetSwitches.then(defaultGetSwitches => defaultGetSwitches.ids?.[0]),
 *     engineVersion: "4.2",
 *     name: name,
 *     mongoLists: [
 *         {
 *             nodeClass: "dds.mongos.mid",
 *         },
 *         {
 *             nodeClass: "dds.mongos.mid",
 *         },
 *     ],
 *     shardLists: [
 *         {
 *             nodeClass: "dds.shard.mid",
 *             nodeStorage: 10,
 *         },
 *         {
 *             nodeClass: "dds.shard.mid",
 *             nodeStorage: 10,
 *         },
 *     ],
 * });
 * const defaultShardingAuditFilter = new alicloud.mongodb.ShardingAuditFilter("default", {
 *     dbInstanceId: defaultShardingInstance.id,
 *     auditStatus: "enable",
 *     filter: "admin,slow",
 *     serviceType: "V2_Standard",
 *     storagePeriod: 30,
 *     hotStoragePeriod: 7,
 * });
 * ```
 *
 * ### Deleting `alicloud.mongodb.ShardingAuditFilter` or removing it from your configuration
 *
 * Terraform cannot destroy resource `alicloud.mongodb.ShardingAuditFilter`. Terraform will remove this resource from the state file, however resources may remain.
 *
 * 📚 Need more examples? VIEW MORE EXAMPLES
 *
 * ## Import
 *
 * Mongodb Sharding Audit Filter can be imported using the id, e.g.
 *
 * ```sh
 * $ pulumi import alicloud:mongodb/shardingAuditFilter:ShardingAuditFilter example <db_instance_id>
 * ```
 */
export class ShardingAuditFilter extends pulumi.CustomResource {
    /**
     * Get an existing ShardingAuditFilter resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: ShardingAuditFilterState, opts?: pulumi.CustomResourceOptions): ShardingAuditFilter {
        return new ShardingAuditFilter(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'alicloud:mongodb/shardingAuditFilter:ShardingAuditFilter';

    /**
     * Returns true if the given object is an instance of ShardingAuditFilter.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is ShardingAuditFilter {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === ShardingAuditFilter.__pulumiType;
    }

    /**
     * Audit state. Valid values: `enable`, `disabled`.
     */
    declare public readonly auditStatus: pulumi.Output<string>;
    /**
     * The ID of the sharding cluster instance.
     */
    declare public readonly dbInstanceId: pulumi.Output<string>;
    /**
     * The type of logs collected by the audit log feature of the instance. Separate multiple types with commas (,). Valid values:
     */
    declare public readonly filter: pulumi.Output<string>;
    /**
     * The hot storage duration of the audit log, in days. The value range is 0 to 7. Only takes effect when `serviceType` is `V2_Standard`.
     */
    declare public readonly hotStoragePeriod: pulumi.Output<number>;
    /**
     * The region ID of the sharding cluster instance.
     */
    declare public /*out*/ readonly regionId: pulumi.Output<string>;
    /**
     * The role of the node in the sharding cluster instance that the audit log filter applies to. Valid values:
     */
    declare public readonly roleType: pulumi.Output<string>;
    /**
     * The edition of the audit log. Valid values: `Standard`, `V2_Standard`. If omitted, the Provider sends `Standard`. In regions where only the V2 audit log is available, set this to `V2_Standard`. Changes to this field are ignored while `auditStatus` is `disabled` — the server switches the edition internally when audit is off and restores the declared value on re-enable.
     */
    declare public readonly serviceType: pulumi.Output<string>;
    /**
     * Audit log retention duration, in days.
     * - When `serviceType` is `Standard`, the value range is 1 to 365 days. The default value is 30 days.
     * - When `serviceType` is `V2_Standard`, this is the cold storage duration and is required. Valid values: `30`, `180`, `365`, `1095`, `1825`.
     */
    declare public readonly storagePeriod: pulumi.Output<number>;

    /**
     * Create a ShardingAuditFilter resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: ShardingAuditFilterArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: ShardingAuditFilterArgs | ShardingAuditFilterState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as ShardingAuditFilterState | undefined;
            resourceInputs["auditStatus"] = state?.auditStatus;
            resourceInputs["dbInstanceId"] = state?.dbInstanceId;
            resourceInputs["filter"] = state?.filter;
            resourceInputs["hotStoragePeriod"] = state?.hotStoragePeriod;
            resourceInputs["regionId"] = state?.regionId;
            resourceInputs["roleType"] = state?.roleType;
            resourceInputs["serviceType"] = state?.serviceType;
            resourceInputs["storagePeriod"] = state?.storagePeriod;
        } else {
            const args = argsOrState as ShardingAuditFilterArgs | undefined;
            if (args?.auditStatus === undefined && !opts.urn) {
                throw new Error("Missing required property 'auditStatus'");
            }
            if (args?.dbInstanceId === undefined && !opts.urn) {
                throw new Error("Missing required property 'dbInstanceId'");
            }
            if (args?.filter === undefined && !opts.urn) {
                throw new Error("Missing required property 'filter'");
            }
            resourceInputs["auditStatus"] = args?.auditStatus;
            resourceInputs["dbInstanceId"] = args?.dbInstanceId;
            resourceInputs["filter"] = args?.filter;
            resourceInputs["hotStoragePeriod"] = args?.hotStoragePeriod;
            resourceInputs["roleType"] = args?.roleType;
            resourceInputs["serviceType"] = args?.serviceType;
            resourceInputs["storagePeriod"] = args?.storagePeriod;
            resourceInputs["regionId"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(ShardingAuditFilter.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering ShardingAuditFilter resources.
 */
export interface ShardingAuditFilterState {
    /**
     * Audit state. Valid values: `enable`, `disabled`.
     */
    auditStatus?: pulumi.Input<string | undefined>;
    /**
     * The ID of the sharding cluster instance.
     */
    dbInstanceId?: pulumi.Input<string | undefined>;
    /**
     * The type of logs collected by the audit log feature of the instance. Separate multiple types with commas (,). Valid values:
     */
    filter?: pulumi.Input<string | undefined>;
    /**
     * The hot storage duration of the audit log, in days. The value range is 0 to 7. Only takes effect when `serviceType` is `V2_Standard`.
     */
    hotStoragePeriod?: pulumi.Input<number | undefined>;
    /**
     * The region ID of the sharding cluster instance.
     */
    regionId?: pulumi.Input<string | undefined>;
    /**
     * The role of the node in the sharding cluster instance that the audit log filter applies to. Valid values:
     */
    roleType?: pulumi.Input<string | undefined>;
    /**
     * The edition of the audit log. Valid values: `Standard`, `V2_Standard`. If omitted, the Provider sends `Standard`. In regions where only the V2 audit log is available, set this to `V2_Standard`. Changes to this field are ignored while `auditStatus` is `disabled` — the server switches the edition internally when audit is off and restores the declared value on re-enable.
     */
    serviceType?: pulumi.Input<string | undefined>;
    /**
     * Audit log retention duration, in days.
     * - When `serviceType` is `Standard`, the value range is 1 to 365 days. The default value is 30 days.
     * - When `serviceType` is `V2_Standard`, this is the cold storage duration and is required. Valid values: `30`, `180`, `365`, `1095`, `1825`.
     */
    storagePeriod?: pulumi.Input<number | undefined>;
}

/**
 * The set of arguments for constructing a ShardingAuditFilter resource.
 */
export interface ShardingAuditFilterArgs {
    /**
     * Audit state. Valid values: `enable`, `disabled`.
     */
    auditStatus: pulumi.Input<string>;
    /**
     * The ID of the sharding cluster instance.
     */
    dbInstanceId: pulumi.Input<string>;
    /**
     * The type of logs collected by the audit log feature of the instance. Separate multiple types with commas (,). Valid values:
     */
    filter: pulumi.Input<string>;
    /**
     * The hot storage duration of the audit log, in days. The value range is 0 to 7. Only takes effect when `serviceType` is `V2_Standard`.
     */
    hotStoragePeriod?: pulumi.Input<number | undefined>;
    /**
     * The role of the node in the sharding cluster instance that the audit log filter applies to. Valid values:
     */
    roleType?: pulumi.Input<string | undefined>;
    /**
     * The edition of the audit log. Valid values: `Standard`, `V2_Standard`. If omitted, the Provider sends `Standard`. In regions where only the V2 audit log is available, set this to `V2_Standard`. Changes to this field are ignored while `auditStatus` is `disabled` — the server switches the edition internally when audit is off and restores the declared value on re-enable.
     */
    serviceType?: pulumi.Input<string | undefined>;
    /**
     * Audit log retention duration, in days.
     * - When `serviceType` is `Standard`, the value range is 1 to 365 days. The default value is 30 days.
     * - When `serviceType` is `V2_Standard`, this is the cold storage duration and is required. Valid values: `30`, `180`, `365`, `1095`, `1825`.
     */
    storagePeriod?: pulumi.Input<number | undefined>;
}
