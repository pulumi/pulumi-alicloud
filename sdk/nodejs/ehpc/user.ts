// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provides a Ehpc User resource.
 *
 * A user in an E-HPC cluster.
 *
 * For information about Ehpc User and how to use it, see [What is User](https://next.api.alibabacloud.com/document/EHPC/2024-07-30/CreateUsers).
 *
 * > **NOTE:** Available since v1.292.0.
 *
 * ## Example Usage
 *
 * Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const userPasswordExampleVpc = new alicloud.vpc.Network("user_password_example_vpc", {
 *     isDefault: false,
 *     cidrBlock: "10.0.0.0/8",
 *     vpcName: "user-password-example-vpc",
 * });
 * const userPasswordExampleVswitch = new alicloud.vpc.Switch("user_password_example_vswitch", {
 *     isDefault: false,
 *     vpcId: userPasswordExampleVpc.id,
 *     zoneId: "cn-hangzhou-k",
 *     cidrBlock: "10.0.0.0/24",
 *     vswitchName: "user-password-example-vsw",
 * });
 * const userPasswordExampleNas = new alicloud.nas.FileSystem("user_password_example_nas", {
 *     description: "user-password-example-nas",
 *     storageType: "Capacity",
 *     nfsAcl: {
 *         enabled: false,
 *     },
 *     zoneId: "cn-hangzhou-k",
 *     encryptType: 0,
 *     protocolType: "NFS",
 *     fileSystemType: "standard",
 *     recycleBin: {
 *         status: "Disable",
 *         reservedDays: 7,
 *     },
 * });
 * const userPasswordExampleAccessGroup = new alicloud.nas.AccessGroup("user_password_example_access_group", {
 *     accessGroupType: "Vpc",
 *     description: "user password example access group",
 *     accessGroupName: "UserPasswordTest",
 *     fileSystemType: "standard",
 * });
 * const userPasswordExampleMountDomain = new alicloud.nas.MountTarget("user_password_example_mount_domain", {
 *     vpcId: userPasswordExampleVpc.id,
 *     networkType: "Vpc",
 *     accessGroupName: userPasswordExampleAccessGroup.accessGroupName,
 *     vswitchId: userPasswordExampleVswitch.id,
 *     fileSystemId: userPasswordExampleNas.id,
 * });
 * const userPasswordExampleSecurityGroup = new alicloud.ecs.SecurityGroup("user_password_example_security_group", {
 *     vpcId: userPasswordExampleVpc.id,
 *     securityGroupType: "normal",
 * });
 * const userPasswordExampleAccessRule = new alicloud.nas.AccessRule("user_password_example_access_rule", {
 *     priority: 1,
 *     accessGroupName: userPasswordExampleAccessGroup.accessGroupName,
 *     fileSystemType: userPasswordExampleNas.fileSystemType,
 *     sourceCidrIp: "10.0.0.0/8",
 * });
 * const userPasswordExampleCluster = new alicloud.ehpc.ClusterV2("user_password_example_cluster", {
 *     clusterCredentials: {
 *         password: "aliHPC123",
 *     },
 *     clusterVpcId: userPasswordExampleVpc.id,
 *     clusterCategory: "Standard",
 *     clusterMode: "Integrated",
 *     securityGroupId: userPasswordExampleSecurityGroup.id,
 *     clusterName: "user-password-example-cluster",
 *     deletionProtection: false,
 *     sharedStorages: [
 *         {
 *             mountDirectory: "/home",
 *             nasDirectory: "/",
 *             mountTargetDomain: userPasswordExampleMountDomain.mountTargetDomain,
 *             protocolType: "NFS",
 *             fileSystemId: userPasswordExampleNas.id,
 *             mountOptions: "-t nfs -o vers=3,nolock,proto=tcp,noresvport",
 *         },
 *         {
 *             mountDirectory: "/opt",
 *             nasDirectory: "/",
 *             mountTargetDomain: userPasswordExampleMountDomain.mountTargetDomain,
 *             protocolType: "NFS",
 *             fileSystemId: userPasswordExampleNas.id,
 *             mountOptions: "-t nfs -o vers=3,nolock,proto=tcp,noresvport",
 *         },
 *         {
 *             mountDirectory: "/ehpcdata",
 *             nasDirectory: "/",
 *             mountTargetDomain: userPasswordExampleMountDomain.mountTargetDomain,
 *             protocolType: "NFS",
 *             fileSystemId: userPasswordExampleNas.id,
 *             mountOptions: "-t nfs -o vers=3,nolock,proto=tcp,noresvport",
 *         },
 *     ],
 *     clusterVswitchId: userPasswordExampleVswitch.id,
 *     manager: {
 *         managerNode: {
 *             systemDisk: {
 *                 category: "cloud_essd",
 *                 size: 40,
 *                 level: "PL0",
 *             },
 *             enableHt: true,
 *             instanceChargeType: "PostPaid",
 *             imageId: "centos_7_6_x64_20G_alibase_20211130.vhd",
 *             instanceType: "ecs.c6.xlarge",
 *             spotStrategy: "NoSpot",
 *         },
 *         scheduler: {
 *             type: "SLURM",
 *             version: "22.05.8",
 *         },
 *         dns: {
 *             type: "nis",
 *             version: "1.0",
 *         },
 *         directoryService: {
 *             type: "nis",
 *             version: "1.0",
 *         },
 *     },
 * });
 * const _default = new alicloud.ehpc.User("default", {
 *     group: "users",
 *     userName: "passworduser1",
 *     clusterId: userPasswordExampleCluster.id,
 *     password: "UserPass123",
 * });
 * ```
 *
 * 📚 Need more examples? VIEW MORE EXAMPLES
 *
 * ## Import
 *
 * Ehpc User can be imported using the id, e.g.
 *
 * ```sh
 * $ pulumi import alicloud:ehpc/user:User example <cluster_id>:<user_name>
 * ```
 */
export class User extends pulumi.CustomResource {
    /**
     * Get an existing User resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: UserState, opts?: pulumi.CustomResourceOptions): User {
        return new User(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'alicloud:ehpc/user:User';

    /**
     * Returns true if the given object is an instance of User.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is User {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === User.__pulumiType;
    }

    /**
     * The cluster ID.
     */
    declare public readonly clusterId: pulumi.Output<string>;
    /**
     * The user group. Valid values: `users`, `wheel`.
     */
    declare public readonly group: pulumi.Output<string>;
    /**
     * The user group ID.
     */
    declare public /*out*/ readonly groupId: pulumi.Output<string>;
    /**
     * The SSH public-key content. This field is accepted only for create and is never returned. This parameter is mutually exclusive with the `password` parameter. This parameter takes effect when the cluster authentication method is set to key (not recommended).
     *
     * > **NOTE:** This parameter is immutable. Changing it after creation has no effect.
     */
    declare public readonly keyPairName: pulumi.Output<string | undefined>;
    /**
     * The user password. This parameter is mutually exclusive with the `keyPairName` parameter. This parameter takes effect when the cluster authentication method is set to password (recommended).
     *
     * > **NOTE:** This parameter is only evaluated during resource creation and update. Modifying it in isolation will not trigger any action.
     */
    declare public readonly password: pulumi.Output<string | undefined>;
    /**
     * The user ID.
     */
    declare public /*out*/ readonly userId: pulumi.Output<string>;
    /**
     * The username.
     */
    declare public readonly userName: pulumi.Output<string>;

    /**
     * Create a User resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: UserArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: UserArgs | UserState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as UserState | undefined;
            resourceInputs["clusterId"] = state?.clusterId;
            resourceInputs["group"] = state?.group;
            resourceInputs["groupId"] = state?.groupId;
            resourceInputs["keyPairName"] = state?.keyPairName;
            resourceInputs["password"] = state?.password;
            resourceInputs["userId"] = state?.userId;
            resourceInputs["userName"] = state?.userName;
        } else {
            const args = argsOrState as UserArgs | undefined;
            if (args?.clusterId === undefined && !opts.urn) {
                throw new Error("Missing required property 'clusterId'");
            }
            if (args?.group === undefined && !opts.urn) {
                throw new Error("Missing required property 'group'");
            }
            if (args?.userName === undefined && !opts.urn) {
                throw new Error("Missing required property 'userName'");
            }
            resourceInputs["clusterId"] = args?.clusterId;
            resourceInputs["group"] = args?.group;
            resourceInputs["keyPairName"] = args?.keyPairName ? pulumi.secret(args.keyPairName) : undefined;
            resourceInputs["password"] = args?.password ? pulumi.secret(args.password) : undefined;
            resourceInputs["userName"] = args?.userName;
            resourceInputs["groupId"] = undefined /*out*/;
            resourceInputs["userId"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        const secretOpts = { additionalSecretOutputs: ["keyPairName", "password"] };
        opts = pulumi.mergeOptions(opts, secretOpts);
        super(User.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering User resources.
 */
export interface UserState {
    /**
     * The cluster ID.
     */
    clusterId?: pulumi.Input<string | undefined>;
    /**
     * The user group. Valid values: `users`, `wheel`.
     */
    group?: pulumi.Input<string | undefined>;
    /**
     * The user group ID.
     */
    groupId?: pulumi.Input<string | undefined>;
    /**
     * The SSH public-key content. This field is accepted only for create and is never returned. This parameter is mutually exclusive with the `password` parameter. This parameter takes effect when the cluster authentication method is set to key (not recommended).
     *
     * > **NOTE:** This parameter is immutable. Changing it after creation has no effect.
     */
    keyPairName?: pulumi.Input<string | undefined>;
    /**
     * The user password. This parameter is mutually exclusive with the `keyPairName` parameter. This parameter takes effect when the cluster authentication method is set to password (recommended).
     *
     * > **NOTE:** This parameter is only evaluated during resource creation and update. Modifying it in isolation will not trigger any action.
     */
    password?: pulumi.Input<string | undefined>;
    /**
     * The user ID.
     */
    userId?: pulumi.Input<string | undefined>;
    /**
     * The username.
     */
    userName?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a User resource.
 */
export interface UserArgs {
    /**
     * The cluster ID.
     */
    clusterId: pulumi.Input<string>;
    /**
     * The user group. Valid values: `users`, `wheel`.
     */
    group: pulumi.Input<string>;
    /**
     * The SSH public-key content. This field is accepted only for create and is never returned. This parameter is mutually exclusive with the `password` parameter. This parameter takes effect when the cluster authentication method is set to key (not recommended).
     *
     * > **NOTE:** This parameter is immutable. Changing it after creation has no effect.
     */
    keyPairName?: pulumi.Input<string | undefined>;
    /**
     * The user password. This parameter is mutually exclusive with the `keyPairName` parameter. This parameter takes effect when the cluster authentication method is set to password (recommended).
     *
     * > **NOTE:** This parameter is only evaluated during resource creation and update. Modifying it in isolation will not trigger any action.
     */
    password?: pulumi.Input<string | undefined>;
    /**
     * The username.
     */
    userName: pulumi.Input<string>;
}
