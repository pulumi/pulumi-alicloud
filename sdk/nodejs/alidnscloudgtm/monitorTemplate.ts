// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * Provides a Alidns Cloud Gtm Monitor Template resource.
 *
 * A Cloud GTM monitor template defines reusable health-check probe configurations (protocol, interval, failure thresholds, probe nodes) that can be attached to Cloud GTM address pools.
 *
 * For information about Alidns Cloud Gtm Monitor Template and how to use it, see [What is Cloud Gtm Monitor Template](https://next.api.alibabacloud.com/document/Alidns/2015-01-09/CreateCloudGtmMonitorTemplate).
 *
 * > **NOTE:** Available since v1.277.0.
 *
 * ## Example Usage
 *
 * Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const _default = new alicloud.alidnscloudgtm.MonitorTemplate("default", {
 *     name: name,
 *     protocol: "http",
 *     ipVersion: "IPv4",
 *     interval: "60",
 *     timeout: "2000",
 *     evaluationCount: 2,
 *     failureRate: 50,
 *     extendInfo: JSON.stringify({
 *         code: 500,
 *         followRedirect: true,
 *         path: "/",
 *     }),
 *     remark: "terraform-example-remark",
 *     ispCityNodes: [
 *         {
 *             cityCode: "357",
 *             ispCode: "465",
 *         },
 *         {
 *             cityCode: "738",
 *             ispCode: "465",
 *         },
 *     ],
 * });
 * ```
 *
 * 📚 Need more examples? VIEW MORE EXAMPLES
 *
 * ## Import
 *
 * Alidns Cloud Gtm Monitor Template can be imported using the id, e.g.
 *
 * ```sh
 * $ pulumi import alicloud:alidnscloudgtm/monitorTemplate:MonitorTemplate example <template_id>
 * ```
 */
export class MonitorTemplate extends pulumi.CustomResource {
    /**
     * Get an existing MonitorTemplate resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: MonitorTemplateState, opts?: pulumi.CustomResourceOptions): MonitorTemplate {
        return new MonitorTemplate(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'alicloud:alidnscloudgtm/monitorTemplate:MonitorTemplate';

    /**
     * Returns true if the given object is an instance of MonitorTemplate.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is MonitorTemplate {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === MonitorTemplate.__pulumiType;
    }

    /**
     * The number of retries after a probe failure. A service is marked abnormal only after this many consecutive failures, preventing transient network fluctuations from triggering false alarms. Valid values: `0`, `1`, `2`, `3`.
     */
    declare public readonly evaluationCount: pulumi.Output<number>;
    /**
     * A JSON string containing protocol-specific probe configuration. The supported keys depend on `protocol`. See `extendInfo` below.
     */
    declare public readonly extendInfo: pulumi.Output<string>;
    /**
     * The failure-rate threshold (%) among selected probe nodes. If the percentage of failing nodes exceeds this value, the service address is marked as abnormal. Valid values: `0`, `20`, `50`, `80`, `100`.
     */
    declare public readonly failureRate: pulumi.Output<number>;
    /**
     * The interval between consecutive probes, in seconds. Valid values: `15`, `60`, `300`, `900`, `1800`, `3600`. The `15` seconds interval is only available for Flagship Edition instances.
     */
    declare public readonly interval: pulumi.Output<string>;
    /**
     * The IP version of the probing node. Valid values: `IPv4`, `IPv6`.
     */
    declare public readonly ipVersion: pulumi.Output<string>;
    /**
     * The set of monitoring nodes that this template will probe from. Use the [ListCloudGtmMonitorNodes](https://help.aliyun.com/document_detail/2797349.html) API to look up available `cityCode` / `ispCode` combinations. See `ispCityNodes` below.
     */
    declare public readonly ispCityNodes: pulumi.Output<outputs.alidnscloudgtm.MonitorTemplateIspCityNode[]>;
    /**
     * The name of the monitor template. It is recommended to use a name that reflects the health-check protocol for easier identification.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * The probing protocol of the template. Valid values: `ping`, `tcp`, `http`, `https`.
     */
    declare public readonly protocol: pulumi.Output<string>;
    /**
     * The remark of the monitor template. Passing an empty value clears the existing remark.
     */
    declare public readonly remark: pulumi.Output<string | undefined>;
    /**
     * Probe request timeout, in milliseconds. Probe packets that do not return within this duration are treated as timeouts. Valid values: `2000`, `3000`, `5000`, `10000`.
     */
    declare public readonly timeout: pulumi.Output<string>;

    /**
     * Create a MonitorTemplate resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: MonitorTemplateArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: MonitorTemplateArgs | MonitorTemplateState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as MonitorTemplateState | undefined;
            resourceInputs["evaluationCount"] = state?.evaluationCount;
            resourceInputs["extendInfo"] = state?.extendInfo;
            resourceInputs["failureRate"] = state?.failureRate;
            resourceInputs["interval"] = state?.interval;
            resourceInputs["ipVersion"] = state?.ipVersion;
            resourceInputs["ispCityNodes"] = state?.ispCityNodes;
            resourceInputs["name"] = state?.name;
            resourceInputs["protocol"] = state?.protocol;
            resourceInputs["remark"] = state?.remark;
            resourceInputs["timeout"] = state?.timeout;
        } else {
            const args = argsOrState as MonitorTemplateArgs | undefined;
            if (args?.evaluationCount === undefined && !opts.urn) {
                throw new Error("Missing required property 'evaluationCount'");
            }
            if (args?.failureRate === undefined && !opts.urn) {
                throw new Error("Missing required property 'failureRate'");
            }
            if (args?.interval === undefined && !opts.urn) {
                throw new Error("Missing required property 'interval'");
            }
            if (args?.ipVersion === undefined && !opts.urn) {
                throw new Error("Missing required property 'ipVersion'");
            }
            if (args?.ispCityNodes === undefined && !opts.urn) {
                throw new Error("Missing required property 'ispCityNodes'");
            }
            if (args?.protocol === undefined && !opts.urn) {
                throw new Error("Missing required property 'protocol'");
            }
            if (args?.timeout === undefined && !opts.urn) {
                throw new Error("Missing required property 'timeout'");
            }
            resourceInputs["evaluationCount"] = args?.evaluationCount;
            resourceInputs["extendInfo"] = args?.extendInfo;
            resourceInputs["failureRate"] = args?.failureRate;
            resourceInputs["interval"] = args?.interval;
            resourceInputs["ipVersion"] = args?.ipVersion;
            resourceInputs["ispCityNodes"] = args?.ispCityNodes;
            resourceInputs["name"] = args?.name;
            resourceInputs["protocol"] = args?.protocol;
            resourceInputs["remark"] = args?.remark;
            resourceInputs["timeout"] = args?.timeout;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(MonitorTemplate.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering MonitorTemplate resources.
 */
export interface MonitorTemplateState {
    /**
     * The number of retries after a probe failure. A service is marked abnormal only after this many consecutive failures, preventing transient network fluctuations from triggering false alarms. Valid values: `0`, `1`, `2`, `3`.
     */
    evaluationCount?: pulumi.Input<number>;
    /**
     * A JSON string containing protocol-specific probe configuration. The supported keys depend on `protocol`. See `extendInfo` below.
     */
    extendInfo?: pulumi.Input<string>;
    /**
     * The failure-rate threshold (%) among selected probe nodes. If the percentage of failing nodes exceeds this value, the service address is marked as abnormal. Valid values: `0`, `20`, `50`, `80`, `100`.
     */
    failureRate?: pulumi.Input<number>;
    /**
     * The interval between consecutive probes, in seconds. Valid values: `15`, `60`, `300`, `900`, `1800`, `3600`. The `15` seconds interval is only available for Flagship Edition instances.
     */
    interval?: pulumi.Input<string>;
    /**
     * The IP version of the probing node. Valid values: `IPv4`, `IPv6`.
     */
    ipVersion?: pulumi.Input<string>;
    /**
     * The set of monitoring nodes that this template will probe from. Use the [ListCloudGtmMonitorNodes](https://help.aliyun.com/document_detail/2797349.html) API to look up available `cityCode` / `ispCode` combinations. See `ispCityNodes` below.
     */
    ispCityNodes?: pulumi.Input<pulumi.Input<inputs.alidnscloudgtm.MonitorTemplateIspCityNode>[]>;
    /**
     * The name of the monitor template. It is recommended to use a name that reflects the health-check protocol for easier identification.
     */
    name?: pulumi.Input<string>;
    /**
     * The probing protocol of the template. Valid values: `ping`, `tcp`, `http`, `https`.
     */
    protocol?: pulumi.Input<string>;
    /**
     * The remark of the monitor template. Passing an empty value clears the existing remark.
     */
    remark?: pulumi.Input<string>;
    /**
     * Probe request timeout, in milliseconds. Probe packets that do not return within this duration are treated as timeouts. Valid values: `2000`, `3000`, `5000`, `10000`.
     */
    timeout?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a MonitorTemplate resource.
 */
export interface MonitorTemplateArgs {
    /**
     * The number of retries after a probe failure. A service is marked abnormal only after this many consecutive failures, preventing transient network fluctuations from triggering false alarms. Valid values: `0`, `1`, `2`, `3`.
     */
    evaluationCount: pulumi.Input<number>;
    /**
     * A JSON string containing protocol-specific probe configuration. The supported keys depend on `protocol`. See `extendInfo` below.
     */
    extendInfo?: pulumi.Input<string>;
    /**
     * The failure-rate threshold (%) among selected probe nodes. If the percentage of failing nodes exceeds this value, the service address is marked as abnormal. Valid values: `0`, `20`, `50`, `80`, `100`.
     */
    failureRate: pulumi.Input<number>;
    /**
     * The interval between consecutive probes, in seconds. Valid values: `15`, `60`, `300`, `900`, `1800`, `3600`. The `15` seconds interval is only available for Flagship Edition instances.
     */
    interval: pulumi.Input<string>;
    /**
     * The IP version of the probing node. Valid values: `IPv4`, `IPv6`.
     */
    ipVersion: pulumi.Input<string>;
    /**
     * The set of monitoring nodes that this template will probe from. Use the [ListCloudGtmMonitorNodes](https://help.aliyun.com/document_detail/2797349.html) API to look up available `cityCode` / `ispCode` combinations. See `ispCityNodes` below.
     */
    ispCityNodes: pulumi.Input<pulumi.Input<inputs.alidnscloudgtm.MonitorTemplateIspCityNode>[]>;
    /**
     * The name of the monitor template. It is recommended to use a name that reflects the health-check protocol for easier identification.
     */
    name?: pulumi.Input<string>;
    /**
     * The probing protocol of the template. Valid values: `ping`, `tcp`, `http`, `https`.
     */
    protocol: pulumi.Input<string>;
    /**
     * The remark of the monitor template. Passing an empty value clears the existing remark.
     */
    remark?: pulumi.Input<string>;
    /**
     * Probe request timeout, in milliseconds. Probe packets that do not return within this duration are treated as timeouts. Valid values: `2000`, `3000`, `5000`, `10000`.
     */
    timeout: pulumi.Input<string>;
}
