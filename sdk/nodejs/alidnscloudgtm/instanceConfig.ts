// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provides a Alidns Cloud Gtm Instance Config resource.
 *
 * CloudGtm Instance Configuration  .
 *
 * For information about Alidns Cloud Gtm Instance Config and how to use it, see [What is Cloud Gtm Instance Config](https://next.api.alibabacloud.com/document/Alidns/2015-01-09/CreateCloudGtmInstanceConfig).
 *
 * > **NOTE:** Available since v1.277.0.
 *
 * ## Example Usage
 *
 * Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const _default = new alicloud.dns.AlidnsDomain("default", {domainName: `${name}.abc`});
 * const defaultInstanceConfig = new alicloud.alidnscloudgtm.InstanceConfig("default", {
 *     addressPoolLbStrategy: "sequence",
 *     scheduleRrType: "A",
 *     scheduleZoneName: _default.domainName,
 *     enableStatus: "disable",
 *     scheduleHostName: "example",
 *     scheduleZoneMode: "custom",
 *     ttl: 600,
 *     sequenceLbStrategyMode: "preemptive",
 *     remark: "remark",
 * });
 * ```
 *
 * 📚 Need more examples? VIEW MORE EXAMPLES
 *
 * ## Import
 *
 * Alidns Cloud Gtm Instance Config can be imported using the id, e.g.
 *
 * ```sh
 * $ pulumi import alicloud:alidnscloudgtm/instanceConfig:InstanceConfig example <config_id>:<instance_id>
 * ```
 */
export class InstanceConfig extends pulumi.CustomResource {
    /**
     * Get an existing InstanceConfig resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: InstanceConfigState, opts?: pulumi.CustomResourceOptions): InstanceConfig {
        return new InstanceConfig(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'alicloud:alidnscloudgtm/instanceConfig:InstanceConfig';

    /**
     * Returns true if the given object is an instance of InstanceConfig.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is InstanceConfig {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === InstanceConfig.__pulumiType;
    }

    /**
     * The load balancing strategy among address pools:
     * - round_robin: Round-robin. For any incoming DNS resolution request, all address pools are returned, and the order of these address pools is rotated with each request.
     * - sequence: Sequential. For any incoming DNS resolution request, the address pool with the smallest sequence number is returned (the sequence number indicates the priority of the address pool, where a smaller number means higher priority). If the address pool with the smallest sequence number is unavailable, the next address pool with the next smallest sequence number is returned.
     * - weight: Weighted. You can assign different weights to each address pool so that DNS queries return address pools in proportion to their weights.
     * - source_nearest: Source proximity. This is the intelligent DNS resolution feature. GTM returns different address pools based on the geographic location of the DNS query source, enabling users to access the nearest available endpoint.
     */
    declare public readonly addressPoolLbStrategy: pulumi.Output<string | undefined>;
    /**
     * The domain name instance configuration ID.
     */
    declare public /*out*/ readonly configId: pulumi.Output<string>;
    /**
     * The enable status of the domain instance:
     * - enable: Enabled. The GTM instance's intelligent scheduling policy is active.
     * - disable: Disabled. The GTM instance's intelligent scheduling policy is inactive.
     */
    declare public readonly enableStatus: pulumi.Output<string>;
    /**
     * The instance ID associated with the GTM 3.
     */
    declare public /*out*/ readonly instanceId: pulumi.Output<string>;
    /**
     * Remarks. The provided parameter value becomes the updated remark content.
     */
    declare public readonly remark: pulumi.Output<string | undefined>;
    /**
     * The host record of the GTM access domain.
     */
    declare public readonly scheduleHostName: pulumi.Output<string | undefined>;
    /**
     * Record type for the access domain name:  
     * - A: IPv4 address
     * - AAAA: IPv6 address
     * - CNAME: domain name.
     */
    declare public readonly scheduleRrType: pulumi.Output<string>;
    /**
     * Access domain name assignment mode:  
     * - custom: Custom access domain name. You define the host record and associate it with a primary or subdomain under the same account as the GTM instance to generate the access domain name.
     * - sys_assign: System-assigned access domain name. This mode is no longer supported. Do not select it.
     */
    declare public readonly scheduleZoneMode: pulumi.Output<string>;
    /**
     * Zone name, which is the parent zone of the GTM access domain name. It is typically a hosted domain under the same account as the GTM instance in the Alibaba Cloud DNS console, and can be either a primary domain or a subdomain.
     */
    declare public readonly scheduleZoneName: pulumi.Output<string | undefined>;
    /**
     * When the load balancing policy between address pools is set to sequential mode, the service restoration mode for preceding resources after an anomaly is resolved is as follows:  
     * - preemptive: Preemptive mode. When a preceding resource recovers, the address pool with the smaller sequence number is prioritized.
     * - non_preemptive: Non-preemptive mode. When a preceding resource recovers, the current address pool continues to be used.
     */
    declare public readonly sequenceLbStrategyMode: pulumi.Output<string | undefined>;
    /**
     * Global TTL (in seconds). This value determines how long the DNS records resolving the access domain name to addresses in the address pool are cached by the ISP's LocalDNS. Custom TTL values are supported.
     */
    declare public readonly ttl: pulumi.Output<number>;

    /**
     * Create a InstanceConfig resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: InstanceConfigArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: InstanceConfigArgs | InstanceConfigState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as InstanceConfigState | undefined;
            resourceInputs["addressPoolLbStrategy"] = state?.addressPoolLbStrategy;
            resourceInputs["configId"] = state?.configId;
            resourceInputs["enableStatus"] = state?.enableStatus;
            resourceInputs["instanceId"] = state?.instanceId;
            resourceInputs["remark"] = state?.remark;
            resourceInputs["scheduleHostName"] = state?.scheduleHostName;
            resourceInputs["scheduleRrType"] = state?.scheduleRrType;
            resourceInputs["scheduleZoneMode"] = state?.scheduleZoneMode;
            resourceInputs["scheduleZoneName"] = state?.scheduleZoneName;
            resourceInputs["sequenceLbStrategyMode"] = state?.sequenceLbStrategyMode;
            resourceInputs["ttl"] = state?.ttl;
        } else {
            const args = argsOrState as InstanceConfigArgs | undefined;
            if (args?.enableStatus === undefined && !opts.urn) {
                throw new Error("Missing required property 'enableStatus'");
            }
            if (args?.scheduleRrType === undefined && !opts.urn) {
                throw new Error("Missing required property 'scheduleRrType'");
            }
            if (args?.scheduleZoneMode === undefined && !opts.urn) {
                throw new Error("Missing required property 'scheduleZoneMode'");
            }
            if (args?.ttl === undefined && !opts.urn) {
                throw new Error("Missing required property 'ttl'");
            }
            resourceInputs["addressPoolLbStrategy"] = args?.addressPoolLbStrategy;
            resourceInputs["enableStatus"] = args?.enableStatus;
            resourceInputs["remark"] = args?.remark;
            resourceInputs["scheduleHostName"] = args?.scheduleHostName;
            resourceInputs["scheduleRrType"] = args?.scheduleRrType;
            resourceInputs["scheduleZoneMode"] = args?.scheduleZoneMode;
            resourceInputs["scheduleZoneName"] = args?.scheduleZoneName;
            resourceInputs["sequenceLbStrategyMode"] = args?.sequenceLbStrategyMode;
            resourceInputs["ttl"] = args?.ttl;
            resourceInputs["configId"] = undefined /*out*/;
            resourceInputs["instanceId"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(InstanceConfig.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering InstanceConfig resources.
 */
export interface InstanceConfigState {
    /**
     * The load balancing strategy among address pools:
     * - round_robin: Round-robin. For any incoming DNS resolution request, all address pools are returned, and the order of these address pools is rotated with each request.
     * - sequence: Sequential. For any incoming DNS resolution request, the address pool with the smallest sequence number is returned (the sequence number indicates the priority of the address pool, where a smaller number means higher priority). If the address pool with the smallest sequence number is unavailable, the next address pool with the next smallest sequence number is returned.
     * - weight: Weighted. You can assign different weights to each address pool so that DNS queries return address pools in proportion to their weights.
     * - source_nearest: Source proximity. This is the intelligent DNS resolution feature. GTM returns different address pools based on the geographic location of the DNS query source, enabling users to access the nearest available endpoint.
     */
    addressPoolLbStrategy?: pulumi.Input<string>;
    /**
     * The domain name instance configuration ID.
     */
    configId?: pulumi.Input<string>;
    /**
     * The enable status of the domain instance:
     * - enable: Enabled. The GTM instance's intelligent scheduling policy is active.
     * - disable: Disabled. The GTM instance's intelligent scheduling policy is inactive.
     */
    enableStatus?: pulumi.Input<string>;
    /**
     * The instance ID associated with the GTM 3.
     */
    instanceId?: pulumi.Input<string>;
    /**
     * Remarks. The provided parameter value becomes the updated remark content.
     */
    remark?: pulumi.Input<string>;
    /**
     * The host record of the GTM access domain.
     */
    scheduleHostName?: pulumi.Input<string>;
    /**
     * Record type for the access domain name:  
     * - A: IPv4 address
     * - AAAA: IPv6 address
     * - CNAME: domain name.
     */
    scheduleRrType?: pulumi.Input<string>;
    /**
     * Access domain name assignment mode:  
     * - custom: Custom access domain name. You define the host record and associate it with a primary or subdomain under the same account as the GTM instance to generate the access domain name.
     * - sys_assign: System-assigned access domain name. This mode is no longer supported. Do not select it.
     */
    scheduleZoneMode?: pulumi.Input<string>;
    /**
     * Zone name, which is the parent zone of the GTM access domain name. It is typically a hosted domain under the same account as the GTM instance in the Alibaba Cloud DNS console, and can be either a primary domain or a subdomain.
     */
    scheduleZoneName?: pulumi.Input<string>;
    /**
     * When the load balancing policy between address pools is set to sequential mode, the service restoration mode for preceding resources after an anomaly is resolved is as follows:  
     * - preemptive: Preemptive mode. When a preceding resource recovers, the address pool with the smaller sequence number is prioritized.
     * - non_preemptive: Non-preemptive mode. When a preceding resource recovers, the current address pool continues to be used.
     */
    sequenceLbStrategyMode?: pulumi.Input<string>;
    /**
     * Global TTL (in seconds). This value determines how long the DNS records resolving the access domain name to addresses in the address pool are cached by the ISP's LocalDNS. Custom TTL values are supported.
     */
    ttl?: pulumi.Input<number>;
}

/**
 * The set of arguments for constructing a InstanceConfig resource.
 */
export interface InstanceConfigArgs {
    /**
     * The load balancing strategy among address pools:
     * - round_robin: Round-robin. For any incoming DNS resolution request, all address pools are returned, and the order of these address pools is rotated with each request.
     * - sequence: Sequential. For any incoming DNS resolution request, the address pool with the smallest sequence number is returned (the sequence number indicates the priority of the address pool, where a smaller number means higher priority). If the address pool with the smallest sequence number is unavailable, the next address pool with the next smallest sequence number is returned.
     * - weight: Weighted. You can assign different weights to each address pool so that DNS queries return address pools in proportion to their weights.
     * - source_nearest: Source proximity. This is the intelligent DNS resolution feature. GTM returns different address pools based on the geographic location of the DNS query source, enabling users to access the nearest available endpoint.
     */
    addressPoolLbStrategy?: pulumi.Input<string>;
    /**
     * The enable status of the domain instance:
     * - enable: Enabled. The GTM instance's intelligent scheduling policy is active.
     * - disable: Disabled. The GTM instance's intelligent scheduling policy is inactive.
     */
    enableStatus: pulumi.Input<string>;
    /**
     * Remarks. The provided parameter value becomes the updated remark content.
     */
    remark?: pulumi.Input<string>;
    /**
     * The host record of the GTM access domain.
     */
    scheduleHostName?: pulumi.Input<string>;
    /**
     * Record type for the access domain name:  
     * - A: IPv4 address
     * - AAAA: IPv6 address
     * - CNAME: domain name.
     */
    scheduleRrType: pulumi.Input<string>;
    /**
     * Access domain name assignment mode:  
     * - custom: Custom access domain name. You define the host record and associate it with a primary or subdomain under the same account as the GTM instance to generate the access domain name.
     * - sys_assign: System-assigned access domain name. This mode is no longer supported. Do not select it.
     */
    scheduleZoneMode: pulumi.Input<string>;
    /**
     * Zone name, which is the parent zone of the GTM access domain name. It is typically a hosted domain under the same account as the GTM instance in the Alibaba Cloud DNS console, and can be either a primary domain or a subdomain.
     */
    scheduleZoneName?: pulumi.Input<string>;
    /**
     * When the load balancing policy between address pools is set to sequential mode, the service restoration mode for preceding resources after an anomaly is resolved is as follows:  
     * - preemptive: Preemptive mode. When a preceding resource recovers, the address pool with the smaller sequence number is prioritized.
     * - non_preemptive: Non-preemptive mode. When a preceding resource recovers, the current address pool continues to be used.
     */
    sequenceLbStrategyMode?: pulumi.Input<string>;
    /**
     * Global TTL (in seconds). This value determines how long the DNS records resolving the access domain name to addresses in the address pool are cached by the ISP's LocalDNS. Custom TTL values are supported.
     */
    ttl: pulumi.Input<number>;
}
