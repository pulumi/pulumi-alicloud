// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provides a Certificate Management Service (Original SSL Certificate) Contact resource.
 *
 * Certificate Contact Person.
 *
 * For information about Certificate Management Service (Original SSL Certificate) Contact and how to use it, see [What is Contact](https://next.api.alibabacloud.com/document/cas/2020-04-07/CreateContact).
 *
 * > **NOTE:** Available since v1.285.0.
 *
 * ## Example Usage
 *
 * Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const _default = new alicloud.sslcertificatesservice.Contact("default", {
 *     name: name,
 *     mobile: "13312345678",
 *     email: "test@example.com",
 *     webhookLists: ["https://oapi.dingtalk.com/robot/send?access_token=xxxx"],
 * });
 * ```
 *
 * 📚 Need more examples? VIEW MORE EXAMPLES
 *
 * ## Import
 *
 * Certificate Management Service (Original SSL Certificate) Contact can be imported using the id, e.g.
 *
 * ```sh
 * $ pulumi import alicloud:sslcertificatesservice/contact:Contact example <contact_id>
 * ```
 */
export class Contact extends pulumi.CustomResource {
    /**
     * Get an existing Contact resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: ContactState, opts?: pulumi.CustomResourceOptions): Contact {
        return new Contact(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'alicloud:sslcertificatesservice/contact:Contact';

    /**
     * Returns true if the given object is an instance of Contact.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is Contact {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === Contact.__pulumiType;
    }

    /**
     * The email address of the contact.
     */
    declare public readonly email: pulumi.Output<string | undefined>;
    /**
     * The ID card number of the contact. CFCA only consumes the idcard when the contact is used to apply for a CFCA-brand certificate; other brands ignore it. CreateContact/UpdateContact accept idcard on any account.
     */
    declare public readonly idcard: pulumi.Output<string | undefined>;
    /**
     * The mobile phone number of the contact.
     */
    declare public readonly mobile: pulumi.Output<string>;
    /**
     * The name of the contact.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * A list of DingTalk robot Webhook URLs used to receive notifications, e.g. `["https://oapi.dingtalk.com/robot/send?access_token=xxx"]`. Each element must be a DingTalk robot URL (`https://oapi.dingtalk.com/robot/send?access_token=...`); other URLs are rejected by the API.
     *
     * > **NOTE:** `email`, `mobile`, and `idcard` are sensitive. The CAS API returns these fields server-masked on read (e.g. `tes****1@example.com`, `133******78`), so they are not reconciled from the API after apply: the configured value is authoritative, out-of-band changes (e.g. editing in the console) are not detectable by Terraform, and `pulumi import` will not populate them (the first plan after import reports them being set). `webhookList` is read back in plaintext from the API.
     */
    declare public readonly webhookLists: pulumi.Output<string[] | undefined>;

    /**
     * Create a Contact resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: ContactArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: ContactArgs | ContactState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as ContactState | undefined;
            resourceInputs["email"] = state?.email;
            resourceInputs["idcard"] = state?.idcard;
            resourceInputs["mobile"] = state?.mobile;
            resourceInputs["name"] = state?.name;
            resourceInputs["webhookLists"] = state?.webhookLists;
        } else {
            const args = argsOrState as ContactArgs | undefined;
            if (args?.mobile === undefined && !opts.urn) {
                throw new Error("Missing required property 'mobile'");
            }
            resourceInputs["email"] = args?.email ? pulumi.secret(args.email) : undefined;
            resourceInputs["idcard"] = args?.idcard ? pulumi.secret(args.idcard) : undefined;
            resourceInputs["mobile"] = args?.mobile ? pulumi.secret(args.mobile) : undefined;
            resourceInputs["name"] = args?.name;
            resourceInputs["webhookLists"] = args?.webhookLists;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        const secretOpts = { additionalSecretOutputs: ["email", "idcard", "mobile"] };
        opts = pulumi.mergeOptions(opts, secretOpts);
        super(Contact.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering Contact resources.
 */
export interface ContactState {
    /**
     * The email address of the contact.
     */
    email?: pulumi.Input<string | undefined>;
    /**
     * The ID card number of the contact. CFCA only consumes the idcard when the contact is used to apply for a CFCA-brand certificate; other brands ignore it. CreateContact/UpdateContact accept idcard on any account.
     */
    idcard?: pulumi.Input<string | undefined>;
    /**
     * The mobile phone number of the contact.
     */
    mobile?: pulumi.Input<string | undefined>;
    /**
     * The name of the contact.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * A list of DingTalk robot Webhook URLs used to receive notifications, e.g. `["https://oapi.dingtalk.com/robot/send?access_token=xxx"]`. Each element must be a DingTalk robot URL (`https://oapi.dingtalk.com/robot/send?access_token=...`); other URLs are rejected by the API.
     *
     * > **NOTE:** `email`, `mobile`, and `idcard` are sensitive. The CAS API returns these fields server-masked on read (e.g. `tes****1@example.com`, `133******78`), so they are not reconciled from the API after apply: the configured value is authoritative, out-of-band changes (e.g. editing in the console) are not detectable by Terraform, and `pulumi import` will not populate them (the first plan after import reports them being set). `webhookList` is read back in plaintext from the API.
     */
    webhookLists?: pulumi.Input<pulumi.Input<string>[] | undefined>;
}

/**
 * The set of arguments for constructing a Contact resource.
 */
export interface ContactArgs {
    /**
     * The email address of the contact.
     */
    email?: pulumi.Input<string | undefined>;
    /**
     * The ID card number of the contact. CFCA only consumes the idcard when the contact is used to apply for a CFCA-brand certificate; other brands ignore it. CreateContact/UpdateContact accept idcard on any account.
     */
    idcard?: pulumi.Input<string | undefined>;
    /**
     * The mobile phone number of the contact.
     */
    mobile: pulumi.Input<string>;
    /**
     * The name of the contact.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * A list of DingTalk robot Webhook URLs used to receive notifications, e.g. `["https://oapi.dingtalk.com/robot/send?access_token=xxx"]`. Each element must be a DingTalk robot URL (`https://oapi.dingtalk.com/robot/send?access_token=...`); other URLs are rejected by the API.
     *
     * > **NOTE:** `email`, `mobile`, and `idcard` are sensitive. The CAS API returns these fields server-masked on read (e.g. `tes****1@example.com`, `133******78`), so they are not reconciled from the API after apply: the configured value is authoritative, out-of-band changes (e.g. editing in the console) are not detectable by Terraform, and `pulumi import` will not populate them (the first plan after import reports them being set). `webhookList` is read back in plaintext from the API.
     */
    webhookLists?: pulumi.Input<pulumi.Input<string>[] | undefined>;
}
