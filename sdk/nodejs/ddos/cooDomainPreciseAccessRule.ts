// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * Provides a DdosCoo Domain Precise Access Rule resource.
 *
 * Precise access control rules for website business.
 *
 * For information about DdosCoo Domain Precise Access Rule and how to use it, see [What is Domain Precise Access Rule](https://next.api.alibabacloud.com/document/ddoscoo/2020-01-01/ModifyWebPreciseAccessRule).
 *
 * > **NOTE:** Available since v1.271.0.
 *
 * ## Example Usage
 *
 * Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform";
 * const domain = config.get("domain") || "terraform-example.alibaba.com";
 * const _default = alicloud.ddos.getDdosCooInstances({});
 * const defaultDomainResource = new alicloud.ddos.DomainResource("default", {
 *     domain: domain,
 *     instanceIds: [_default.then(_default => _default.ids?.[0])],
 *     proxyTypes: [{
 *         proxyPorts: [443],
 *         proxyType: "https",
 *     }],
 *     realServers: ["177.167.32.11"],
 *     rsType: 0,
 * });
 * const defaultCooDomainPreciseAccessRule = new alicloud.ddos.CooDomainPreciseAccessRule("default", {
 *     conditions: [{
 *         matchMethod: "contain",
 *         field: "header",
 *         content: "222",
 *         headerName: "15",
 *     }],
 *     action: "accept",
 *     expires: 0,
 *     domain: defaultDomainResource.id,
 *     name: name,
 * });
 * ```
 *
 * 📚 Need more examples? VIEW MORE EXAMPLES
 *
 * ## Import
 *
 * DdosCoo Domain Precise Access Rule can be imported using the id, e.g.
 *
 * ```sh
 * $ pulumi import alicloud:ddos/cooDomainPreciseAccessRule:CooDomainPreciseAccessRule example <domain>:<name>
 * ```
 */
export class CooDomainPreciseAccessRule extends pulumi.CustomResource {
    /**
     * Get an existing CooDomainPreciseAccessRule resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: CooDomainPreciseAccessRuleState, opts?: pulumi.CustomResourceOptions): CooDomainPreciseAccessRule {
        return new CooDomainPreciseAccessRule(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'alicloud:ddos/cooDomainPreciseAccessRule:CooDomainPreciseAccessRule';

    /**
     * Returns true if the given object is an instance of CooDomainPreciseAccessRule.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is CooDomainPreciseAccessRule {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === CooDomainPreciseAccessRule.__pulumiType;
    }

    /**
     * Action to take on match. Valid values:
     */
    declare public readonly action: pulumi.Output<string>;
    /**
     * List of matching conditions. See `condition` below.
     */
    declare public readonly conditions: pulumi.Output<outputs.ddos.CooDomainPreciseAccessRuleCondition[]>;
    /**
     * Domain name of the website service.
     * > **NOTE:**  The domain name must already have a website service forwarding rule configured. You can call [DescribeDomains](https://help.aliyun.com/document_detail/91724.html) to query all domain names.
     */
    declare public readonly domain: pulumi.Output<string>;
    /**
     * Rule validity period, in seconds. This parameter takes effect only when the rule's matching action is set to block (`action` is `block`), blocking access requests during the validity period. If this parameter is not specified, the rule remains effective permanently.
     */
    declare public readonly expires: pulumi.Output<number | undefined>;
    /**
     * Rule name.
     */
    declare public readonly name: pulumi.Output<string>;

    /**
     * Create a CooDomainPreciseAccessRule resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: CooDomainPreciseAccessRuleArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: CooDomainPreciseAccessRuleArgs | CooDomainPreciseAccessRuleState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as CooDomainPreciseAccessRuleState | undefined;
            resourceInputs["action"] = state?.action;
            resourceInputs["conditions"] = state?.conditions;
            resourceInputs["domain"] = state?.domain;
            resourceInputs["expires"] = state?.expires;
            resourceInputs["name"] = state?.name;
        } else {
            const args = argsOrState as CooDomainPreciseAccessRuleArgs | undefined;
            if (args?.action === undefined && !opts.urn) {
                throw new Error("Missing required property 'action'");
            }
            if (args?.conditions === undefined && !opts.urn) {
                throw new Error("Missing required property 'conditions'");
            }
            if (args?.domain === undefined && !opts.urn) {
                throw new Error("Missing required property 'domain'");
            }
            resourceInputs["action"] = args?.action;
            resourceInputs["conditions"] = args?.conditions;
            resourceInputs["domain"] = args?.domain;
            resourceInputs["expires"] = args?.expires;
            resourceInputs["name"] = args?.name;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(CooDomainPreciseAccessRule.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering CooDomainPreciseAccessRule resources.
 */
export interface CooDomainPreciseAccessRuleState {
    /**
     * Action to take on match. Valid values:
     */
    action?: pulumi.Input<string>;
    /**
     * List of matching conditions. See `condition` below.
     */
    conditions?: pulumi.Input<pulumi.Input<inputs.ddos.CooDomainPreciseAccessRuleCondition>[]>;
    /**
     * Domain name of the website service.
     * > **NOTE:**  The domain name must already have a website service forwarding rule configured. You can call [DescribeDomains](https://help.aliyun.com/document_detail/91724.html) to query all domain names.
     */
    domain?: pulumi.Input<string>;
    /**
     * Rule validity period, in seconds. This parameter takes effect only when the rule's matching action is set to block (`action` is `block`), blocking access requests during the validity period. If this parameter is not specified, the rule remains effective permanently.
     */
    expires?: pulumi.Input<number>;
    /**
     * Rule name.
     */
    name?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a CooDomainPreciseAccessRule resource.
 */
export interface CooDomainPreciseAccessRuleArgs {
    /**
     * Action to take on match. Valid values:
     */
    action: pulumi.Input<string>;
    /**
     * List of matching conditions. See `condition` below.
     */
    conditions: pulumi.Input<pulumi.Input<inputs.ddos.CooDomainPreciseAccessRuleCondition>[]>;
    /**
     * Domain name of the website service.
     * > **NOTE:**  The domain name must already have a website service forwarding rule configured. You can call [DescribeDomains](https://help.aliyun.com/document_detail/91724.html) to query all domain names.
     */
    domain: pulumi.Input<string>;
    /**
     * Rule validity period, in seconds. This parameter takes effect only when the rule's matching action is set to block (`action` is `block`), blocking access requests during the validity period. If this parameter is not specified, the rule remains effective permanently.
     */
    expires?: pulumi.Input<number>;
    /**
     * Rule name.
     */
    name?: pulumi.Input<string>;
}
