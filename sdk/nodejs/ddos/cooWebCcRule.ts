// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * Provides a DdosCoo Web Cc Rule resource.
 *
 * CC frequency control rules.
 *
 * For information about DdosCoo Web Cc Rule and how to use it, see [What is Web Cc Rule](https://next.api.alibabacloud.com/document/ddoscoo/2020-01-01/ConfigWebCCRuleV2).
 *
 * > **NOTE:** Available since v1.271.0.
 *
 * ## Example Usage
 *
 * Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform";
 * const domain = config.get("domain") || "terraform-example.alibaba.com";
 * const _default = alicloud.ddos.getDdosCooInstances({});
 * const defaultDomainResource = new alicloud.ddos.DomainResource("default", {
 *     domain: domain,
 *     instanceIds: [_default.then(_default => _default.ids?.[0])],
 *     proxyTypes: [{
 *         proxyPorts: [443],
 *         proxyType: "https",
 *     }],
 *     realServers: ["177.167.32.11"],
 *     rsType: 0,
 * });
 * const defaultCooWebCcRule = new alicloud.ddos.CooWebCcRule("default", {
 *     ruleDetail: {
 *         action: "block",
 *         rateLimit: {
 *             interval: 11,
 *             threshold: 2,
 *             ttl: 840,
 *             target: "header",
 *             subKey: "33",
 *         },
 *         conditions: [
 *             {
 *                 matchMethod: "belong",
 *                 field: "ip",
 *                 content: "1.1.1.1",
 *             },
 *             {
 *                 matchMethod: "contain",
 *                 field: "uri",
 *                 content: "/a",
 *             },
 *             {
 *                 matchMethod: "contain",
 *                 field: "header",
 *                 headerName: "123",
 *                 content: "1234",
 *             },
 *         ],
 *         statistics: {
 *             mode: "distinct",
 *             field: "header",
 *             headerName: "12",
 *         },
 *         statusCode: {
 *             enabled: true,
 *             code: 100,
 *             useRatio: false,
 *             countThreshold: 2,
 *             ratioThreshold: 5,
 *         },
 *     },
 *     name: name,
 *     domain: defaultDomainResource.id,
 * });
 * ```
 *
 * 📚 Need more examples? VIEW MORE EXAMPLES
 *
 * ## Import
 *
 * DdosCoo Web Cc Rule can be imported using the id, e.g.
 *
 * ```sh
 * $ pulumi import alicloud:ddos/cooWebCcRule:CooWebCcRule example <domain>:<name>
 * ```
 */
export class CooWebCcRule extends pulumi.CustomResource {
    /**
     * Get an existing CooWebCcRule resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: CooWebCcRuleState, opts?: pulumi.CustomResourceOptions): CooWebCcRule {
        return new CooWebCcRule(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'alicloud:ddos/cooWebCcRule:CooWebCcRule';

    /**
     * Returns true if the given object is an instance of CooWebCcRule.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is CooWebCcRule {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === CooWebCcRule.__pulumiType;
    }

    /**
     * The domain name of the website service.  
     *
     * > **NOTE:**  The domain name must already have website service forwarding rules configured. You can call [DescribeDomains](https://help.aliyun.com/document_detail/91724.html) to query all domain names.
     */
    declare public readonly domain: pulumi.Output<string>;
    /**
     * Rule name.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * Rule details.   See `ruleDetail` below.
     */
    declare public readonly ruleDetail: pulumi.Output<outputs.ddos.CooWebCcRuleRuleDetail>;

    /**
     * Create a CooWebCcRule resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: CooWebCcRuleArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: CooWebCcRuleArgs | CooWebCcRuleState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as CooWebCcRuleState | undefined;
            resourceInputs["domain"] = state?.domain;
            resourceInputs["name"] = state?.name;
            resourceInputs["ruleDetail"] = state?.ruleDetail;
        } else {
            const args = argsOrState as CooWebCcRuleArgs | undefined;
            if (args?.domain === undefined && !opts.urn) {
                throw new Error("Missing required property 'domain'");
            }
            if (args?.ruleDetail === undefined && !opts.urn) {
                throw new Error("Missing required property 'ruleDetail'");
            }
            resourceInputs["domain"] = args?.domain;
            resourceInputs["name"] = args?.name;
            resourceInputs["ruleDetail"] = args?.ruleDetail;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(CooWebCcRule.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering CooWebCcRule resources.
 */
export interface CooWebCcRuleState {
    /**
     * The domain name of the website service.  
     *
     * > **NOTE:**  The domain name must already have website service forwarding rules configured. You can call [DescribeDomains](https://help.aliyun.com/document_detail/91724.html) to query all domain names.
     */
    domain?: pulumi.Input<string>;
    /**
     * Rule name.
     */
    name?: pulumi.Input<string>;
    /**
     * Rule details.   See `ruleDetail` below.
     */
    ruleDetail?: pulumi.Input<inputs.ddos.CooWebCcRuleRuleDetail>;
}

/**
 * The set of arguments for constructing a CooWebCcRule resource.
 */
export interface CooWebCcRuleArgs {
    /**
     * The domain name of the website service.  
     *
     * > **NOTE:**  The domain name must already have website service forwarding rules configured. You can call [DescribeDomains](https://help.aliyun.com/document_detail/91724.html) to query all domain names.
     */
    domain: pulumi.Input<string>;
    /**
     * Rule name.
     */
    name?: pulumi.Input<string>;
    /**
     * Rule details.   See `ruleDetail` below.
     */
    ruleDetail: pulumi.Input<inputs.ddos.CooWebCcRuleRuleDetail>;
}
