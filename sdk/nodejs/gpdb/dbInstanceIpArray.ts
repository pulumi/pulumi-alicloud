// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provides a GPDB DB Instance IP Array resource.
 *
 * Whitelist IP Group.
 *
 * For information about GPDB DB Instance IP Array and how to use it, see [What is DB Instance IP Array](https://www.alibabacloud.com/help/en/).
 *
 * > **NOTE:** Available since v1.231.0.
 *
 * ## Example Usage
 *
 * Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const _default = alicloud.getZones({
 *     availableResourceCreation: "VSwitch",
 * });
 * const defaultNpLRa1 = new alicloud.vpc.Network("defaultNpLRa1", {cidrBlock: "192.168.0.0/16"});
 * const defaultwLA5v4 = new alicloud.vpc.Switch("defaultwLA5v4", {
 *     vpcId: defaultNpLRa1.id,
 *     zoneId: "cn-beijing-h",
 *     cidrBlock: "192.168.1.0/24",
 * });
 * const defaultHKdDs3 = new alicloud.gpdb.Instance("defaultHKdDs3", {
 *     instanceSpec: "2C8G",
 *     segNodeNum: 2,
 *     segStorageType: "cloud_essd",
 *     instanceNetworkType: "VPC",
 *     dbInstanceCategory: "Basic",
 *     paymentType: "PayAsYouGo",
 *     sslEnabled: 0,
 *     engineVersion: "6.0",
 *     zoneId: "cn-beijing-h",
 *     vswitchId: defaultwLA5v4.id,
 *     storageSize: 50,
 *     masterCu: 4,
 *     vpcId: defaultNpLRa1.id,
 *     dbInstanceMode: "StorageElastic",
 *     engine: "gpdb",
 *     description: name,
 * });
 * const defaultDbInstanceIpArray = new alicloud.gpdb.DbInstanceIpArray("default", {
 *     dbInstanceIpArrayAttribute: "taffyFish",
 *     securityIpLists: [
 *         "12.34.56.78",
 *         "11.45.14.0",
 *         "19.19.81.0",
 *     ],
 *     dbInstanceIpArrayName: "taffy",
 *     dbInstanceId: defaultHKdDs3.id,
 * });
 * ```
 *
 * ## Import
 *
 * GPDB DB Instance IP Array can be imported using the id, e.g.
 *
 * ```sh
 * $ pulumi import alicloud:gpdb/dbInstanceIpArray:DbInstanceIpArray example <db_instance_id>:<db_instance_ip_array_name>
 * ```
 */
export class DbInstanceIpArray extends pulumi.CustomResource {
    /**
     * Get an existing DbInstanceIpArray resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: DbInstanceIpArrayState, opts?: pulumi.CustomResourceOptions): DbInstanceIpArray {
        return new DbInstanceIpArray(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'alicloud:gpdb/dbInstanceIpArray:DbInstanceIpArray';

    /**
     * Returns true if the given object is an instance of DbInstanceIpArray.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is DbInstanceIpArray {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === DbInstanceIpArray.__pulumiType;
    }

    /**
     * The instance ID.
     *
     * > **NOTE:**  You can call the [DescribeDBInstances](https://www.alibabacloud.com/help/en/doc-detail/86911.html) operation to query details about all AnalyticDB for PostgreSQL instances within a region, including instance IDs.
     */
    public readonly dbInstanceId!: pulumi.Output<string>;
    /**
     * The default is empty. To distinguish between different attribute values, the console does not display groups with the 'hidden' attribute.
     */
    public readonly dbInstanceIpArrayAttribute!: pulumi.Output<string | undefined>;
    /**
     * The name of the IP address whitelist. If you do not specify this parameter, the default whitelist is queried.
     *
     * > **NOTE:**   Each instance supports up to 50 IP address whitelists.
     */
    public readonly dbInstanceIpArrayName!: pulumi.Output<string>;
    /**
     * The method of modification. Valid values:
     *
     * - `Cover`: overwrites the whitelist.
     * - `Append`: appends data to the whitelist.
     * - `Delete`: deletes the whitelist.
     */
    public readonly modifyMode!: pulumi.Output<string | undefined>;
    /**
     * The IP address whitelist contains a maximum of 1000 IP addresses separated by commas in the following three formats:
     * - 0.0.0.0/0
     * - 10.23.12.24(IP)
     * - 10.23.12.24/24(CIDR mode, Classless Inter-Domain Routing, '/24' indicates the length of the prefix in the address, and the range is '[1,32]')
     */
    public readonly securityIpLists!: pulumi.Output<string[]>;

    /**
     * Create a DbInstanceIpArray resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: DbInstanceIpArrayArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: DbInstanceIpArrayArgs | DbInstanceIpArrayState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as DbInstanceIpArrayState | undefined;
            resourceInputs["dbInstanceId"] = state ? state.dbInstanceId : undefined;
            resourceInputs["dbInstanceIpArrayAttribute"] = state ? state.dbInstanceIpArrayAttribute : undefined;
            resourceInputs["dbInstanceIpArrayName"] = state ? state.dbInstanceIpArrayName : undefined;
            resourceInputs["modifyMode"] = state ? state.modifyMode : undefined;
            resourceInputs["securityIpLists"] = state ? state.securityIpLists : undefined;
        } else {
            const args = argsOrState as DbInstanceIpArrayArgs | undefined;
            if ((!args || args.dbInstanceId === undefined) && !opts.urn) {
                throw new Error("Missing required property 'dbInstanceId'");
            }
            if ((!args || args.dbInstanceIpArrayName === undefined) && !opts.urn) {
                throw new Error("Missing required property 'dbInstanceIpArrayName'");
            }
            if ((!args || args.securityIpLists === undefined) && !opts.urn) {
                throw new Error("Missing required property 'securityIpLists'");
            }
            resourceInputs["dbInstanceId"] = args ? args.dbInstanceId : undefined;
            resourceInputs["dbInstanceIpArrayAttribute"] = args ? args.dbInstanceIpArrayAttribute : undefined;
            resourceInputs["dbInstanceIpArrayName"] = args ? args.dbInstanceIpArrayName : undefined;
            resourceInputs["modifyMode"] = args ? args.modifyMode : undefined;
            resourceInputs["securityIpLists"] = args ? args.securityIpLists : undefined;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(DbInstanceIpArray.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering DbInstanceIpArray resources.
 */
export interface DbInstanceIpArrayState {
    /**
     * The instance ID.
     *
     * > **NOTE:**  You can call the [DescribeDBInstances](https://www.alibabacloud.com/help/en/doc-detail/86911.html) operation to query details about all AnalyticDB for PostgreSQL instances within a region, including instance IDs.
     */
    dbInstanceId?: pulumi.Input<string>;
    /**
     * The default is empty. To distinguish between different attribute values, the console does not display groups with the 'hidden' attribute.
     */
    dbInstanceIpArrayAttribute?: pulumi.Input<string>;
    /**
     * The name of the IP address whitelist. If you do not specify this parameter, the default whitelist is queried.
     *
     * > **NOTE:**   Each instance supports up to 50 IP address whitelists.
     */
    dbInstanceIpArrayName?: pulumi.Input<string>;
    /**
     * The method of modification. Valid values:
     *
     * - `Cover`: overwrites the whitelist.
     * - `Append`: appends data to the whitelist.
     * - `Delete`: deletes the whitelist.
     */
    modifyMode?: pulumi.Input<string>;
    /**
     * The IP address whitelist contains a maximum of 1000 IP addresses separated by commas in the following three formats:
     * - 0.0.0.0/0
     * - 10.23.12.24(IP)
     * - 10.23.12.24/24(CIDR mode, Classless Inter-Domain Routing, '/24' indicates the length of the prefix in the address, and the range is '[1,32]')
     */
    securityIpLists?: pulumi.Input<pulumi.Input<string>[]>;
}

/**
 * The set of arguments for constructing a DbInstanceIpArray resource.
 */
export interface DbInstanceIpArrayArgs {
    /**
     * The instance ID.
     *
     * > **NOTE:**  You can call the [DescribeDBInstances](https://www.alibabacloud.com/help/en/doc-detail/86911.html) operation to query details about all AnalyticDB for PostgreSQL instances within a region, including instance IDs.
     */
    dbInstanceId: pulumi.Input<string>;
    /**
     * The default is empty. To distinguish between different attribute values, the console does not display groups with the 'hidden' attribute.
     */
    dbInstanceIpArrayAttribute?: pulumi.Input<string>;
    /**
     * The name of the IP address whitelist. If you do not specify this parameter, the default whitelist is queried.
     *
     * > **NOTE:**   Each instance supports up to 50 IP address whitelists.
     */
    dbInstanceIpArrayName: pulumi.Input<string>;
    /**
     * The method of modification. Valid values:
     *
     * - `Cover`: overwrites the whitelist.
     * - `Append`: appends data to the whitelist.
     * - `Delete`: deletes the whitelist.
     */
    modifyMode?: pulumi.Input<string>;
    /**
     * The IP address whitelist contains a maximum of 1000 IP addresses separated by commas in the following three formats:
     * - 0.0.0.0/0
     * - 10.23.12.24(IP)
     * - 10.23.12.24/24(CIDR mode, Classless Inter-Domain Routing, '/24' indicates the length of the prefix in the address, and the range is '[1,32]')
     */
    securityIpLists: pulumi.Input<pulumi.Input<string>[]>;
}
