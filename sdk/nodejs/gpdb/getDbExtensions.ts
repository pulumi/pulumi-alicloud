// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * This data source provides Gpdb Db Extension available to the user.[What is Db Extension](https://next.api.alibabacloud.com/document/gpdb/2016-05-03/CreateExtensions)
 *
 * > **NOTE:** Available since v1.291.0.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const defaultiYyNGW = new alicloud.vpc.Network("defaultiYyNGW", {cidrBlock: "192.168.0.0/16"});
 * const defaultPlruct = new alicloud.vpc.Switch("defaultPlruct", {
 *     vpcId: defaultiYyNGW.id,
 *     zoneId: "cn-beijing-h",
 *     cidrBlock: "192.168.1.0/24",
 * });
 * const defaultqsmpIy = new alicloud.gpdb.Instance("defaultqsmpIy", {
 *     instanceSpec: "2C8G",
 *     segNodeNum: 2,
 *     segStorageType: "cloud_essd",
 *     instanceNetworkType: "VPC",
 *     dbInstanceCategory: "Basic",
 *     paymentType: "PayAsYouGo",
 *     sslEnabled: 0,
 *     engineVersion: "6.0",
 *     engine: "gpdb",
 *     zoneId: "cn-beijing-h",
 *     vswitchId: defaultPlruct.id,
 *     storageSize: 50,
 *     masterCu: 4,
 *     vpcId: defaultiYyNGW.id,
 *     dbInstanceMode: "StorageElastic",
 * });
 * const defaultOwner = new alicloud.gpdb.Account("defaultOwner", {
 *     accountName: "tf_example",
 *     accountPassword: "Example1234",
 *     accountDescription: "tf_example",
 *     dbInstanceId: defaultqsmpIy.id,
 * });
 * const defaultPPmRVa = new alicloud.gpdb.Database("defaultPPmRVa", {
 *     owner: defaultOwner.accountName,
 *     databaseName: "seagull",
 *     dbInstanceId: defaultqsmpIy.id,
 *     characterSetName: "UTF8",
 *     collate: "en_US.utf8",
 *     ctype: "en_US.utf8",
 * });
 * const defaultDbExtension = new alicloud.gpdb.DbExtension("default", {
 *     extensionName: "uuid-ossp",
 *     dbInstanceId: defaultqsmpIy.id,
 *     databaseName: defaultPPmRVa.databaseName,
 *     isLatestVersion: true,
 * });
 * const _default = alicloud.gpdb.getDbExtensionsOutput({
 *     ids: [defaultDbExtension.id],
 *     dbInstanceId: defaultqsmpIy.id,
 *     databaseName: defaultPPmRVa.databaseName,
 * });
 * export const alicloudGpdbDbExtensionExampleId = _default.apply(_default => _default.extensions?.[0]?.id);
 * ```
 */
export function getDbExtensions(args: GetDbExtensionsArgs, opts?: pulumi.InvokeOptions): Promise<GetDbExtensionsResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("alicloud:gpdb/getDbExtensions:getDbExtensions", {
        "databaseName": args.databaseName,
        "dbInstanceId": args.dbInstanceId,
        "enableDetails": args.enableDetails,
        "ids": args.ids,
        "outputFile": args.outputFile,
    }, opts);
}

/**
 * A collection of arguments for invoking getDbExtensions.
 */
export interface GetDbExtensionsArgs {
    /**
     * The name of the database.
     */
    databaseName: string;
    /**
     * The instance ID.
     *
     * > **NOTE:**   You can call the [DescribeDBInstances](https://www.alibabacloud.com/help/en/doc-detail/86911.html) operation to query the information about all AnalyticDB for PostgreSQL instances within a region, including instance IDs.
     */
    dbInstanceId: string;
    /**
     * Default to `false`. Set it to `true` can output more details about resource attributes.
     */
    enableDetails?: boolean;
    /**
     * A list of Db Extension IDs. The value is formulated as `<db_instance_id>:<database_name>:<extension_name>`.
     */
    ids?: string[];
    /**
     * File name where to save data source results (after running `pulumi preview`).
     */
    outputFile?: string;
}

/**
 * A collection of values returned by getDbExtensions.
 */
export interface GetDbExtensionsResult {
    readonly databaseName: string;
    readonly dbInstanceId: string;
    readonly enableDetails?: boolean;
    /**
     * A list of Db Extension Entries. Each element contains the following attributes:
     */
    readonly extensions: outputs.gpdb.GetDbExtensionsExtension[];
    /**
     * The provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
    /**
     * A list of Db Extension IDs.
     */
    readonly ids: string[];
    readonly outputFile?: string;
}
/**
 * This data source provides Gpdb Db Extension available to the user.[What is Db Extension](https://next.api.alibabacloud.com/document/gpdb/2016-05-03/CreateExtensions)
 *
 * > **NOTE:** Available since v1.291.0.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const defaultiYyNGW = new alicloud.vpc.Network("defaultiYyNGW", {cidrBlock: "192.168.0.0/16"});
 * const defaultPlruct = new alicloud.vpc.Switch("defaultPlruct", {
 *     vpcId: defaultiYyNGW.id,
 *     zoneId: "cn-beijing-h",
 *     cidrBlock: "192.168.1.0/24",
 * });
 * const defaultqsmpIy = new alicloud.gpdb.Instance("defaultqsmpIy", {
 *     instanceSpec: "2C8G",
 *     segNodeNum: 2,
 *     segStorageType: "cloud_essd",
 *     instanceNetworkType: "VPC",
 *     dbInstanceCategory: "Basic",
 *     paymentType: "PayAsYouGo",
 *     sslEnabled: 0,
 *     engineVersion: "6.0",
 *     engine: "gpdb",
 *     zoneId: "cn-beijing-h",
 *     vswitchId: defaultPlruct.id,
 *     storageSize: 50,
 *     masterCu: 4,
 *     vpcId: defaultiYyNGW.id,
 *     dbInstanceMode: "StorageElastic",
 * });
 * const defaultOwner = new alicloud.gpdb.Account("defaultOwner", {
 *     accountName: "tf_example",
 *     accountPassword: "Example1234",
 *     accountDescription: "tf_example",
 *     dbInstanceId: defaultqsmpIy.id,
 * });
 * const defaultPPmRVa = new alicloud.gpdb.Database("defaultPPmRVa", {
 *     owner: defaultOwner.accountName,
 *     databaseName: "seagull",
 *     dbInstanceId: defaultqsmpIy.id,
 *     characterSetName: "UTF8",
 *     collate: "en_US.utf8",
 *     ctype: "en_US.utf8",
 * });
 * const defaultDbExtension = new alicloud.gpdb.DbExtension("default", {
 *     extensionName: "uuid-ossp",
 *     dbInstanceId: defaultqsmpIy.id,
 *     databaseName: defaultPPmRVa.databaseName,
 *     isLatestVersion: true,
 * });
 * const _default = alicloud.gpdb.getDbExtensionsOutput({
 *     ids: [defaultDbExtension.id],
 *     dbInstanceId: defaultqsmpIy.id,
 *     databaseName: defaultPPmRVa.databaseName,
 * });
 * export const alicloudGpdbDbExtensionExampleId = _default.apply(_default => _default.extensions?.[0]?.id);
 * ```
 */
export function getDbExtensionsOutput(args: GetDbExtensionsOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetDbExtensionsResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("alicloud:gpdb/getDbExtensions:getDbExtensions", {
        "databaseName": args.databaseName,
        "dbInstanceId": args.dbInstanceId,
        "enableDetails": args.enableDetails,
        "ids": args.ids,
        "outputFile": args.outputFile,
    }, opts);
}

/**
 * A collection of arguments for invoking getDbExtensions.
 */
export interface GetDbExtensionsOutputArgs {
    /**
     * The name of the database.
     */
    databaseName: pulumi.Input<string>;
    /**
     * The instance ID.
     *
     * > **NOTE:**   You can call the [DescribeDBInstances](https://www.alibabacloud.com/help/en/doc-detail/86911.html) operation to query the information about all AnalyticDB for PostgreSQL instances within a region, including instance IDs.
     */
    dbInstanceId: pulumi.Input<string>;
    /**
     * Default to `false`. Set it to `true` can output more details about resource attributes.
     */
    enableDetails?: pulumi.Input<boolean | undefined>;
    /**
     * A list of Db Extension IDs. The value is formulated as `<db_instance_id>:<database_name>:<extension_name>`.
     */
    ids?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * File name where to save data source results (after running `pulumi preview`).
     */
    outputFile?: pulumi.Input<string | undefined>;
}
