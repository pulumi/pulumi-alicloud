// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provides a AnalyticDB for PostgreSQL (GPDB) Db Extension resource.
 *
 * For information about AnalyticDB for PostgreSQL (GPDB) Db Extension and how to use it, see [What is Db Extension](https://next.api.alibabacloud.com/document/gpdb/2016-05-03/CreateExtensions).
 *
 * > **NOTE:** Available since v1.291.0.
 *
 * ## Example Usage
 *
 * Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const defaultiYyNGW = new alicloud.vpc.Network("defaultiYyNGW", {cidrBlock: "192.168.0.0/16"});
 * const defaultPlruct = new alicloud.vpc.Switch("defaultPlruct", {
 *     vpcId: defaultiYyNGW.id,
 *     zoneId: "cn-beijing-h",
 *     cidrBlock: "192.168.1.0/24",
 * });
 * const defaultqsmpIy = new alicloud.gpdb.Instance("defaultqsmpIy", {
 *     instanceSpec: "2C8G",
 *     segNodeNum: 2,
 *     segStorageType: "cloud_essd",
 *     instanceNetworkType: "VPC",
 *     dbInstanceCategory: "Basic",
 *     paymentType: "PayAsYouGo",
 *     sslEnabled: 0,
 *     engineVersion: "6.0",
 *     engine: "gpdb",
 *     zoneId: "cn-beijing-h",
 *     vswitchId: defaultPlruct.id,
 *     storageSize: 50,
 *     masterCu: 4,
 *     vpcId: defaultiYyNGW.id,
 *     dbInstanceMode: "StorageElastic",
 * });
 * const defaultOwner = new alicloud.gpdb.Account("defaultOwner", {
 *     accountName: "tf_example",
 *     accountPassword: "Example1234",
 *     accountDescription: "tf_example",
 *     dbInstanceId: defaultqsmpIy.id,
 * });
 * const defaultPPmRVa = new alicloud.gpdb.Database("defaultPPmRVa", {
 *     owner: defaultOwner.accountName,
 *     databaseName: "seagull",
 *     dbInstanceId: defaultqsmpIy.id,
 *     characterSetName: "UTF8",
 *     collate: "en_US.utf8",
 *     ctype: "en_US.utf8",
 * });
 * const _default = new alicloud.gpdb.DbExtension("default", {
 *     extensionName: "uuid-ossp",
 *     dbInstanceId: defaultqsmpIy.id,
 *     databaseName: defaultPPmRVa.databaseName,
 *     isLaexampleVersion: true,
 * });
 * ```
 *
 * 📚 Need more examples? VIEW MORE EXAMPLES
 *
 * ## Import
 *
 * AnalyticDB for PostgreSQL (GPDB) Db Extension can be imported using the id, e.g.
 *
 * ```sh
 * $ pulumi import alicloud:gpdb/dbExtension:DbExtension example <db_instance_id>:<database_name>:<extension_name>
 * ```
 */
export class DbExtension extends pulumi.CustomResource {
    /**
     * Get an existing DbExtension resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: DbExtensionState, opts?: pulumi.CustomResourceOptions): DbExtension {
        return new DbExtension(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'alicloud:gpdb/dbExtension:DbExtension';

    /**
     * Returns true if the given object is an instance of DbExtension.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is DbExtension {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === DbExtension.__pulumiType;
    }

    /**
     * Plug-in current version.
     */
    declare public /*out*/ readonly currentVersion: pulumi.Output<string>;
    /**
     * The name of the database.
     */
    declare public readonly databaseName: pulumi.Output<string>;
    /**
     * The instance ID.
     *
     * > **NOTE:**   You can call the [DescribeDBInstances](https://www.alibabacloud.com/help/en/doc-detail/86911.html) operation to query the information about all AnalyticDB for PostgreSQL instances within a region, including instance IDs.
     */
    declare public readonly dbInstanceId: pulumi.Output<string>;
    /**
     * Plug-in description.
     */
    declare public /*out*/ readonly description: pulumi.Output<string>;
    /**
     * Plug-in id.
     */
    declare public /*out*/ readonly extensionId: pulumi.Output<string>;
    /**
     * The name of the extension to install. Each resource manages exactly one extension; multiple extension names are not supported.
     */
    declare public readonly extensionName: pulumi.Output<string>;
    /**
     * Whether the instance needs to be restarted for installation.
     */
    declare public /*out*/ readonly isInstallNeedRestart: pulumi.Output<boolean>;
    /**
     * Whether the extension is at its latest version. Setting it to true calls UpgradeExtensions to upgrade the extension to the latest version. Leaving it unset or setting it to false triggers no upgrade and does not downgrade the extension. On read, this field is refreshed with the actual value returned by the server.
     */
    declare public readonly isLatestVersion: pulumi.Output<boolean>;
    /**
     * Plug-in latest version.
     */
    declare public /*out*/ readonly latestVersion: pulumi.Output<string>;
    /**
     * The status of the extension.
     */
    declare public /*out*/ readonly status: pulumi.Output<string>;

    /**
     * Create a DbExtension resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: DbExtensionArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: DbExtensionArgs | DbExtensionState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as DbExtensionState | undefined;
            resourceInputs["currentVersion"] = state?.currentVersion;
            resourceInputs["databaseName"] = state?.databaseName;
            resourceInputs["dbInstanceId"] = state?.dbInstanceId;
            resourceInputs["description"] = state?.description;
            resourceInputs["extensionId"] = state?.extensionId;
            resourceInputs["extensionName"] = state?.extensionName;
            resourceInputs["isInstallNeedRestart"] = state?.isInstallNeedRestart;
            resourceInputs["isLatestVersion"] = state?.isLatestVersion;
            resourceInputs["latestVersion"] = state?.latestVersion;
            resourceInputs["status"] = state?.status;
        } else {
            const args = argsOrState as DbExtensionArgs | undefined;
            if (args?.databaseName === undefined && !opts.urn) {
                throw new Error("Missing required property 'databaseName'");
            }
            if (args?.dbInstanceId === undefined && !opts.urn) {
                throw new Error("Missing required property 'dbInstanceId'");
            }
            if (args?.extensionName === undefined && !opts.urn) {
                throw new Error("Missing required property 'extensionName'");
            }
            resourceInputs["databaseName"] = args?.databaseName;
            resourceInputs["dbInstanceId"] = args?.dbInstanceId;
            resourceInputs["extensionName"] = args?.extensionName;
            resourceInputs["isLatestVersion"] = args?.isLatestVersion;
            resourceInputs["currentVersion"] = undefined /*out*/;
            resourceInputs["description"] = undefined /*out*/;
            resourceInputs["extensionId"] = undefined /*out*/;
            resourceInputs["isInstallNeedRestart"] = undefined /*out*/;
            resourceInputs["latestVersion"] = undefined /*out*/;
            resourceInputs["status"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(DbExtension.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering DbExtension resources.
 */
export interface DbExtensionState {
    /**
     * Plug-in current version.
     */
    currentVersion?: pulumi.Input<string | undefined>;
    /**
     * The name of the database.
     */
    databaseName?: pulumi.Input<string | undefined>;
    /**
     * The instance ID.
     *
     * > **NOTE:**   You can call the [DescribeDBInstances](https://www.alibabacloud.com/help/en/doc-detail/86911.html) operation to query the information about all AnalyticDB for PostgreSQL instances within a region, including instance IDs.
     */
    dbInstanceId?: pulumi.Input<string | undefined>;
    /**
     * Plug-in description.
     */
    description?: pulumi.Input<string | undefined>;
    /**
     * Plug-in id.
     */
    extensionId?: pulumi.Input<string | undefined>;
    /**
     * The name of the extension to install. Each resource manages exactly one extension; multiple extension names are not supported.
     */
    extensionName?: pulumi.Input<string | undefined>;
    /**
     * Whether the instance needs to be restarted for installation.
     */
    isInstallNeedRestart?: pulumi.Input<boolean | undefined>;
    /**
     * Whether the extension is at its latest version. Setting it to true calls UpgradeExtensions to upgrade the extension to the latest version. Leaving it unset or setting it to false triggers no upgrade and does not downgrade the extension. On read, this field is refreshed with the actual value returned by the server.
     */
    isLatestVersion?: pulumi.Input<boolean | undefined>;
    /**
     * Plug-in latest version.
     */
    latestVersion?: pulumi.Input<string | undefined>;
    /**
     * The status of the extension.
     */
    status?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a DbExtension resource.
 */
export interface DbExtensionArgs {
    /**
     * The name of the database.
     */
    databaseName: pulumi.Input<string>;
    /**
     * The instance ID.
     *
     * > **NOTE:**   You can call the [DescribeDBInstances](https://www.alibabacloud.com/help/en/doc-detail/86911.html) operation to query the information about all AnalyticDB for PostgreSQL instances within a region, including instance IDs.
     */
    dbInstanceId: pulumi.Input<string>;
    /**
     * The name of the extension to install. Each resource manages exactly one extension; multiple extension names are not supported.
     */
    extensionName: pulumi.Input<string>;
    /**
     * Whether the extension is at its latest version. Setting it to true calls UpgradeExtensions to upgrade the extension to the latest version. Leaving it unset or setting it to false triggers no upgrade and does not downgrade the extension. On read, this field is refreshed with the actual value returned by the server.
     */
    isLatestVersion?: pulumi.Input<boolean | undefined>;
}
