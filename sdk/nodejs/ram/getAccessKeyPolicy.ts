// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provides a RAM Access Key Policy data source.
 *
 * Reads the network access restriction policy for the AccessKey of an Alibaba Cloud account (primary account) or a RAM user.
 *
 * For information about RAM Access Key Policy and how to use it, see [What is Access Key Policy](https://www.alibabacloud.com/help/en/ram/developer-reference/api-ims-2019-08-15-getaccesskeypolicy).
 *
 * > **NOTE:** Available since v1.286.0.
 *
 * ## Example Usage
 *
 * Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const _default = alicloud.getAccount({});
 * const defaultUser = new alicloud.ram.User("default", {name: name});
 * const defaultAccessKey = new alicloud.ram.AccessKey("default", {userName: defaultUser.name});
 * const defaultAccessKeyPolicy = new alicloud.ram.AccessKeyPolicy("default", {
 *     userAccessKeyId: defaultAccessKey.id,
 *     userPrincipalName: pulumi.all([defaultUser.name, _default]).apply(([name, _default]) => `${name}@${_default.id}.onaliyun.com`),
 *     accessKeyPolicy: JSON.stringify({
 *         Status: "Active",
 *         Statements: [{
 *             Type: "ClassicWhiteList",
 *             IPList: ["10.0.0.1/32"],
 *         }],
 *     }),
 * });
 * const defaultGetAccessKeyPolicy = alicloud.ram.getAccessKeyPolicyOutput({
 *     userAccessKeyId: defaultAccessKeyPolicy.userAccessKeyId,
 *     userPrincipalName: defaultAccessKeyPolicy.userPrincipalName,
 * });
 * ```
 */
export function getAccessKeyPolicy(args: GetAccessKeyPolicyArgs, opts?: pulumi.InvokeOptions): Promise<GetAccessKeyPolicyResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("alicloud:ram/getAccessKeyPolicy:getAccessKeyPolicy", {
        "userAccessKeyId": args.userAccessKeyId,
        "userPrincipalName": args.userPrincipalName,
    }, opts);
}

/**
 * A collection of arguments for invoking getAccessKeyPolicy.
 */
export interface GetAccessKeyPolicyArgs {
    /**
     * The ID of the access key whose network access restriction policy is to be read.
     */
    userAccessKeyId: string;
    /**
     * The logon name of the RAM user. Specify this parameter when reading the access key policy of another RAM user. If it is left empty, the policy of the specified access key of the current user is read.
     */
    userPrincipalName?: string;
}

/**
 * A collection of values returned by getAccessKeyPolicy.
 */
export interface GetAccessKeyPolicyResult {
    /**
     * The network access restriction policy in JSON format.
     */
    readonly accessKeyPolicy: string;
    /**
     * The provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
    readonly userAccessKeyId: string;
    readonly userPrincipalName?: string;
}
/**
 * Provides a RAM Access Key Policy data source.
 *
 * Reads the network access restriction policy for the AccessKey of an Alibaba Cloud account (primary account) or a RAM user.
 *
 * For information about RAM Access Key Policy and how to use it, see [What is Access Key Policy](https://www.alibabacloud.com/help/en/ram/developer-reference/api-ims-2019-08-15-getaccesskeypolicy).
 *
 * > **NOTE:** Available since v1.286.0.
 *
 * ## Example Usage
 *
 * Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "terraform-example";
 * const _default = alicloud.getAccount({});
 * const defaultUser = new alicloud.ram.User("default", {name: name});
 * const defaultAccessKey = new alicloud.ram.AccessKey("default", {userName: defaultUser.name});
 * const defaultAccessKeyPolicy = new alicloud.ram.AccessKeyPolicy("default", {
 *     userAccessKeyId: defaultAccessKey.id,
 *     userPrincipalName: pulumi.all([defaultUser.name, _default]).apply(([name, _default]) => `${name}@${_default.id}.onaliyun.com`),
 *     accessKeyPolicy: JSON.stringify({
 *         Status: "Active",
 *         Statements: [{
 *             Type: "ClassicWhiteList",
 *             IPList: ["10.0.0.1/32"],
 *         }],
 *     }),
 * });
 * const defaultGetAccessKeyPolicy = alicloud.ram.getAccessKeyPolicyOutput({
 *     userAccessKeyId: defaultAccessKeyPolicy.userAccessKeyId,
 *     userPrincipalName: defaultAccessKeyPolicy.userPrincipalName,
 * });
 * ```
 */
export function getAccessKeyPolicyOutput(args: GetAccessKeyPolicyOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetAccessKeyPolicyResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("alicloud:ram/getAccessKeyPolicy:getAccessKeyPolicy", {
        "userAccessKeyId": args.userAccessKeyId,
        "userPrincipalName": args.userPrincipalName,
    }, opts);
}

/**
 * A collection of arguments for invoking getAccessKeyPolicy.
 */
export interface GetAccessKeyPolicyOutputArgs {
    /**
     * The ID of the access key whose network access restriction policy is to be read.
     */
    userAccessKeyId: pulumi.Input<string>;
    /**
     * The logon name of the RAM user. Specify this parameter when reading the access key policy of another RAM user. If it is left empty, the policy of the specified access key of the current user is read.
     */
    userPrincipalName?: pulumi.Input<string | undefined>;
}
