// *** WARNING: this file was generated by the Pulumi Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * Provides an Alicloud Serverless App Engine (SAE) Application Load Balancer Attachment resource.
 *
 * For information about Serverless App Engine (SAE) Load Balancer Intranet Attachment and how to use it, see [alicloud.sae.LoadBalancerIntranet](https://www.alibabacloud.com/help/en/sae/latest/bindslb).
 *
 * > **NOTE:** Available since v1.165.0.
 *
 * ## Example Usage
 *
 * Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as alicloud from "@pulumi/alicloud";
 * import * as random from "@pulumi/random";
 *
 * const config = new pulumi.Config();
 * const name = config.get("name") || "tf-example";
 * const defaultRegions = alicloud.getRegions({
 *     current: true,
 * });
 * const defaultRandomInteger = new random.RandomInteger("defaultRandomInteger", {
 *     max: 99999,
 *     min: 10000,
 * });
 * const defaultZones = alicloud.getZones({
 *     availableResourceCreation: "VSwitch",
 * });
 * const defaultNetwork = new alicloud.vpc.Network("defaultNetwork", {
 *     vpcName: name,
 *     cidrBlock: "10.4.0.0/16",
 * });
 * const defaultSwitch = new alicloud.vpc.Switch("defaultSwitch", {
 *     vswitchName: name,
 *     cidrBlock: "10.4.0.0/24",
 *     vpcId: defaultNetwork.id,
 *     zoneId: defaultZones.then(defaultZones => defaultZones.zones?.[0]?.id),
 * });
 * const defaultSecurityGroup = new alicloud.ecs.SecurityGroup("defaultSecurityGroup", {vpcId: defaultNetwork.id});
 * const defaultNamespace = new alicloud.sae.Namespace("defaultNamespace", {
 *     namespaceId: pulumi.all([defaultRegions, defaultRandomInteger.result]).apply(([defaultRegions, result]) => `${defaultRegions.regions?.[0]?.id}:example${result}`),
 *     namespaceName: name,
 *     namespaceDescription: name,
 *     enableMicroRegistration: false,
 * });
 * const defaultApplication = new alicloud.sae.Application("defaultApplication", {
 *     appDescription: name,
 *     appName: name,
 *     namespaceId: defaultNamespace.id,
 *     imageUrl: "registry-vpc.cn-hangzhou.aliyuncs.com/lxepoo/apache-php5",
 *     packageType: "Image",
 *     jdk: "Open JDK 8",
 *     securityGroupId: defaultSecurityGroup.id,
 *     vpcId: defaultNetwork.id,
 *     vswitchId: defaultSwitch.id,
 *     timezone: "Asia/Beijing",
 *     replicas: 5,
 *     cpu: 500,
 *     memory: 2048,
 * });
 * const defaultApplicationLoadBalancer = new alicloud.slb.ApplicationLoadBalancer("defaultApplicationLoadBalancer", {
 *     loadBalancerName: name,
 *     vswitchId: defaultSwitch.id,
 *     loadBalancerSpec: "slb.s2.small",
 *     addressType: "intranet",
 * });
 * const defaultLoadBalancerIntranet = new alicloud.sae.LoadBalancerIntranet("defaultLoadBalancerIntranet", {
 *     appId: defaultApplication.id,
 *     intranetSlbId: defaultApplicationLoadBalancer.id,
 *     intranets: [{
 *         protocol: "TCP",
 *         port: 80,
 *         targetPort: 8080,
 *     }],
 * });
 * ```
 *
 * ## Import
 *
 * The resource can be imported using the id, e.g.
 *
 * ```sh
 *  $ pulumi import alicloud:sae/loadBalancerIntranet:LoadBalancerIntranet example <id>
 * ```
 */
export class LoadBalancerIntranet extends pulumi.CustomResource {
    /**
     * Get an existing LoadBalancerIntranet resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: LoadBalancerIntranetState, opts?: pulumi.CustomResourceOptions): LoadBalancerIntranet {
        return new LoadBalancerIntranet(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'alicloud:sae/loadBalancerIntranet:LoadBalancerIntranet';

    /**
     * Returns true if the given object is an instance of LoadBalancerIntranet.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is LoadBalancerIntranet {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === LoadBalancerIntranet.__pulumiType;
    }

    /**
     * The target application ID that needs to be bound to the SLB.
     */
    public readonly appId!: pulumi.Output<string>;
    /**
     * Use designated private network SLBs that have been purchased to support non-shared instances.
     */
    public /*out*/ readonly intranetIp!: pulumi.Output<string>;
    /**
     * The intranet SLB ID.
     */
    public readonly intranetSlbId!: pulumi.Output<string | undefined>;
    /**
     * The bound private network SLB. See `intranet` below.
     */
    public readonly intranets!: pulumi.Output<outputs.sae.LoadBalancerIntranetIntranet[]>;

    /**
     * Create a LoadBalancerIntranet resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: LoadBalancerIntranetArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: LoadBalancerIntranetArgs | LoadBalancerIntranetState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as LoadBalancerIntranetState | undefined;
            resourceInputs["appId"] = state ? state.appId : undefined;
            resourceInputs["intranetIp"] = state ? state.intranetIp : undefined;
            resourceInputs["intranetSlbId"] = state ? state.intranetSlbId : undefined;
            resourceInputs["intranets"] = state ? state.intranets : undefined;
        } else {
            const args = argsOrState as LoadBalancerIntranetArgs | undefined;
            if ((!args || args.appId === undefined) && !opts.urn) {
                throw new Error("Missing required property 'appId'");
            }
            if ((!args || args.intranets === undefined) && !opts.urn) {
                throw new Error("Missing required property 'intranets'");
            }
            resourceInputs["appId"] = args ? args.appId : undefined;
            resourceInputs["intranetSlbId"] = args ? args.intranetSlbId : undefined;
            resourceInputs["intranets"] = args ? args.intranets : undefined;
            resourceInputs["intranetIp"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(LoadBalancerIntranet.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering LoadBalancerIntranet resources.
 */
export interface LoadBalancerIntranetState {
    /**
     * The target application ID that needs to be bound to the SLB.
     */
    appId?: pulumi.Input<string>;
    /**
     * Use designated private network SLBs that have been purchased to support non-shared instances.
     */
    intranetIp?: pulumi.Input<string>;
    /**
     * The intranet SLB ID.
     */
    intranetSlbId?: pulumi.Input<string>;
    /**
     * The bound private network SLB. See `intranet` below.
     */
    intranets?: pulumi.Input<pulumi.Input<inputs.sae.LoadBalancerIntranetIntranet>[]>;
}

/**
 * The set of arguments for constructing a LoadBalancerIntranet resource.
 */
export interface LoadBalancerIntranetArgs {
    /**
     * The target application ID that needs to be bound to the SLB.
     */
    appId: pulumi.Input<string>;
    /**
     * The intranet SLB ID.
     */
    intranetSlbId?: pulumi.Input<string>;
    /**
     * The bound private network SLB. See `intranet` below.
     */
    intranets: pulumi.Input<pulumi.Input<inputs.sae.LoadBalancerIntranetIntranet>[]>;
}
