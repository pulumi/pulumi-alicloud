// *** WARNING: this file was generated by pulumi-language-java. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

package com.pulumi.alicloud.sslcertificatesservice.inputs;

import com.pulumi.core.Output;
import com.pulumi.core.annotations.Import;
import com.pulumi.exceptions.MissingRequiredPropertyException;
import java.lang.String;
import java.util.Objects;


public final class InstanceParameterArgs extends com.pulumi.resources.ResourceArgs {

    public static final InstanceParameterArgs Empty = new InstanceParameterArgs();

    /**
     * The module code. Common values are `fullSpec` (certificate brand and grade for a single-domain certificate), `wildcardSpec` (the same for a wildcard certificate), `fullDomainCount` (number of single domain names) and `wildcardDomainCount` (number of wildcard domain names).
     * 
     */
    @Import(name="code", required=true)
    private Output<String> code;

    /**
     * @return The module code. Common values are `fullSpec` (certificate brand and grade for a single-domain certificate), `wildcardSpec` (the same for a wildcard certificate), `fullDomainCount` (number of single domain names) and `wildcardDomainCount` (number of wildcard domain names).
     * 
     */
    public Output<String> code() {
        return this.code;
    }

    /**
     * The value of the module identified by `code`. For the domain-count modules this is an integer from 1 to 150. For `fullSpec` and `wildcardSpec` this is a specification code such as `ws.dv.f`.
     * 
     * Specification codes accepted by `fullSpec` follow a `brand.grade.type` pattern, for example:
     * 
     * | Value | Brand | Grade |
     * | --- | --- | --- |
     * | `ws.dv.f` | WoSign | DV |
     * | `rap.dv.f` | Rapid | DV |
     * | `vt.dv.f` / `vt.ov.f` | vTrus | DV / OV |
     * | `cf.ov.f` / `cf.ev.f` | CFCA | OV / EV |
     * | `gs.dv.f` / `gs.ov.f` | GlobalSign | DV / OV |
     * | `geo.dv.f` / `geo.ov.f` / `geo.ev.f` | GeoTrust | DV / OV / EV |
     * | `ss.ov.f` / `ss.ev.f` | DigiCert | OV / EV |
     * 
     * &gt; **NOTE:** The list above is a snapshot and is not exhaustive. Specifications, prices and availability differ per account and change over time. Call [DescribePricingModule](https://next.api.alibabacloud.com/document/BssOpenApi/2017-12-14/DescribePricingModule) with `ProductCode` and `ProductType` set to `cas` to get the values your account can actually order.
     * 
     */
    @Import(name="value", required=true)
    private Output<String> value;

    /**
     * @return The value of the module identified by `code`. For the domain-count modules this is an integer from 1 to 150. For `fullSpec` and `wildcardSpec` this is a specification code such as `ws.dv.f`.
     * 
     * Specification codes accepted by `fullSpec` follow a `brand.grade.type` pattern, for example:
     * 
     * | Value | Brand | Grade |
     * | --- | --- | --- |
     * | `ws.dv.f` | WoSign | DV |
     * | `rap.dv.f` | Rapid | DV |
     * | `vt.dv.f` / `vt.ov.f` | vTrus | DV / OV |
     * | `cf.ov.f` / `cf.ev.f` | CFCA | OV / EV |
     * | `gs.dv.f` / `gs.ov.f` | GlobalSign | DV / OV |
     * | `geo.dv.f` / `geo.ov.f` / `geo.ev.f` | GeoTrust | DV / OV / EV |
     * | `ss.ov.f` / `ss.ev.f` | DigiCert | OV / EV |
     * 
     * &gt; **NOTE:** The list above is a snapshot and is not exhaustive. Specifications, prices and availability differ per account and change over time. Call [DescribePricingModule](https://next.api.alibabacloud.com/document/BssOpenApi/2017-12-14/DescribePricingModule) with `ProductCode` and `ProductType` set to `cas` to get the values your account can actually order.
     * 
     */
    public Output<String> value() {
        return this.value;
    }

    private InstanceParameterArgs() {}

    private InstanceParameterArgs(InstanceParameterArgs $) {
        this.code = $.code;
        this.value = $.value;
    }

    public static Builder builder() {
        return new Builder();
    }
    public static Builder builder(InstanceParameterArgs defaults) {
        return new Builder(defaults);
    }

    public static final class Builder {
        private InstanceParameterArgs $;

        public Builder() {
            $ = new InstanceParameterArgs();
        }

        public Builder(InstanceParameterArgs defaults) {
            $ = new InstanceParameterArgs(Objects.requireNonNull(defaults));
        }

        /**
         * @param code The module code. Common values are `fullSpec` (certificate brand and grade for a single-domain certificate), `wildcardSpec` (the same for a wildcard certificate), `fullDomainCount` (number of single domain names) and `wildcardDomainCount` (number of wildcard domain names).
         * 
         * @return builder
         * 
         */
        public Builder code(Output<String> code) {
            $.code = code;
            return this;
        }

        /**
         * @param code The module code. Common values are `fullSpec` (certificate brand and grade for a single-domain certificate), `wildcardSpec` (the same for a wildcard certificate), `fullDomainCount` (number of single domain names) and `wildcardDomainCount` (number of wildcard domain names).
         * 
         * @return builder
         * 
         */
        public Builder code(String code) {
            return code(Output.of(code));
        }

        /**
         * @param value The value of the module identified by `code`. For the domain-count modules this is an integer from 1 to 150. For `fullSpec` and `wildcardSpec` this is a specification code such as `ws.dv.f`.
         * 
         * Specification codes accepted by `fullSpec` follow a `brand.grade.type` pattern, for example:
         * 
         * | Value | Brand | Grade |
         * | --- | --- | --- |
         * | `ws.dv.f` | WoSign | DV |
         * | `rap.dv.f` | Rapid | DV |
         * | `vt.dv.f` / `vt.ov.f` | vTrus | DV / OV |
         * | `cf.ov.f` / `cf.ev.f` | CFCA | OV / EV |
         * | `gs.dv.f` / `gs.ov.f` | GlobalSign | DV / OV |
         * | `geo.dv.f` / `geo.ov.f` / `geo.ev.f` | GeoTrust | DV / OV / EV |
         * | `ss.ov.f` / `ss.ev.f` | DigiCert | OV / EV |
         * 
         * &gt; **NOTE:** The list above is a snapshot and is not exhaustive. Specifications, prices and availability differ per account and change over time. Call [DescribePricingModule](https://next.api.alibabacloud.com/document/BssOpenApi/2017-12-14/DescribePricingModule) with `ProductCode` and `ProductType` set to `cas` to get the values your account can actually order.
         * 
         * @return builder
         * 
         */
        public Builder value(Output<String> value) {
            $.value = value;
            return this;
        }

        /**
         * @param value The value of the module identified by `code`. For the domain-count modules this is an integer from 1 to 150. For `fullSpec` and `wildcardSpec` this is a specification code such as `ws.dv.f`.
         * 
         * Specification codes accepted by `fullSpec` follow a `brand.grade.type` pattern, for example:
         * 
         * | Value | Brand | Grade |
         * | --- | --- | --- |
         * | `ws.dv.f` | WoSign | DV |
         * | `rap.dv.f` | Rapid | DV |
         * | `vt.dv.f` / `vt.ov.f` | vTrus | DV / OV |
         * | `cf.ov.f` / `cf.ev.f` | CFCA | OV / EV |
         * | `gs.dv.f` / `gs.ov.f` | GlobalSign | DV / OV |
         * | `geo.dv.f` / `geo.ov.f` / `geo.ev.f` | GeoTrust | DV / OV / EV |
         * | `ss.ov.f` / `ss.ev.f` | DigiCert | OV / EV |
         * 
         * &gt; **NOTE:** The list above is a snapshot and is not exhaustive. Specifications, prices and availability differ per account and change over time. Call [DescribePricingModule](https://next.api.alibabacloud.com/document/BssOpenApi/2017-12-14/DescribePricingModule) with `ProductCode` and `ProductType` set to `cas` to get the values your account can actually order.
         * 
         * @return builder
         * 
         */
        public Builder value(String value) {
            return value(Output.of(value));
        }

        public InstanceParameterArgs build() {
            if ($.code == null) {
                throw new MissingRequiredPropertyException("InstanceParameterArgs", "code");
            }
            if ($.value == null) {
                throw new MissingRequiredPropertyException("InstanceParameterArgs", "value");
            }
            return $;
        }
    }

}
